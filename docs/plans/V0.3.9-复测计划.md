# V0.3.9 复测计划（S4-R1）

状态：**S4-R1 与 S4-R2 两轮复测均已执行（桌面端，2026-09-10）**；S4-R2 复测批次的结果见 §15。逐项结果与证据见《V0.3.9-验收记录.md》§7–§11 与 `evidence/retest-20260910/`；问题登记见《V0.3.9-问题与修复台账.md》`V039-R1-001` 与「复测批次回写（S4-R1）」。
执行结论摘要：修复批次的 9 条修复项（001、002、003、014、015、016、017、018、019）**全部通过**；008 经导出 payload 判定为 reasoning-only 的思考段并**关闭**；009 通过；B-05 与 B-01 的出站故障三形态达成；**B-03 的负向三条失败**（剥离未交付）；**B-01 的名称解析失败形态受阻**（端点校验使候选无法以保留域进入可用运行态）；开测前准备暴露 **V039-R1-001**（首启清理脚本的日志清理未生效）。**移动端未执行**，按用户指示待桌面端问题收敛后再排期。

本计划依据 2026-09-10 五点产品级决策编写，输入为 `evidence/batch-2026-09-10/`（S4 桌面批次，通过 6 / 失败 5 / 受阻子项 4）与 `docs/plans/V0.3.9-问题与修复台账.md`（19 条）。
执行要求沿用《V0.3.9-真机验证补充测试.md》：状态仅四种，缺入口即失败，问题按 §10 格式入台账，「修改方式」固定句原样且不执行。**S4 整批只测不修**，本计划中的开发交付由修复批次完成，复测批次只验证。

---

## 1. 产品级决策与本计划的映射

| 决策 | 内容 | 对计划的影响 |
| --- | --- | --- |
| B-01 | 用简易沙盒进行测试 | 断网类注入不再依赖主机防火墙权限。沙盒方案见 §4，A06「断网」子项按沙盒重建 |
| B-02 | 播放数量已足够，不再重测，固化现有结果 | 语音合成条数与限流阈值不再复测；固化数据见 §7；语音侧复测范围收窄到 V039-S4-016 / 018 的行为 |
| B-03 | 以最小成本彻底剥离 Codex 要求，只支持 OpenAI Chat Completions 兼容端点 | 原 A07 Codex 事件子项取消；新增「仅 Chat Completions 可用」的正向验证，见 §5 |
| B-04 | A05 标注为再也不进行测试 | A05 记为**不适用（产品决策）**，从所有待测清单移除，见 §8 |
| B-05 | 按替代方案进行 | A08 混合时间线改用可真实委派的 pair 承载世界书卡后重取，见 §6 |

---

## 2. 范围

**复测对象**（本轮只覆盖以下条目，其余不动）：

| 组 | 条目 | 说明 |
| --- | --- | --- |
| P1 修复复验 | V039-S4-001、002、003 | 协议序列化、首次运行模式、记忆写入与入口 |
| P2 修复复验 | V039-S4-014、015、016、017、018 | 配置静默改写、失败原因缺失、播放控制守卫、装配预算、抢占误报失败 |
| 取证复测 | V039-S4-019 | 空输出告警与重试归因（需先补 INFO 级日志） |
| 子项复测 | B-01（A06 断网）、B-05（A08 混合时间线） | 沙盒与 pair 组合重做 |
| 正向验证 | B-03（仅 Chat Completions 可用） | 范围变更后的新验收项 |
| 待确认 | V039-S4-008、009 | 与 B-05 同批取证的既有待确认项 |

**不在范围**：

- **A05**：产品决策为永久不测，记不适用（§8）。
- **移动端 A10/A11 与移动平台矩阵**：按移动端单独安排，不得用桌面结果替代。
- 已通过且本轮无代码变更的用例（A01、A04、A12、A13、A14）：依《补充测试》最高指令，已完成测试严禁重复再测，仅在本轮改动触及其实证路径时才做定点回归（§9 给出触发条件）。
- 语音合成条数、限流阈值：按 B-02 不再复测。

---

## 3. 开测前检查（每轮必做）

1. 冻结候选提交与契约版本，核对 `batch-manifest.json` 中的 EXE / Sidecar / 前端资源 / 夹具 SHA256；发现候选被替换、重建或资源变更立即停止并上报。
2. 设备、音频设备、网络形态、真实服务与模型标识重新登记（只记供应商与模型标识，不写密钥）。
3. **本轮新增取证要求（上一批的缺口）**：Sidecar 原始 stdout/stderr **全量落盘，不按日志级别过滤**。上一批的 `sidecar.log` 只保留了 WARNING 及以上，直接导致 V039-S4-019 无法归因、也无法确认抢占路径是否触发（`logger.info("抢占中断旧合成任务")` 被过滤掉）。复测批次的 `sidecar.log` 必须是未过滤全文，并单独保留一份 `sidecar-info.log` 便于按级别检索。
4. 证据根目录 `evidence/retest-YYYYMMDD/`，每用例一个目录，含 `result.json`、`commands.ps1`、`ws-frames.log`、`http.log`、`sidecar.log`、`tauri.log`、截图/录像与 `notes.md`。
5. 协议脚本沿用 `tools/ph_client.py`（真实鉴权 token、逐帧时间戳、token 只记 SHA256 前缀），新增用例脚本置于 `tools/`。

---

## 4. B-01 沙盒方案

本机现状（2026-09-10 实测）：WSL2 已安装且含 Ubuntu 发行版；Hypervisor 已启用；当前会话**非管理员**；`C:\Windows\System32\WindowsSandbox.exe` **不存在**，Windows 沙盒不可用。

### 4.1 首选：进程级网络沙盒（本地 TCP 转发器 + `HTTPS_PROXY`）

思路是把「断网」下沉到本机一个可控的 TCP 转发器，用环境变量把候选的出站流量引过去，再按需要掐断它。全程不需要管理员权限，且可一键还原。

- **实现**：复用 `tools/run_a14.py` 中已有的 `TcpProxy`（`start()` / `cut()` 通过 `writer.transport.abort()` / `stop()`），扩展为 `tools/net_sandbox.py`，固定监听本机端口并把上游指向 `api.deepseek.com:443`。
- **接入**：为启动候选的用户会话设置 `HTTPS_PROXY=http://127.0.0.1:<port>`（用户级，非机器级，可 `setx` 还原或仅在测试进程环境内注入）；启动 EXE 前完成设置，结束后清除并核对已清除。
- **四种故障形态**（对应补充测试要求的错误注入）：

  | 形态 | 沙盒动作 | 产品侧应观察到的现象 |
  | --- | --- | --- |
  | 连接被拒 | 转发器停止监听（`stop()`） | 连接失败，错误可定位到连接阶段 |
  | 连接被重置 | 已建立连接上 `cut()` | 传输中断，错误可定位到传输阶段 |
  | 卡住/超时 | 暂停转发不关闭 | 请求悬挂至超时，超时原因可见 |
  | 名称解析失败 | 转发器改为解析不可达主机名并返回失败 | 解析阶段失败，区别于前三种 |

- **验证前提（执行时先确认，不成立则改用 4.2）**：候选的 HTTP 客户端是否读取代理环境变量。若读取，`HTTPS_PROXY` 即可生效，且**无需**写 `dialogue.base_url`——这一点很关键，因为 V039-S4-014 已证明 `base_url` 写入会被静默改写，走配置改端点在该缺陷修复前不可靠。
- **隔离性说明**：沙盒只影响被设置环境变量的进程，不影响主机其他程序；测试结束后必须清除环境变量并留证。

### 4.2 备选：WSL2 内断网

WSL2 自带独立网络命名空间，`wsl -u root` 免 Windows 管理员即可 `ip link set eth0 down` 或 `iptables -A OUTPUT -j DROP`，断网与恢复都是秒级、可脚本化。

- **前提**：候选 Sidecar 需能在 Linux 下启动。执行前先做一次最小启动探测（`python -m pair_harness.desktop_backend --help` 及真实启动）。
- **限制**：Sidecar 含 Windows 专有路径（电源状态、音频设备等）；若启动探测失败，本备选作废，不强行改造候选。
- 若可用，本方案对「DNS 失败」形态更真实（可在 WSL 内改 `/etc/resolv.conf` 指向无效解析器）。

### 4.3 兜底

一次性提升权限的会话或 Hyper-V 虚拟机（Hypervisor 已启用）。仅当 4.1 与 4.2 均不成立时启用，并在 `result.json` 中说明前两种方案失败的具体原因。

---

## 5. B-03 Codex 剥离：开发交付与复测验证

**开发轨交付**（修复批次，本计划不执行）：

1. 移除对 Responses API 的依赖与相关校验，产品只支持 OpenAI Chat Completions 兼容端点。
2. 供应商切换不再因缺少 Responses 后端而拒绝；不再出现「codex 引擎要求 Responses API 后端…」这类提示。
3. 不再向用户暴露实际不可用的 Codex OAuth 入口（要么移除，要么明确标注不可用，不得留下点了没结果的路）。

**复测轨验证**（本轮执行）：

- 以现有 DeepSeek（Chat Completions）端点为唯一真实供应商，跑通委派全链路：角色自然语言 → 结构化委派 → 多轮工具 → 真实审批 → 文件落盘 → 助手结果摘要 → 角色终局回复。
- 断言出口：全链路成功后报告中不出现终态错误（覆盖 V039-S4-009）；工具执行与审批在工作台与落库可逐条追踪。
- 负向断言：仓库内不存在要求 Responses 的路径或提示；供应商切换不再被 Responses 拒绝；不可用的 OAuth 入口不再出现。
- 成本口径：不引入新付费供应商，全部走既有已授权端点。

---

## 6. B-05 A08 混合时间线：按替代方案重做

1. 构造组合：把 fix-c（85 条世界书，正文 129,992 字节）绑定到 A07 已跑通真实委派的 pair（第四面镜 / 白厄），不再使用只含占位说明的夹具卡。
2. 复用 A07 的委派提示词，重跑一个回合，读取该回合的 `messages` 与 `tool_runs`。
3. 通过标准：同一回合内同时成立 4 类以上时间线（用户消息 / 角色发言 / 助手自然语言 / 工具记录，含思考条目）与世界书激活；隐藏原文按需显式请求，隐藏文本不进普通对话。
4. 同批核对遗留的「审批结果为允许但 `assistant.natural_language` 为空」（V039-S4-008）与 V039-S4-009，若仍复现则把两条从「待确认」改为确认并升级严重度。
5. 世界书装配预算按 V039-S4-017 修复结果核对：`budget_used` 与 `budget_total` 口径一致、超预算有告警、`overflow_entries` 只列确实未注入的条目、世界书模块实际占用字符数与诊断一致。

---

## 7. B-02 固化结果（不再复测）

时间窗 2026-09-10T10:24:09Z – 10:29:11Z，会话 `10014344-d00d-40be-8fc4-9ba57bb81e55`（字节路径）与 `b3b1bc41-67c7-4b69-baa3-ffc1923e9109`（条数路径）。

| 指标 | 数值 |
| --- | --- |
| 合成尝试总数 | 40 |
| 播完（`voice.mobile_tts_end`） | 36 |
| 失败（`voice.mobile_tts_failed`） | 4 |
| └ DashScope `Throttling.RateQuota` | 2（10:28:42.681Z、10:28:51.000Z） |
| └「TTS 消息不存在」误报 | 2（10:26:22.970Z、10:28:36.588Z）→ V039-S4-018 |
| **首次限流前播完** | **34 条**（首次限流发生在第 35 次尝试） |
| 首次失败（V039-S4-018 型）前播完 | 11 条 |
| 限流形态 | 间歇性而非硬墙：限流后仍有 2 条播完（10:28:58.465Z、10:29:11.163Z） |

结论固化：这 40 次里只有 2 次是真正的供应商限流，另 2 次是产品自身把正常抢占上报成了失败（V039-S4-018）。**「语音输出受阻」不能整体归因于限流**，需先修 V039-S4-018 才能得到干净的限流基线。

**后续语音侧复测范围**（不复测条数）：

- V039-S4-016：修复后按桌面调用方与持权远程调用方两条路径各验证一次，三个共用守卫的方法（`voice.preview` / `voice.tts_play` / `voice.card_preview`）逐一确认语义自洽。
- V039-S4-018：修复后验证抢占与停止只产生「已中断」的正常收尾，不产生 `voice.mobile_tts_failed`；真实供应商失败仍必须如实上报并携带脱敏报文。
- 语音输出侧的最小确认合计 ≤3 条合成，控制在已授权额度内。

---

## 8. A05 判定（B-04）

A05 记为 **不适用（产品决策：永久不再测试）**，写入 `V0.3.9-验收记录.md`。此后任何批次不得再把 A05 列为待测、未执行或受阻项，也不得用其他用例结果替代它。移动平台矩阵（A10/A11）不受本决策影响，仍按移动端单独安排。

---

## 9. 回归触发条件

已通过用例默认不重测。仅当修复批次触及其实证路径时做定点回归：

| 用例 | 触发条件 |
| --- | --- |
| A01 | 队列、取消、并发派发或审批归属相关代码被改动 |
| A04 | 重启恢复、会话/时间线持久化相关代码被改动 |
| A12 | 审批裁决、幂等、超时相关代码被改动 |
| A13 | 角色卡导入导出、长字段读写相关代码被改动 |
| A14 | 鉴权、租约、断链恢复、休眠恢复相关代码被改动 |

---

## 10. 判定与交付要求

- 每个复测 ID 独立目录与 `result.json`，字段齐全（批次、验收 ID、候选提交、契约、构建、设备、网络、前置状态、步骤、预期/实际、时间戳、项目/聊天/任务 ID、证据路径、严重度、频率、影响范围、状态、blocker、owner、next_action）。
- 状态仅四种；受阻项必须附尝试日志、blocker、复现命令、责任人、下一次尝试条件。
- 所有新发现问题按《补充测试》§10 格式写入 `V0.3.9-问题与修复台账.md`，「修改方式」为固定句原样，测试侧不执行该句、不改代码、不向用户提问。
- 未执行的项明确列出，不混入通过；不得用本计划结果宣称 A05 或移动端通过。

---

## 11. 退出条件

1. 上表「复测对象」每一项都有独立目录与 `result.json`，状态与证据一致。
2. P1/P2 修复项复验结论明确（通过或仍失败），仍失败的在台账中保留并给出新证据。
3. B-01 沙盒路径在 `result.json` 中记录实际生效方案（首选/备选/兜底）与隔离性证据。
4. B-03 的正向与负向断言均有原始帧或日志证据。
5. B-05 的混合时间线取得含工具与思考条目的真实回合证据。
6. 受阻项有 blocker 与下一次尝试条件；无未登记问题。

---

## 12. 执行顺序

1. 开测前检查与沙盒就绪（§3、§4）。
2. P1 组复验：V039-S4-001、002、003。
3. P2 组复验：V039-S4-014、015，随后 016、018（语音侧，最小次数）、017。
4. 子项复测：B-01（A06 断网，沙盒内）、B-05（A08 混合时间线，含 V039-S4-008/009 待确认项）。
5. 正向验证：B-03（仅 Chat Completions 可用）。
6. 取证复测：V039-S4-019（依赖 §3.3 的全量日志）。

7. 收敛：更新 `V0.3.9-验收记录.md`、台账、日报；A05 记不适用。

---

## 13. 修复批次交付状态（S4-R1 开测前置，2026-09-10）

> 本节只登记**修复批次**的交付物与开测前置条件。**复测批次尚未开始**：§2「复测对象」表中每一项的状态仍为「待复测」，本节不改变该状态，也不得被引用为任何条目的复测结论。

### 13.1 已交付（离线）

19 项中 **18 项已完成代码修复与离线验证**；`V039-S4-008` 本轮无代码改动、**仍为待确认**（见本节末条）。逐项改动、测试文件与状态见《V0.3.9-问题与修复台账.md》「修复批次回写（S4-R1）」§1。要点：

- P1 组：001（协议载荷 ISO 化 + 编码失败如实回执）、002（默认真实模式 + 演示模式拒绝给出连接结论 + 前端标识）、003（`memory.create` + 模型 `memory` 协议 + 会话作用域落库 + 运行时边界 + UI 入口）。
- P2 组：014、015、016、017、018 均有对应代码改动与离线测试；017 已按冻结契约收口（空正文条目照常激活、门控首条判定还原）。
- 取证项 019：已补 INFO 级可数归因，判定与重试语义未变。
- 008 本轮无代码改动：当前源码不会产生「正文与思考同时为空」的消息，那几条空 `text` 条目可能是 reasoning-only 的段，但历史 payload 未取证，**本条仍为待确认**。

离线门禁结果（原始命令与数字）见台账「修复批次回写」§3。

### 13.2 开测前置条件（逐项标注当前满足情况）

1. **冻结修复后的候选**（**未满足**）：本轮未打包 EXE / Sidecar / APK，也未提交 git（前端 `dist` 的 `npm run build` 已跑通，但那只是静态资源构建，不是候选产物）。开测前必须先提交、重建并打包 EXE / Sidecar / 前端资源 / APK，把 SHA256 写入 `batch-manifest.json`；不得用 `897193a` 旧候选的产物冒充本轮修复结果。
2. **全量落盘未过滤日志，并以 `PAIR_HARNESS_LOG_LEVEL=INFO` 启动 Sidecar**（**未满足**，须在开测命令中落实）：Sidecar 的 `logging.basicConfig` 默认级别是 `WARNING`，本轮新增的 019 归因日志（模型输出不可用 / 是否重试 / 最终来源）在默认级别下**不会输出**；离线实测已确认不设该变量时两个相关 logger 的 `isEnabledFor(INFO)` 均为 `False`，设为 `INFO` 后为 `True` 且实际落 stderr。漏设该变量会让 019 与 018 的抢占路径取证得出「无记录」的错误结论。
3. **017 已收口（离线）**（**已满足**）：最终字段集为 `budget_constant_used` / `budget_prunable_used` / `budget_limit_reached`（不再有 reconciliation / separator）；门控按冻结契约以「累计文本是否为空」判定序列首条，空正文条目照常激活。收口后即可随候选一起冻结。
4. **静态检查已清零**（**已满足**）：`desktop` 与 `desktop/mobile` 的 `npm run typecheck`、`npm run build` 均已 exit 0（开测前再跑一次留证即可）。
5. **沙盒就绪**（**未满足**）：B-01 的进程级网络沙盒（§4.1）需在开测前完成可用性确认并留证。
6. **范围约束（非前置）**：语音合成条数与限流阈值按 B-02 永久不复测，不得因 012 的改动而重开该阈值验证；012 只复验错误生命周期与守卫语义。

### 13.3 复测时必须一并核对的证据更正

本轮在原始证据中发现三处验收叙述与证据不一致，复测记账时必须以此为准（详见台账「修复批次回写」§2）：

1. **014**：`A06/a06-injections.json` 里是三次不同的写入尝试，必须分开记：(a) `refused_base_url_set`（base_url 改 `https://127.0.0.1:1`）与 `dns_set`（改 `https://no-such-host-s4.invalid`）返回 `ok:false / provider_endpoint_mismatch`——这是**端点写入的明确拒绝**，本身就是正确行为；(b) `provider_switch_rejected`（切到 `openai_compatible`）返回 `ok:false / internal_error`——**只有这一次**是不可定位的失败。**不存在「config.set 返回成功却丢弃字段」**。复测时应按「拒绝能否定位、`test_connection` 结论是否可归属」判定，不要把 (a) 的两条端点拒绝算作缺陷。
2. **015**：错误 Key 回合的空原因在原始 `sidecar.log` 中**没有** traceback 支撑，成因以代码核对为准（异常自述为空且无回落）。复测时需保留该回合的未过滤日志以坐实。
3. **008**：当前源码不会产生「正文与思考同时为空」的消息，那几条空 `text` 条目**可能是 reasoning-only 的段**；但 `A07/db-delegation.txt` 只导出了 `text` 列，历史 payload 未取证。B-05 复测必须导出 payload 才能判定，不能只看 `text` 列，也不得据此提前关闭本条。

### 13.4 与 §2 范围的对应

| 组 | 条目 | 修复批次交付 | 复测状态 |
| --- | --- | --- | --- |
| P1 修复复验 | 001、002、003 | 已完成（离线） | 待复测（需 13.2 前置） |
| P2 修复复验 | 014、015、016、018 | 已完成（离线） | 待复测 |
| P2 修复复验 | 017 | 已完成（离线） | 待复测 |
| 取证复测 | 019 | 已完成（离线） | 待复测（依赖 §3.3 全量日志） |
| 子项复测 | B-01（A06 断网）、B-05（A08 混合时间线） | 与修复批次无关 | 待复测（B-05 需按 13.3 核对 payload） |
| 正向验证 | B-03（仅 Chat Completions 可用） | Rust/Python 侧本轮有改动 | 待复测 |
| 待确认 | 008、009 | 009 已按 ACP v1 协议精确映射修复；**008 本轮无代码改动、状态仍为待确认** | 待复测（008 必须导出 payload.reasoning 才能判定，不得计为已解决） |

**本节不解除任何验收要求**：离线通过不替代真实链路证据，也不据此宣称任何用例通过。

---

## 14. 第二轮修复批次交付（S4-R2，2026-09-10）

> 本节只登记**第二轮修复批次**的离线交付与下一次复测的前置条件。它不改变 §2「复测对象」表中各条的状态，也不改变《V0.3.9-问题与修复台账.md》「复测批次回写（S4-R1）」里已落定的任何真机结论。

### 14.1 本轮离线交付（简述）

1. **B-03（Codex 剥离）**：后端只构建 `AcpCodingEngine`（DeepSeek-Reasonix ACP），删除 Responses 校验与 Codex 可执行文件读取；通用 `openai_compatible` 端点写 Reasonix 配置（`kind=openai`）；历史 `openai_oauth` / `engine=codex` 账号启动不再退出，改为非致命 `provider_unavailable` / `engine_removed`；`config.set` 显式旧 `engine` 回 `invalid_engine`；`codex.oauth_start` / `codex.api_login` 回 `codex_login_removed`；前端移除 OAuth 入口，服务商只剩 `deepseek` 与 `openai_compatible`，自定义端点原样落库，遗留 OAuth 账号按 `provider_supported=false` 呈现且可显式改选恢复；Rust 资源注入、`tauri.conf.json` 的 `resources/codex` 与 `ci.yml` 的 Codex 占位均已删除。离线门禁：根 pytest **1095 passed / 4 skipped**、desktop **475**、mobile **336**、cargo **23**。**状态：离线已修（真机复验待做）。**
2. **V039-R1-001（打包首启清理）**：`tauri-with-first-run-reset.ps1` 改纯 ASCII / 无 BOM / LF，失败路径统一 `[Console]::Error.WriteLine(...)` 后 `exit 1`，构建非零退出时 `throw`；`build-sidecar.ps1` 剥离 Codex 先决并 ASCII 化；`tests/unit/test_v039_r1_reset_script.py` **15 项全绿**。**状态：离线已修（真机复验待做）**，本轮未真实打包。
3. **B-01（名称解析失败形态）**：其 blocker `_require_responses_backend` 已随 B-03 删除，原 blocker 在代码侧不再存在；但该形态在台账中的状态**仍为受阻**，须真机重跑后才能改。

逐项改动、验证口径与状态汇总见《V0.3.9-问题与修复台账.md》「修复批次回写（S4-R2）」。

### 14.2 下一次复测的前置条件

1. **冻结新候选并重建产物**（§13.2 第 1 条仍未完成）：本轮 B-03 与 V039-R1-001 都改动了运行时与打包脚本，上一轮的 EXE / Sidecar 产物已不代表当前代码。开测前必须重新构建并打包 EXE、Sidecar、前端资源与 APK，把 SHA256 写入 `batch-manifest.json`；不得沿用旧候选产物。
2. **B-03 负向三条须真机重验**：仓库内不存在要求 Responses 的路径与提示；供应商切换不再被 Responses 拒绝；不可用的 OAuth 入口不再出现。并补齐历史账号形态：以 `openai_oauth` / `engine=codex` 配置启动不退出、发出非致命 `provider_unavailable` / `engine_removed`，界面按 `provider_supported=false` 呈现，显式改选后可恢复。
3. **B-01 名称解析失败形态须真机重验**：下一次尝试的条件已满足，但须以真实沙盒取得区别于「连接被拒 / 传输中断 / 请求悬挂」的解析阶段失败证据后，才允许把状态从受阻改掉。
4. **打包后日志清理须真机重验**：实际执行一次打包，核对 `%APPDATA%\com.jonahwu.hsr-partner-harness\sidecar.stderr.log` 及其滚动副本被清除，并确认清理失败时打包以非零退出。
5. **`PAIR_HARNESS_LOG_LEVEL=INFO`**：Sidecar 必须以 INFO 启动、stderr 全量落盘不过滤（§3.3 与 §13.2 第 2 条），否则 V039-S4-019 的归因日志与 018 的抢占路径取证会得出「无记录」的错误结论。

### 14.3 状态口径

本轮一律记「离线已修（真机复验待做）」，不写「通过」。§2 各条状态与台账「复测批次回写（S4-R1）」中的真机结论（含 B-03 负向三条的**失败**、B-01 名称解析形态的**受阻**）在本节不做任何改动。

---

## 15. S4-R2 复测执行结果（2026-09-10，桌面端）

> 本节只记录**执行结论与状态迁移**，逐项证据、复现命令与环境改动见《V0.3.9-验收记录.md》「S4-R2 复测批次」§12–§16；新发现问题见《V0.3.9-问题与修复台账.md》`V039-R2-001`。

本计划 §14.2 的四项开测前置全部落实并取得真机证据：

| §14.2 前置 | 结果 |
| --- | --- |
| 1. 冻结新候选并重建产物 | **已落实**：`npm run build:sidecar` + `npm run tauri -- build --no-bundle`（经首启清理包装脚本）重建 EXE 与 Sidecar；APK 由 `build-android.ps1` 重建；SHA256 写入 `evidence/retest-r2-20260910/batch-manifest.json`（候选仍为工作树快照，未提交 git） |
| 2. B-03 负向三条真机重验 + 历史账号形态 | **通过**（三条负向 + 历史账号形态 + 正向全链路） |
| 3. B-01 名称解析失败形态真机重验 | **通过**（候选以保留域端点进入可用运行态，失败可归因到客户端解析阶段） |
| 4. 打包后日志清理真机重验 | **通过**（`sidecar.stderr.log` 及其滚动副本被清除；清理失败以 exit 1 传播） |
| 5. `PAIR_HARNESS_LOG_LEVEL=INFO` 全量落盘 | **已落实**（逐用例保留未过滤 `sidecar.log` 与 `sidecar-info.log`） |

状态迁移：`V039-R1-001` 失败 → **通过**；`B-03 负向三条` 失败 → **通过**；`B-01 名称解析失败形态` 受阻 → **通过**。本轮失败 0、受阻 0，新登记 `V039-R2-001`（P3，`config.test_connection` 连接层失败的可见原因为空）。

**§2「复测对象」中 S4-R1 已通过的 001/002/003/008/009/014/015/016/017/018/019 与 B-05 本轮未复跑**（依 §9 默认不重测，且本轮改动未触及其实证路径）；`V039-S4-015` 的空原因回落分支在**回合路径**上仍未构造，`V039-R2-001` 是同一信息在**探测路径**上的缺失。**移动端不在本节范围**，其结果见《V0.3.9-验收记录.md》「手机端真机批次」§17–§21。

### 15.1 真机测试终结与遗留 P3 的离线收口（2026-09-11）

按 2026-09-11 用户裁决，**全部真机测试就此终结**，本计划不再安排任何真机批次：未执行项保持未执行，不补写为通过；§2 与 §8 中「移动端 A10/A11 按移动端单独安排」的安排随之停止，A10/A11 的最终记录见《V0.3.9-验收记录.md》§31。

两条遗留 P3 以**离线修复 + 离线验证**收口（本轮无任何真机执行，未打包、未连接设备、未提交 git）：

| 条目 | 离线修复 | 离线验证 |
| --- | --- | --- |
| `V039-R2-001` | `_probe_dialogue_connection` 的失败提示复用回合路径的 `_failure_reason()`：异常自述为空时回落真实类型名，不再出现「连接失败：」空原因 | `tests/unit/test_v039_r2_application_fixes.py` 2 用例；另有真实链路核对（进程内代理指向死端口 → 「连接失败：All connection attempts failed」） |
| `V039-R2-002` | `desktop/scripts/build-android.ps1` 在调用 gradle 前按 `tauri.conf.json` 版本派生 versionCode 并写出 `gen/android/app/tauri.properties`（0.3.9 → 3009），版本非法即报错退出 | `tests/unit/test_v039_r2_android_version_code.py` 10 用例（含 tempfile 沙箱整脚本 dry-run）；旧脚本下 8 失败、修复后 10 通过 |

离线门禁：根 `./.venv/Scripts/python.exe -m pytest -q` **1107 passed / 4 skipped / exit 0**。按同一裁决「现有问题修复并经离线测试通过后，即视为测试通过」，两条状态由待修迁移为**通过**；逐项改动与迁移表见《V0.3.9-问题与修复台账.md》「修复批次回写（S4-R4，遗留 P3 离线修复，2026-09-11）」，裁决与最终记录见《V0.3.9-验收记录.md》§28–§32。
