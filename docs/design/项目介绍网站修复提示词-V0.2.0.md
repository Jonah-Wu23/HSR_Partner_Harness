# 项目介绍网站修复提示词

请在 `E:\AI\HSR Partner Harness` 仓库中，只针对项目介绍网站 `docs/index.html` 做以下两类修复：

## 1. 统一中文表达

按照 `C:\Users\JonahWu\.agents\skills\zh-global-guardrails\SKILL.md` 的规则检查网站上的中文文案，并在不改变信息含义的前提下修改：

- 删除 AI 常见的先否后肯句式、空洞排比、疗法腔、过度安抚和自证清白话术。
- 删除没有信息量的连接词、宣传套话和夸张承诺，改成正常、直接的中文。
- 保留项目已有的角色名、产品名、技术名词、版本号和真实功能，不要为了追求短句把自然中文拆碎。
- 文案需要像一个真实产品页面，语气清楚，有产品感，但不要写成广告口号堆叠。
- 不新增来源不明的用户数量、性能数字、商业客户、融资信息或兼容性承诺。

## 2. 匹配 v0.2.0

以以下仓库文件为事实来源：

- `README.md`
- `docs/design/v0.2.0-release-notes.md`
- `docs/design/最终交付文档-V0.2.0.md`
- `docs/architecture.md`

把网站中仍然停留在旧版本、旧功能或旧状态的内容改成 v0.2.0 的实际情况，至少检查：

- 版本号、发布状态和下载入口。
- 本地账号与设置中心。
- 聊天模式和协作模式的区别。
- Codex 编程助手固定使用 `gpt-5.6-sol`，推理档位为轻度 `low`、中 `medium`、高 `high`、极高 `xhigh`、最高 `max`。
- 任务队列的编辑、撤回、调序和自动派发。
- DeepSeek 思考与正文的流式显示，以及原始 JSON 不直接进入消息气泡。
- Sidecar 连接状态、自动重连、语音和 VAD 随包状态。
- Windows x64 NSIS 安装包和未签名提示。

## 修改边界

- 只改 `docs/index.html` 的文案和与文案直接相关的链接文字。
- 不重做页面布局、配色、动画、插画、字体、组件结构或交互流程。
- 不修改 `desktop/`、`src/`、`config/`、`tests/` 和 README。
- 不新增页面，不新增功能，不引入新的外部服务。
- 保留现有视觉设计，只修正文案和 v0.2.0 信息。

## 完成标准

1. 网站所有主要中文文案符合 `zh-global-guardrails` 的表达要求。
2. 网站不再出现与 v0.2.0 冲突的版本号、功能描述或发布状态。
3. 页面原有布局、颜色、动效和响应式行为保持不变。
4. 修改后运行网站已有的最小构建或静态检查，并报告实际结果。
5. 最终只列出修改过的 `docs/index.html` 内容和检查结果，不要顺手修改其他文件。
