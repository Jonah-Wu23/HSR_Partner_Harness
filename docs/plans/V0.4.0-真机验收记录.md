# V0.4.0 真机验收记录（T16 移动与公网隧道批次）

> 依据《V0.4.0 关键产品级决策.md》§5 与《V0.4.0-实施计划.md》§6 编制。
> 状态仅四种：**通过**、**失败**、**受阻**、**不适用**。
> 执行纪律：**批次只测不修**。批次内发现问题只在台账登记，不现场改代码。
> 准则约束：**Let It Fail, Let It Go**。真实失败如实暴露，不得用兜底伪造成功或掩盖未执行任务。

---

## 1. 批次信息

| 项目 | 记录 / 预填内容 |
| --- | --- |
| 批次编号 | `batch-v0.4.0-real-device`（已执行完毕） |
| 实施时间 | 2026-09-13 01:36 – 03:05（北京时间） |
| 候选提交 / 分支 | `feat/v0.4.0` @ `89eeb2c`（T1–T15 实现与审核整改）+ `f17b06a`（计划与台账文档）+ `11463cb`（hash 回填）+ `9216ec5`（验收收尾，未入包、不影响运行时行为；打包产物对应 `89eeb2c` 的代码状态） |
| 协议契约 | V0.4.0 契约增补（`docs/plans/V0.4.0-实施计划.md` §4） |
| PC 端构建物 | `desktop/src-tauri/target/release/hsr-partner-harness.exe`（SHA256 `28d48dbdff32722b95b3850517273a719906acb85c143934c9ff8614a71f344b`，`--lan` 启动） |
| Sidecar 构建物 | `resources/sidecar/pair-harness-sidecar/pair-harness-sidecar.exe`（SHA256 `376de5e96dc4afd790ea237fc1934d7ebaa45b4a1c4c235238452b5d69275d21`） |
| 测试 PC 环境 | Windows 11 x64；校园网 WLAN 与小米 15 热点双环境 |
| 移动测试设备 | 小米 15（24129PN74C），Android arm64 Debug APK（versionCode 4000，adb 无线调试安装） |
| 隧道环境 | Cloudflare Quick Tunnel（官方 `cloudflared` 2026.9.0，本次地址 `https://controlling-statute-origin-containing.trycloudflare.com`） |
| 真实服务 | 对话 DeepSeek；委派 Reasonix（ACP）；语音 DashScope（北京专属域） |
| 证据存放根目录 | `evidence/batch-v0.4.0-real-device/`（`live-capture/capture.log` 为全程毫秒级抓取：SQLite 配对状态 diff + Sidecar stderr 增量） |
| 测试方法备注 | 手机与 PC 间无可直连路径（校园网准入拦客户端互访、热点网关方向性丢包，均经对照实验定性），WS 链路经 `adb reverse tcp:8765 tcp:8765` 替代传输，协议路径与真实配对完全一致；M03/M12 经公网隧道为真实蜂窝路径 |

---

## 2. 真机验收 12 项检查矩阵

严格依据《V0.4.0 关键产品级决策.md》§5 与《V0.4.0-实施计划.md》§6 设立以下 12 项检查矩阵：

| 序号 | 验收项 | 契约依据 | 验证方法与步骤 | 预期结果 | 实测状态 | 证据路径 / 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| **M01** | 默认回环监听隔离 | 决策稿 D2 / 计划 §4.1 | 默认启动应用（不带 `--lan`）。用同一局域网下的另一台设备（手机或另一台电脑）访问 PC 局域网 IP:8765。 | 连接被拒绝（Connection Refused），端口不对局域网开放；`serve.started` 上报 `mode: "loopback"`。 | **通过** | `M01/phone-probe-8765.txt`；`netstat` 确认仅 `127.0.0.1:8765` LISTENING；PC 本机经物理网卡 IP 访问 8765 得「积极拒绝」（本机流量不经防火墙，为纯 TCP 栈证据）。手机侧探测不可达系校园网准入拦客户端互访（环境层，不遮蔽应用行为） |
| **M02** | 显式开启局域网直连 | 决策稿 D2 / 计划 §4.1 | 启动参数附加 `--lan`。局域网其他设备访问 PC 局域网 IP:8765，并观察桌面端远程设置面板。 | 局域网连接成功；桌面端常驻显示「局域网已暴露」黄色警告；关闭 `--lan` 或关闭开关后提示消失且局域网中断。 | **通过**（局域网直连受阻备注） | `--lan` 后 `0.0.0.0:8765` LISTENING；面板「接入地址：10.60.35.240:8765」（`serve.started` 上报真实局域网地址 ✓）；「当前处于局域网共享模式」常驻提示 ✓。手机→PC 直连被网络环境拦（热点网关方向性丢包），WS 链路经 `adb reverse` 替代传输 |
| **M03** | 公网隧道与安全上下文 | 决策稿 D1 / 计划 §4.2 | 桌面端开启公网接入；手机**断开 Wi-Fi、使用蜂窝移动网络**，浏览器打开随机生成的 `https://<random>.trycloudflare.com` 页面。 | HTTPS 页面正常加载；浏览器麦克风权限提示正常弹出并可授权，Web Audio 安全上下文成立。 | **通过** | 用户人工确认四要素：页面正常加载（配对页/聊天列表）、地址栏安全锁标志、无证书警告、无「不安全」提示。隧道地址 `https://controlling-statute-origin-containing.trycloudflare.com`（2026.9.0）。观察项：`tunnel_started` 审计驻留内存未落库（见 R1-003） |
| **M04** | 动态二维码与重启作废 | 决策稿 D1, D7 / 计划 §4.1 | 桌面端生成配对二维码，手机扫码完成配对；随后重启 PC 端应用，观察旧二维码与手机状态。 | 扫码成功进入对话；PC 重启后由于 Quick Tunnel 重新分配主机名，旧二维码与旧隧道地址失效，界面如实提示重配。 | **通过** | `M04/issue-code-response.txt`、`M04/phone-01-initial.png`、`M04/phone-02-after-scan.png`；扫码自动填码 + 手动补地址路径配对成功进聊天页；二维码中的 ws 地址未自动带入（已在 R1-006 登记，用户手动输入替代）；「作废旧码并生成新码」语义按钮真机呈现 |
| **M05** | 配对码限流与封锁退避 | 决策稿 D3 / 计划 §4.4 | 手机端连续 5 次输入错误配对码；第 6 次输入正确配对码；等待封锁期结束后再次输入正确码。 | 连续 5 次失败后触发封锁，提示重试倒计时（60s）；封锁期内正确码仍被拒绝（`rate_limited`）；解锁后成功配对；审计写 `pairing_rate_limited`。 | **通过** | `M05/m05-m06-capture-excerpt.log`：来源 `ab084461…` fail_count 2→3→4→5，第 5 次失败审计 `pairing_rate_limited retry_after_s=60`、`blocked_until` 置位；封锁期内正确码仍拒（用户人工确认）；限流来源为每条 WS 连接独立 UUID（D3 明示「以连接为单位」的设计权衡，见 R1-005） |
| **M06** | 单码有效性 | 决策稿 D4 / 计划 §4.5 | 桌面端点击「作废旧码并生成新码」，连续生成 2 次。手机端使用第一次生成的配对码尝试配对。 | 首次生成的旧码立即失效，返回 `invalid_code`；仅最后一枚生成的配对码可兑换 token；审计记 `pairing_code_issued`。 | **通过** | `M05/m05-m06-capture-excerpt.log`：`pairing_code_issued` ×2 期间 `active_codes` 恒为 1；旧码被拒后新码配对成功（`connect device=我的手机`）。用户人工确认三态提示区分正确 |
| **M07** | 控制面来源分级收窄 | 决策稿 D6 / 计划 §4.2 | 已配对手机端经由 WebSocket 尝试调用控制面方法（如 `remote.issue_code`、`remote.revoke`、`remote.tunnel_start`）。 | 全部被鉴权门拦截，返回错误 `forbidden_scope`；审计记录 `scope_denied`；桌面端 stdin / 回环调用正常。 | **通过** | `M07/m07-six-methods.txt`：有效 token（q5YfkgSY…）从 WS 侧调 `remote.issue_code/list_devices/revoke/tunnel_start/tunnel_stop/tunnel_status` 六方法全部 `forbidden_scope`（6/6）；`scope_denied` 审计 ×6 落库（`method=… origin=remote`）；桌面端 UI（desktop origin）的生成码/撤销/列表全程正常。观察项：`scope_denied` 审计驻留内存、随下一次状态变更才持久化（R1-003） |
| **M08** | 设备令牌生命周期 | 决策稿 D5 / 计划 §4.4, §4.5 | 手工将 SQLite 中某已配对设备令牌的 `expires_at` 修改为过去时间戳，随后该手机发起请求。 | 请求被拒并返回 `expired_token`，手机端明确提示「授权已过期，请重新配对」并不再重连；桌面端依然能列出并撤销该设备。 | **通过** | `M08/phone-after-restart.png`：改库重启后手机自动重连被拒，显示「登录令牌已过期（最长30天或7天未使用），请重新配对」并稳定停留配对页（无静默重连，重启后库内无新 `connect` 审计）；桌面端列表仍列出「我的手机」且撤销可用（用户人工确认）。`expired_token` 审计驻留内存（R1-003 同项） |
| **M09** | 设备撤销即时生效 | 决策稿 D5 / 计划 §4.4 | 手机处于连接状态，桌面端在已配对设备列表中点击「撤销」。 | 手机端 WebSocket 连接被立即切断（关闭码 4401），之后重连鉴权返回 `revoked_token`，禁止重入。 | **通过** | `M09/phone-after-revoke.png`：确认撤销后手机立即断连，显示「本设备已被桌面端撤销授权，请重新配对」+「数据同步失败：配对鉴权已失效或设备已被撤销」；审计 `command \| revoke device=我的手机`；重连 `app.bootstrap: revoked_token`、`remote.release_control: revoked_token` 审计落库 |
| **M10** | 隧道进程崩溃真实暴露 | 决策稿 D1 / 计划 §4.3 | 隧道运行在 `ready` 状态时，通过任务管理器或 `kill` 外部强行终止 `cloudflared.exe` 进程。 | 桌面端与事件流立即派发 `tunnel.failed`，状态切为 `failed` 并展示退出码与失败原因，严禁停留在「已连接」或伪装就绪。 | **失败** → 修复后模拟复测通过（2026-09-13，见 §6；非真机） | 设置页内强杀 `cloudflared` 后桌面 UI **停留「已连接」未即时刷新**，关闭设置重开才显示「公网隧道启动失败：隧道进程已被外部终止 (退出码 1)」——Sidecar 侧状态机与失败信息正确（退出码、真实原因如实上报），但 UI 未即时反映 `tunnel.failed` 事件，违反「严禁停留在已连接」的字面预期。登记 R1-002（P1）。手机端断开如实提示 ✓。**修复后模拟复测**：真实 Sidecar + 真实 cloudflared Quick Tunnel 就绪后强杀，stdout 真实发出 `tunnel.failed`（真实退出码、序号连续）；桌面 store/面板消费由 vitest 回归锁定（证据见 §6） |
| **M11** | PWA 静态产物干净校验 | 决策稿 D7 / 计划 §4.6 | 在构建期与打包产物中检查 PWA 静态发布目录（执行 `validate-pwa-dist.ps1`）。 | 目录仅含编译后的 HTML/JS/CSS/Web 字体等静态资源，零用户数据、零私钥、零项目文件。 | **通过**（结论复核） | 构建期门禁在审核整改后的重新打包中真实执行（`desktop/mobile/dist` 与 `resources/mobile-dist` 两处校验通过，36 项单元测试全绿）；本次批次无新增构建，结论复核登记 |
| **M12** | 公网隧道语音全链路 | 计划 §6 第 12 条 | 手机蜂窝网络下按住说话，录制语音指令；角色回复并播放 TTS；助手执行工具命令。 | 手机端 ASR 识别字句准确上行；角色自然语言回复通过 DashScope TTS 流式下行播放；助手与工具执行事件保持静音无 TTS。 | **通过** | 用户人工确认三要素：识别准、回复 TTS 下行播放、工具事件静音。蜂窝 + 公网隧道真实链路 |

---

## 3. 问题登记与修复台账

批次内发现缺陷按「只测不修」纪律登记如下（2026-09-13 批次）：

| 问题编号 | 严重级 (P0-P3) | 触发用例 | 现象描述与影响 | 错误日志 / 契约偏差 | 根因初判 | 修复状态 |
| --- | --- | --- | --- | --- | --- | --- |
| V040-R1-001 | P2 | M04/M09 | 已配对设备列表在配对成功后不自动刷新：配对成功后面板仍显示「已配对设备（0）」，切换设置栏目再切回后恢复为 1。用户在配对后短时间内看不到新设备 | 列表数据来自 `remote.list_devices` 的一次性拉取，配对成功事件未触发重新拉取 | `RemotePairingPanel` 未订阅配对成功事件刷新列表 | 已修复（提交 80d8291：Sidecar 广播 `remote.paired`，面板据 `devicesRevision` 重拉列表；vitest 回归锁定，见 §6） |
| V040-R1-002 | P1 | M10 | 隧道进程被外部强杀后，桌面 UI 停留「已连接」未即时刷新；关闭设置重开才显示「公网隧道启动失败：隧道进程已被外部终止 (退出码 1)」。期间用户看到的是就绪假态 | Sidecar 侧状态机正确切 `failed`（重开后展示的退出码与真实原因准确），`tunnel.failed` 事件派发与 UI 即时消费之间的链路失效；违反矩阵「严禁停留在已连接」 | 桌面端未即时消费 `tunnel.failed` 事件刷新面板状态（根因修正：remote-only 手机语音事件消费全局序号但不写桌面 stdout，桌面流序号缺口触发「缓冲→快照→按序号丢弃」吞掉隧道事件） | 已修复（提交 80d8291：tunnel.* 走控制通道即时应用；vitest + 进程级模拟复测通过，见 §6） |
| V040-R1-003 | P3 | M07/M08/M03 | 部分审计条目（`scope_denied`、`auth_failed: expired_token`、`tunnel_started`）驻留 Sidecar 内存，随下一次配对状态持久化才落库；Sidecar 在此之前退出则该批审计丢失 | M07 六条 `scope_denied` 在触发一次新码生成后才落库；M08 的 `expired_token` 直至批次结束未落库；`tunnel_started` 全程未落库 | `authorize()`/`tunnel` 的审计只写内存，无即时持久化钩子 | 已修复（提交 80d8291：审计写入经注入钩子当刻落库；pytest 回归锁定，见 §6。备注：写放大为该方案的固有代价，审计列表无上限，后续版本可评估上限/批量落盘） |
| V040-R1-004 | P3 | M03 | 隧道管理过程日志（INFO 级：下载、哈希校验、子进程启动、主机名解析）未出现在 `sidecar.stderr.log`，故障排查缺少过程线索 | `TunnelManager` 使用 `logger.info`，Python logging 默认级别 WARNING | 日志级别配置未为 tunnel 模块开启 INFO | 已修复（提交 80d8291：tunnel 模块固定 INFO，并补子进程启动/主机名就绪过程日志；pytest 回归锁定） |
| V040-R1-005 | 观察（非缺陷） | M05 | 配对限流来源为每条 WS 连接独立的 `uuid4` key：连接重建即重置失败计数。决策稿 D3 明示「来源以连接为单位即可」（避免隧道场景按 IP 退化为全局限流造成拒绝服务），实际防线由单码有效 + 码 TTL 300s + 生成新码作废承担 | 与决策稿 D3 设计一致，不构成契约偏差；作为安全模型权衡记录 | 设计权衡（决策稿冻结） | 无需修复；后续版本可在决策稿层面评估「按 token/设备维度限流」 |
| V040-R1-006 | P3 | M04 | 手机端「扫码填入配对码」仅自动填入配对码，二维码中的 `?ws=` 服务地址未自动带入「桌面端服务地址」框，需手动输入 | 扫码解析后地址字段未填入或二维码 payload 中地址解析失败（现场以手动输入 `ws://127.0.0.1:8765/ws` 完成） | 待查（手机端扫码解析逻辑或二维码 payload 格式） | 本批不修：修复触及移动端代码，须随 APK 重建一并验证发布，本批未重建 APK（桌面修复批次）；二维码 payload 中 `?ws=` 地址格式本身可用（手动输入即完成配对），登记延续至后续移动端批次 |

---

## 4. 停止边界与验收签收

### 4.1 真机前停止边界达成核对
- [x] T1 监听面收窄与 `--lan` 参数实现并通过离线测试
- [x] T2 配对码限流（连续 5 次失败封锁、退避翻倍、单码有效）已实现并通过测试
- [x] T3 令牌生命周期（30天绝对/7天空闲）与快照 v2 升级实现并通过测试
- [x] T4 控制面来源分级（仅回环/桌面端）与审计日志补齐并通过测试
- [x] T5 隧道管理模块（`tunnel.py` 下载、哈希校验、状态机、退出清理）已实现并通过测试
- [x] T6 Rust 侧 `--lan` 与隧道透传已实现
- [x] T7 桌面端 `RemotePairingPanel` 改造（动态 URL、隧道五态、局域网提示）已完成
- [x] T8 登录页标语更换（「一条会话，两条工作轨。」）已完成并通过测试
- [x] T9 移动端适配（`https`/`wss` 支持、三态错误友好提示）已完成
- [x] T10 PWA 静态目录构建期安全校验（36 项单元测试全绿）已完成
- [x] T11 文档同批更新（用户语音说明、计划路线图更新）已完成
- [x] T13 官方下载 URL、固定版本 `2026.9.0` 与 SHA256 哈希校验完备性核查完成
- [x] T15 数据库与配对状态平滑迁移、兼容性及零数据初启测试全部通过
- [x] T12 全量自动化门禁通过并留证：独立审核复跑 Python 1192 passed / 4 skipped、desktop 47 套件 497 passed、mobile 30 套件 361 passed、两次 typecheck 0 错误、cargo fmt 0 差异 + cargo test 28 passed；审核整改（3 项代码修复、3 项测试新增、夹具编码加固）后 Python 全量复跑 **1195 passed / 4 skipped**、迁移用例 7 passed——前端与 Rust 代码此后未再改动，其门禁结论延续有效。
- [x] T14 版本与打包完成并留证：五处版本号 0.4.0；审核整改后重跑 `npm run build:sidecar`（exit 0，PWA 校验两处通过）并经首次运行清理包装脚本重建 EXE 与 NSIS；Android arm64 Debug APK 构建成功并经包内内容级验证（见 §5.3）。
- [x] **当前状态：已抵达真机前停止边界。未启动真实设备测试，等待真机验收批次授权。**

### 4.2 签收结论

真机验收批次于 2026-09-13 执行完毕（批次只测不修纪律全程遵守）。

- 矩阵通过数：**11 / 12**
- 矩阵失败数：**1 / 12**（M10：Sidecar 侧状态机与失败信息正确，桌面 UI 未即时刷新，R1-002）
- 矩阵受阻数：0（M02 的局域网直连以 `adb reverse` 替代通道完成协议路径验证，网络环境限制在证据备注中如实记录，不计受阻）
- 安全门槛核对：未配对/未鉴权连接无法触发命令执行、限流/单码/令牌到期/控制面限回环四项行为与 §4 契约一致、真实失败未被转换为成功——**全部在真机路径上成立**；R1-002 属 UI 即时性缺陷，不改变 Sidecar 侧真实状态与审计语义，但修复前隧道强杀后用户可能在设置页看到最长一次面板渲染周期的就绪假态。
- 签收状态：**已签收（含 1 项失败，P1 待修复后按影响面复测 M10）**

### 4.3 批次执行记录（组网与测试方法）

- 组网：初始尝试校园网 WLAN 直连与小米热点直连均不可达（校园网准入按端口白名单拦客户端间流量：5357 通、ICMP/445/8765/8090 全拦；热点网关对客户端发起方向存在 ROM 级限制，DefaultInbound=Allow 后仍丢包），经对照实验定性为网络环境限制，非应用缺陷。验收 WS 链路改用 `adb reverse tcp:8765 tcp:8765`（协议路径与真实配对一致）；M03/M12 走公网隧道真实蜂窝路径。
- 测试期防火墙变更：为完成 M01 refused 语义区分，将 WLAN 网络分类改为专用并放行 8765/TCP 入站（规则名 `HSR-Partner-Harness 8765 (T16 acceptance, temporary)`）——该规则在验收结束后应删除；Private 分类与关闭的「默认入站 Allow」试验配置应一并还原。
- 证据采集：全程毫秒级抓取（SQLite `remote.pairing_state` 轮询 diff + `sidecar.stderr.log` 增量 + 手机 logcat），落档于 `evidence/batch-v0.4.0-real-device/live-capture/capture.log`。

---

## 5. 独立审核整改与重新打包记录（2026-09-11）

T1–T15 交付后，独立审核子代理对 15 项声明逐条取证并复跑全部测试门禁，裁决 **FAIL**：四项阻断 + 一批建议缺陷。同日完成整改，本节为整改留证。

### 5.1 阻断项整改

**1. 重新打包（原 CRITICAL：安装包内 Sidecar 落后源码，缺 T15 最后一次修复）。**

- 重跑 `npm run build:sidecar`（exit 0，修改后的 PWA 校验在 `desktop/mobile/dist` 与 `resources/mobile-dist` 两处通过）与 `npm run tauri -- build --bundles nsis`（exit 0，经 `tauri-with-first-run-reset.ps1` 首次运行清理）。
- 包内证据：sidecar PYZ 内 `application_service` / `tunnel` / `pairing` / `ws_server` 四模块与 EXE 入口 `__main__` 脚本，经 marshal code object（co_code / co_consts / co_names / co_varnames）与工作区源码字节码级比对全部 **MATCH**；NSIS 包内提取的 `pair-harness-sidecar.exe` SHA256 与 resources 下新构建一致；迁移用例复跑 7 passed。
- 构建物哈希：

| 构建物 | SHA256 |
| --- | --- |
| `desktop/src-tauri/target/release/hsr-partner-harness.exe` | `28d48dbdff32722b95b3850517273a719906acb85c143934c9ff8614a71f344b` |
| `desktop/src-tauri/target/release/bundle/nsis/HSR Partner Harness_0.4.0_x64-setup.exe` | `c0e36c2b4c617c55533e6d21806d2738c5c930690c2a7ba8db65df7664fc05f2` |
| `desktop/src-tauri/resources/sidecar/pair-harness-sidecar/pair-harness-sidecar.exe` | `376de5e96dc4afd790ea237fc1934d7ebaa45b4a1c4c235238452b5d69275d21` |

**2026-09-13 重建（R1-002/R1-001/R1-003/R1-004 修复批次，提交 80d8291）——上表哈希全部作废，以本表为准：**

经 `npm run build:sidecar`（PWA 校验两处通过）与 `tauri-with-first-run-reset.ps1 build --bundles nsis`（exit 0，首次运行数据清理已执行：PairHarness 数据目录、WebView Local/Session Storage、sidecar.stderr.log 全部清空）重建。本批不触及移动端，Android APK 沿用 2026-09-12 构建的 0.4.0 arm64 Debug 包（见 §5.3）。

| 构建物 | SHA256 |
| --- | --- |
| `desktop/src-tauri/target/release/hsr-partner-harness.exe` | `b5ad85c9b0a4f0d18ba4e56254c2e7fc5b90b621bf668fabe935eb16e818d36a` |
| `desktop/src-tauri/target/release/bundle/nsis/HSR Partner Harness_0.4.0_x64-setup.exe` | `329288a7c921d23c1d37e4818ec2e872109d49c73e0d99b7705cc6193716bd78` |
| `desktop/src-tauri/resources/sidecar/pair-harness-sidecar/pair-harness-sidecar.exe` | `5c30f1124c08353a670f816e18ca83ef3ba9bb23b21df5188f9cbec486661482`（tauri 打包目录副本同哈希） |

**2. 候选提交建立**：按计划纪律逐路径 `git add`（未用 `git add -A`），提交 hash 回填至 §1 批次信息表。

**3. 台账补记**：§4.1 补 T12 / T14 两条核对项，T12 实测数字以独立审核复跑与本轮整改后复跑为准（此前的 1185 为 T15 修复前旧快照数字，作废）。

**4. darwin 哈希处置**：经 GitHub Release API（2026.9.0 tag）核对，官方 `cloudflared-darwin-amd64.tgz` / `cloudflared-darwin-arm64.tgz` SHA256 为 `8f2ecf41776d942bcc8070a56e7bafa4c5de70a1d1781110e2eb3774cca512a8` / `c0eccb3758420d1f4e46cbf2b8ecde01d9802a154232a817f25133340009fcc7`，原表两项确为错误值；且官方 darwin 资产是 `.tgz` 压缩包，原实现无解压流程，macOS 上本就无法工作。处置：删除 darwin 条目，`detect_platform_asset` 在 macOS 显式抛 `unsupported_platform`（产品未承诺 macOS 分发，不做未经真实验证的解压路径）；其余四平台哈希经同一 API 逐字核对无误。

### 5.2 建议缺陷同步修复

- **配对封锁失败路径当刻落盘**：`application_service.py` `_remote_pair` 失败分支补 `_persist_pairing_state()`，失败计数与封锁退避在拒绝请求当刻持久化（§4.4「重启不重置」），新增端到端测试（封锁状态跨重启存活、封锁期内正确码仍被拒）。
- **`tests/unit/test_pairing.py` 审计断言写虚**：原断言因 and/or 优先级退化为只数条数；改为逐方法校验 `scope_denied` 审计存在且 detail 含方法名与来源。
- **`validate-pwa-dist.ps1`**：forbidden 扩展检查提前（原为死分支——白名单先行导致 Forbidden 报错永不可达）；内容扫描从 json/txt/webmanifest 扩至 js/mjs/cjs/css/html/htm/map（打包产物主体）；新增 2 项测试，真实 dist 通过。
- **D7 标签引用链闭合**：决策稿 §4 第 20 条补「（D7）」标注；实施计划 §4.6 点明 D7「隧道 URL 不是秘密」与 PWA 静态目录约束的推导关系。
- **测试夹具编码健壮性**：三个 PowerShell 驱动测试文件在 `PYTHONUTF8=1` 环境按 UTF-8 解码 GBK 输出崩溃（12 项失败），改为按系统 ANSI（mbcs）解码并 errors=replace，复跑全绿。

### 5.3 Android APK（T14）

环境此前判定受阻，实际在 `E:\AI\CHD-class-table` 下齐备：JDK 17（Temurin 17.0.20.1）、Android SDK（platforms android-34/35/36）、NDK 28.2.13676358。2026-09-12 以注入环境变量方式执行 `scripts/build-android.ps1`，构建 arm64 Debug APK：

- 版本派生正确：`gen/android/app/tauri.properties` 写出 `versionName=0.4.0` / `versionCode=4000`（0.4.0 → 4×10⁶，符合派生式）。
- 产物：`desktop/src-tauri/gen/android/app/build/outputs/apk/arm64/debug/app-arm64-debug.apk`（129,439,676 字节，SHA256 `af27908ed5bbd1fdd76808d5145c89308de2791483d914c707462ec5ba8f1258`）。
- 包内内容级验证：`assets/assets/index-C-JeaTkU.css`（41,189 B）与 `assets/assets/index-C_8QOZa5.js`（343,580 B）与 `desktop/mobile/dist/assets/` 当前构建哈希名一致——新前端代码已打入 APK，非旧资源残留；`lib/arm64-v8a/libhsr_partner_harness_lib.so` 为本次 Rust 构建新产物。
- 备注：gradle 产出 APK 后脚本输出管道未回收（后台进程挂起被终止），脚本的 "Built APK" 收尾输出未能留档，以上结论均来自产物本身的内容级核验，不依赖脚本回执。

---

## 6. R1-002 修复后 M10 模拟复测（2026-09-13）

> 本次复测为**模拟复测**（vitest 离线回归 + 进程级脚本模拟），未使用真机与桌面 GUI，符合批次测试边界；不得据此改写原批次「真机失败」的历史结论，M10 原始状态保留为失败并附复测结论。

### 6.1 根因修正（与原台账初判的差异）

原登记初判为「设置面板不订阅实时事件」。修复批次通读事件链路后实证修正：桌面 store 对 `tunnel.failed` 的响应式处理本身完好（真实 store 上派发事件即时落到 `remotePairing.tunnel`，vitest 回归锁定），presenter → AppShell → SettingsCenter → 面板为纯 props 透传（`presenters.ts:511`、`AppShell.tsx:368`、`SettingsCenter.tsx:821`），链路经源码审核确认；面板渲染层的端到端自动化超出本批测试边界，留待真机批次复核。真实根因是 remote-only 手机语音事件（`_publish_remote_only`，`application_service.py:4357`）消费全局事件序号但不写桌面 stdout，桌面事件流出现序号缺口；缺口触发「缓冲 → 快照水合 → 按序号丢弃」路径（快照 sequence 取自同一计数器，缓冲事件序号必然不大于它），`tunnel.failed` 被静默吞掉，面板停留「已连接」。

### 6.2 修复内容（提交 80d8291）

- 桌面 store：`tunnel.started` / `tunnel.stopped` / `tunnel.failed` / `remote.paired` 按控制通道即时应用（不缓冲、不推进 `lastSequence`、保留 stream_id 代次隔离）；契约增补记入实施计划 §4.7。
- R1-001 同批：`remote.pair` 成功广播 `remote.paired { device_name }`，store 推进 `devicesRevision`，设置面板据变化重拉设备列表。
- R1-003 同批：审计写入经注入钩子当刻落库。
- R1-004 同批：tunnel 模块固定 INFO 级别并补过程日志。

### 6.3 复测方式与结果

**vitest 离线回归（桌面 store/面板）**：

- 「序号缺口下 tunnel.failed 即时应用，不进入缓冲丢弃路径」：断言 `remotePairing.tunnel.state` 立即变为 `failed` 且 `needsBootstrap=false`、事件缓冲为空；`desktop/src/services/actions.remote.test.ts`。
- 「R1-001：devicesRevision 变化时重拉设备列表」与 store 侧 `remote.paired` 推进 revision：`desktop/src/ui/settings/remote/__tests__/RemotePairingPanel.test.tsx`、同上文件。
- 共新增 6 项 vitest；desktop 47 套件 503 passed，typecheck 0 错误。

**进程级模拟（真实 Sidecar + 真实 cloudflared）**：

- 脚本 `evidence/batch-v0.4.0-retest-m10/retest_tunnel_failed.py`（转写 `transcript.log`）：以真实 Sidecar 子进程启动（`--serve 18799` 回环，隔离数据目录，复用真机批次已校验的 cloudflared 2026.9.0 二进制），经 stdin 以 desktop 来源发送 `remote.tunnel_start`；Quick Tunnel 真实就绪（`https://acceptance-valve-katie-october.trycloudflare.com`，`tunnel.started` seq=3）后 `taskkill /F` 强杀 cloudflared；stdout 真实发出 `tunnel.failed`（seq=4，`隧道进程已被外部终止 (退出码 1)`），序号连续、失败原因携带真实退出码。
- 数据目录全新隔离，cloudflared 由 Sidecar 现场从官方源下载（GitHub Release 2026.9.0，经 SHA256 校验），stderr 留下 R1-004 的完整过程日志直证：`开始下载 cloudflared: https://github.com/cloudflare/cloudflared/releases/download/2026.9.0/cloudflared-windows-amd64.exe`、`cloudflared 子进程已启动 pid=… local_port=18799`、`Quick Tunnel 主机名解析就绪 hostname=…`（`transcript.log` 末段）。
- 桌面端消费由上述 vitest 回归锁定（本批次测试边界禁止 GUI 自动化，桌面 UI 环节以 store 级测试 + 源码链路审核为证）。

**审计落库回归（pytest）**：`scope_denied`、`expired_token`、`tunnel_started` 写入当刻落库，共新增 7 项。

**阶段二全量测试门禁实测（2026-09-13，全部持平或优于 2026-09-11 基线）**：

| 门禁 | 实测 | 基线 |
| --- | --- | --- |
| Python `pytest -q` | 1202 passed / 4 skipped | 1195 passed / 4 skipped |
| desktop `npm test -- --run` | 47 套件 503 passed | 47 套件 497 passed |
| desktop `npm run typecheck` | 0 错误 | 0 错误 |
| mobile `npm test -- --run` | 30 套件 361 passed | 30 套件 361 passed |
| `cargo fmt --check` | 0 差异 | 0 差异 |
| `cargo test` | 28 passed | 28 passed |
