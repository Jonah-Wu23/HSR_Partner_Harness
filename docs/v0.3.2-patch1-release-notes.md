# HSR Partner Harness v0.3.2-patch1

v0.3.2-patch1 是 v0.3.2 的构建修复版，功能与 v0.3.2 一致。

## 本版本修复

- 修复 CI 失败：`desktop/src-tauri/src/main.rs` 三处代码未按 rustfmt 格式提交，`cargo fmt --check` 失败导致 Rust checks job 挂掉。
- 修复 CI 失败：`tests/unit/test_concurrent_entries.py` 中"跨聊天并发"测试使用共享 `asyncio.Event` 做第二次等待，事件置位后 `wait()` 立即返回，断言与 chat-b 任务启动竞态；改为按会话区分的 `started_for(conversation_id)` 事件，真正等待 B 聊天推进到自己的工具事件。
- 修复 CI 失败：`tests/unit/test_voice_runtime.py` 的 PTT 测试在 finally 中依赖 `states[-1] == "listening"`，但播放循环可能先于 `push_to_talk_start` 因 playing 检查 break 而提前恢复，恢复后的 listening 被后续 `speech_started` 覆盖导致条件永不成立；等待目标改为单调递增、不可覆盖的 `vad.sessions`（播放循环重开 VAD 会话的确切证据）。产品代码无改动——break 于 playing 检查是正常停止路径。
- 版本号同步为 `0.3.2-patch1`（Python、前端、Tauri、Rust 四处）。

## 2026-08-17 构建结果

- Python：CI 全量 487 passed / 4 skipped；本地 512 passed / 5 skipped（含本地角色卡新测试）。CI 连续多轮运行无 flaky 复现。
- 前端：Vitest 全过，TypeScript typecheck 与 Vite production build 通过。
- Rust：13 passed；`cargo fmt --check` 通过。
- 桌面端：`desktop/src-tauri/target/release/hsr-partner-harness.exe` 全新构建（`cargo clean` 后完整重编译）；NSIS 安装包 `HSR Partner Harness_0.3.2-patch1_x64-setup.exe` 已生成。构建后首次运行状态重置已执行，`PairHarness` 本地数据目录与 WebView Local/Session Storage 已清空，deepseek 与 DashScope Key 不在任何构建制品中。
- 发布：已创建 tag `v0.3.2-patch1` 并发布 GitHub Release，安装包作为 release 附件分发。
