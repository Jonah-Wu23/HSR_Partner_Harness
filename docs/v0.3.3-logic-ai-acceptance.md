# V0.3.3 强逻辑 AI 轨道验收记录

> 阶段：V0.3.3（角色卡底座、语音边界冻结、手机远程 P0）——强逻辑 AI 轨道
> 分支：`v0.3.3-logic-ai`（基于 main @ 6681356）
> 日期：2026-08-18
> 按 `AGENTS.md` 第 10 节要求，区分代码实现、离线测试、真实联调、真机验收、视觉验收与打包结果。

## 0. 执行方式

主控（glm-5.3）+ 4 名实现成员（基元律动 DSV4F，经 workflow 显式指定 provider/model 派发）：

| 成员 | 交付 | 提交 |
| --- | --- | --- |
| A（DSV4F） | 角色卡持久化：迁移 9 + CharacterCardRepository | ef9ffe9 |
| B（DSV4F） | 助手永久禁用 TTS、语音边界冻结 | fb8b2fc |
| C（DSV4F） | WSServerMode + EventFanout + PWA 静态伺服 | 877ccf7 |
| D（DSV4F） | PairingService 配对码与 token 鉴权 | 6e2650b |
| 主控 | 批 0 基线与契约冻结、批 3 接线、验收、联调 | 47f3695…56f94d4 |

主控修复项：C 的 `event_fanout.py` 前向引用未定义名；批 3 接线期的循环导入（event_fanout→router→application_service）；补充遗漏的 `remote.issue_code` 命令（桌面端生成配对码入口）。

## 1. 代码实现（对照计划 V0.3.3 强逻辑 AI 16 条）

- 条 1–3：`character_cards`/`character_assets` 迁移 9（schema.sql 同构直建）；仓库服务（list/get/create_draft/update/duplicate/archive/delete/select_active）；序列化只走已交付 codec，未重写。
- 条 4：内置角色经 `card.list`/`card.get` 只读暴露（`builtin:<id>`，source=builtin，写操作拒绝 card_read_only），未重写内置内容。
- 条 5：角色卡与配对关系的最小接线（select_active / get_active_card_id 落 app_state；完整配对装配归 V0.3.5 角色创作工作室）。
- 条 6–8：`is_tts_eligible` 冻结为仅 `(CHARACTER, CHARACTER_SPEECH)`；助手消息全 kind 三入口（自动朗读/重播/预览）抛 `assistant_tts_disabled`；助手音色创建入口（provision 助手侧说话方）拒绝；v0.3.2 助手音色 ID 保留为无效旧数据。一键生成默认集合排除助手侧。
- 条 9：`assert_single_assistant_markdown` 装配断言接入三处 ExecutionContext 构建点；重复注入/混入即失败（上下文压缩机制本身归 V0.3.9，断言函数届时复用）。
- 条 10：参考音频 data URI 结论沿用并行任务验证记录，未重复消耗额度。
- 条 11–16：`--serve <port>` WS 服务器模式（同端口静态+WS，与 stdin 循环并行共享 service/Router）；EventFanout（stdout 权威优先、单订阅者故障隔离）；配对码（6 位、一次性、TTL 300s）与 token（secrets、恒定时间比较、撤销即时生效）鉴权，鉴权门在 dispatch 之前 fail-closed；PWA 静态伺服（`PAIR_HARNESS_PWA_DIR`，None 时 404）；事件带既有 sequence 单调序号；重连经 app.bootstrap 快照对齐，无合成事件。

## 2. 离线测试

- 全量：**591 passed / 5 skipped**（基线 522/5；`test_desktop_sidecar_loop` 命名管道集成测试需完整权限环境）。
- 新增：A 10 用例（含旧库 user_version=8 升级）、B 改造语音断言约 80 项（角色路径断言零改动全保持）、C 14 用例（真实 localhost WS）、D 41 用例（含 secrets/hmac 安全自查）、主控批 3 接线 18 用例（card/remote 命令、装配断言、配对状态重启恢复、--serve 进程内端到端）。
- `remote.issue_code` 补交后复跑全量绿。

## 3. 真实联调（脚本 `scripts/smoke_serve_mode.py`，真实子进程）

真实 `python -m pair_harness.desktop_backend --demo --serve <port>` 进程，11 项全过：

1. 进程启动，backend.ready 到 stdout；
2. stdin `remote.issue_code` 生成 6 位配对码；
3. stdin `card.list` 返回 3 个内置角色（stdin 循环与 WS 并行不受影响）；
4. WS 未鉴权 `app.bootstrap` 被拒（missing_token）；
5. WS `remote.pair` 配对换 token；
6. WS 带 token `app.bootstrap` 成功，response 回同一连接；
7. stdin 触发 `conversation.rename`，WS 收到扇出事件 `conversation.changed`（sequence 单调）；
8. stdin `remote.revoke` 撤销；
9. 已撤销 token 再连被真实拒绝；
10. `app.shutdown` 后进程退出码 0；
11. 端口释放无残留。

## 4. 真机/真实网络验收（2026-08-22 验收会执行）

环境：Windows 11，WLAN（网络类别"公用"），Mihomo TUN + 系统代理并存（实测对局域网入站与事件链路无影响）；小米手机浏览器，与 PC 同一 Wi-Fi；真实引擎（Reasonix ACP，仓库根 `.env` 配置），独立 `--data-dir`。

- [x] 真手机（手机浏览器）经局域网完成配对。**部分通过**：配对、错误配对码拒绝、未配对连接只见配对页全部通过；但"发送角色消息并收到真实回复"**无法执行**——手机端 PWA 只有委派输入（`chat.submit target=assistant`），没有角色消息入口（发现 1，见 visual 记录"真机验收发现"）。
- [x] 手机发起委派任务在 PC 项目目录真实执行，手机看到结构化结果卡片。真实 Reasonix 引擎多段思考流式 → `write_file` 工具卡运行中 → `approval.requested`（approval_id=1，路径 `E:\AI\某个测试项目\hello.txt`）→ stdin `approval.resolve` 批准 → 工具卡已完成、turn completed、会话标题自动生成（"新聊天"→"创建验收文件"）、角色总结真实 TTS（synthesizing→playing）。`hello.txt` 真实落盘、内容正确。事件序号 5→32 连续无缺口。附带真实验证：PC 重启 sidecar 后手机凭持久化 token 免配对自动重连。
- [ ] 桌面与手机同时在线看到同一条事件流，会话与任务状态不串线（进程内双客户端连续性已由集成测试覆盖，真机待验；当前 Tauri 未向 sidecar 传 `--serve`，双端同进程形态需补接线后执行）。

环境备注（供 V0.4.0"手机远程使用说明"）：公用网络下 Windows 防火墙已有规则不覆盖 venv Python，需手动放行入站 TCP 8765（本次规则名 `PairHarness Sidecar 8765`）；手动跑 sidecar 时 reasonix 需显式 `PAIR_HARNESS_REASONIX_BIN` 指向 npm `.cmd`（Tauri 打包形态由内置二进制注入，无此问题）。

注：本阶段手机无语音；http 局域网下麦克风不可用属预期。真实供应商对话链路（DeepSeek/Reasonix）回归沿用 v0.3.2-patch1 基线，本轮未改动其路径。

## 5. 视觉验收

强视觉 AI 轨道（React 桌面端 + 手机端布局）不在本轨道范围；`docs/design/web-prototype/` 原型已入库供其消费。本轨道交付的命令/事件/状态枚举即视觉轨道的接入契约。

## 6. 打包结果

按 `AGENTS.md` 发布节奏：V0.3.3 属开发迭代，应更新 `desktop/src-tauri/target/release/hsr-partner-harness.exe`，不生成 NSIS 安装包（下一次 NSIS 在 V0.4.0）。**release EXE 重建与前端联编属强视觉轨道集成后统一执行**，本记录日仅交付 Python 侧（Sidecar 逻辑与测试）。

## 7. 已知边界与移交项

- 手机 PWA 静态产物尚未构建（强视觉轨道交付后经 `PAIR_HARNESS_PWA_DIR` 指定打包，未指定时 / 返回 404，如实暴露）。
- `card.update` 接收 v3 payload；头像受管理资产目录与 `character_assets` 写入路径归 V0.3.5（配对能力与创作工作室同阶段）。
- WS 服务器绑定 0.0.0.0（局域网/Tailscale 接入），安全性由配对码+token 鉴权门保证；无 token 的连接无法触发 `handle_command`（fail-closed 已由测试与冒烟双重验证）。
- 审计日志当前为内存态（随 pairing_state 持久化），独立的连接日志文件留待 V0.3.5 多设备管理。
- **Ctrl+C 关闭路径缺陷（2026-08-22 真机验收发现）**：`--serve` 模式下手动 Ctrl+C 触发 KeyboardInterrupt，aiohttp WS 连接清理抛未处理异常（`CancelledError` 级联 `InvalidStateError`），退出不干净；`app.shutdown` 优雅退出路径（冒烟第 10–11 项）不受影响。需捕获 KeyboardInterrupt 后走同一有序停机路径，处置待用户定。
