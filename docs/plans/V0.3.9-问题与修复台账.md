# V0.3.9 问题与修复台账（真机验证 · S4 桌面批次）

> 本文件为**唯一问题登记处**，按《V0.3.9-真机验证补充测试.md》第 10 节格式逐条记录，不增删字段。
> 每条问题的证据路径相对于 `E:\AI\HSR-Partner-Harness-v0.3.9-logic\evidence\batch-2026-09-10\`。
> S4 整批只测不修：本文件不写测试工程师的修复动作，修复计划与复验结论由后续修复批次追加。

## 问题编号：V039-S4-001

作业限制：
- 批次/验收 ID：batch-2026-09-10 / A06（同时影响 A02 的摘要读取）
- 候选提交/构建：`897193a2fb983cd39c8328e98f7e225963a1ee64`，EXE 11,200,512 B（2026-09-10 02:08 +0800）
- 设备与网络：Windows 11 10.0.26200 / 2560x1440 / 缩放 150%；本机直连公网
- 前置状态：候选以 `--real` 运行，`--serve 8765`；账号「验收账号」已配置 DeepSeek；库内已有真实回合与摘要记录
- 项目/聊天/任务 ID：project-alpha `4577124a-20f7-4b21-a39d-546f0ce4078c`；会话 `10014344-d00d-40be-8fc4-9ba57bb81e55`、`c4630f11-07eb-4932-9727-b47bce3051c9`、`e5e91b6f-722a-4cd6-8557-989760491aee`
- 时间戳：2026-09-10T10:21Z（首次命中）、2026-09-10T10:33Z（爆炸半径复测）
- 证据路径：`A06/ws-frames-blast.log`、`A06/ws-frames.log`、`A06/a06-run.json`、`A06/sidecar.log`、`A02/a02-run.json`
- 严重度/频率/影响范围：P1 / 必现 / 桌面端与手机端共用的只读诊断读取路径
- 责任轨：Sidecar Python（协议序列化层 + 诊断只读命令）

业务现象：
（前提条件）候选以真实模式运行，`turn_metrics` 表内有 7 条真实回合计录，`conversation_summaries` 表内有 `status=completed` 的真实摘要（正文为模型对该会话语料的概括）。
（操作步骤）经真实鉴权 WS 通道依次调用 `metrics.query`（携带 conversation_id 或空过滤）与 `summary.get`（携带 conversation_id）；随后打开桌面端诊断抽屉的「指标」页并点击「刷新指标」。
（目前结果）两个命令均无任何响应，客户端在 30 秒后收到 `backend_timeout`；sidecar stderr 每次都留下同一条原始异常：

```
ERROR:asyncio:Task exception was never retrieved
future: <Task finished name='Task-903' coro=<SidecarRouter.handle_line() ...>
exception=ProtocolError('协议消息不可序列化：Object of type datetime is not JSON serializable')
TypeError: Object of type datetime is not JSON serializable
  File "pair_harness\desktop_backend\protocol.py", line 20, in encode_message
  ...
  File "pair_harness\desktop_backend\router.py", line 175, in handle_line
  File "pair_harness\desktop_backend\router.py", line 124, in respond
```

同一轮爆炸半径复测显示：`app.bootstrap`、`conversation.open`、`card.list`、`memory.list`、`power.get_status`、`remote.list_devices`、`remote.control_status`、`account.list`、`config.get` 全部正常返回，仅 `metrics.query` 与 `summary.get` 超时。诊断抽屉在失败的同时仍渲染「查询返回 0 条指标记录。」，与红字失败提示并列上屏。

期望表现：
（预期结果）两个只读命令返回结构化结果；缺失字段为 `null`、真实零为 `0`，出现错误时错误可定位且不吞异常；查询失败时不得同时给出「0 条」这一成功语义的结论。

修改方式：请自行在后端代码中定位问题,找到你认为最好的解决方案并给出开发Plan,说明拟修改内容，修复理由和验证方式;不要直接修改代码,也不要问我问题。

## 问题编号：V039-S4-002

作业限制：
- 批次/验收 ID：batch-2026-09-10 / A00、A07、A08（阻断全部真实链路用例的默认路径）
- 候选提交/构建：`897193a2fb983cd39c8328e98f7e225963a1ee64`，同上 EXE
- 设备与网络：Windows 11 10.0.26200；本机直连公网
- 前置状态：`%LOCALAPPDATA%\PairHarness` 不存在（首次运行清理后的冷启动状态），未放置 `.env`
- 项目/聊天/任务 ID：会话 `61bc818e-cab6-45f6-9f35-c6c7fdb82d3d`；引导第 2 步保存的 Key 写入账号 `default-local` 之后注册的账号 secret_refs
- 时间戳：2026-09-10T04:57:10Z（脚本化回复落库）
- 证据路径：`F-01-demo-mode-scripted-reply/`（截图）、`A00-real-chain/db-messages.txt`、`%APPDATA%\com.jonahwu.hsr-partner-harness\sidecar.stderr.log`
- 严重度/频率/影响范围：P1 / 必现 / 所有未随包提供 `.env` 的分发安装
- 责任轨：Rust 启动判定 + Python 演示/真实模式切换

业务现象：
（前提条件）删除本地数据目录后启动候选 EXE（无 `.env`），走首次引导并用真实 DeepSeek Key 完成「保存并测试」。
（操作步骤）引导第 2 步填入真实 Key → 保存并测试（界面推进到第 3 步「都准备好了」）→ 进入应用 → 向角色发送一条消息。
（目前结果）进程表显示 `pair-harness-sidecar.exe --demo --serve 8765 --project ...`；落库回复为脚本化固定文本「我听着呢。先在这里陪你把这件事慢慢说清楚。」，会话标题为脚本化产物「关于你好，今天过得怎么样」；全程无对外模型请求。界面无任何演示模式标识。同一进程在放置 `.env` 重启后即为 `--real` 并返回真实模型回复，说明账号级配置不参与真实模式判定。

期望表现：
（预期结果）界面配置的供应商生效（真实模型生成回复）；若候选因缺少环境配置只能运行演示数据，必须显式标注且不得接受并「测试通过」真实 Key。

修改方式：请自行在后端代码中定位问题,找到你认为最好的解决方案并给出开发Plan,说明拟修改内容，修复理由和验证方式;不要直接修改代码,也不要问我问题。

## 问题编号：V039-S4-003

作业限制：
- 批次/验收 ID：batch-2026-09-10 / A03
- 候选提交/构建：`897193a2fb983cd39c8328e98f7e225963a1ee64`，同上 EXE
- 设备与网络：Windows 11 10.0.26200；本机直连公网
- 前置状态：真实模式运行；`pair_memories` 表存在且为空；账号「验收账号」；project-alpha
- 项目/聊天/任务 ID：project-alpha `4577124a-20f7-4b21-a39d-546f0ce4078c`；账号 `0b38c054-d2f1-4d1d-80c5-c06ac05c1cd6`
- 时间戳：2026-09-10T10:37Z（入口侦查与协议核验）
- 证据路径：`A03/a03-run.json`（含四面侦查命中与协议探测结果）、`A03/ws-frames.log`
- 严重度/频率/影响范围：P1 / 必现 / 长期记忆功能整体（桌面端与手机端共用）
- 责任轨：Sidecar Python（写入路径缺失）+ 桌面端前端（无增删改入口）

业务现象：
（前提条件）真实模式运行，界面上下文状态条常驻显示「长期记忆已启用 / 配对共享 / 0 条生效」。
（操作步骤）① 代码面侦查长期记忆的写入路径；② 侦查桌面端 UI 是否有增删改入口；③ 侦查 src-tauri 是否暴露相关命令；④ 用真实鉴权连接调用 `memory.list`（缺失分量 / 完整分量 / 同 pair 换 assistant_identity / 换项目 / 按会话解析）与 `memory.update`、`memory.delete`（未知 id）。
（目前结果）① `upsert_memory` 在 `src/pair_harness/` 全库仅有一处定义（`storage/sqlite_store.py:1796`），**调用点零处**——运行期不存在任何写入 `pair_memories` 的路径；② `desktop/src/services/actions.ts` 与 `contracts/actions.ts` 无记忆命令动作，`desktop/src/ui`、`desktop/src/app` 内零处调用 `memory.list/update/delete`（仅 store 消费 `memory.updated/deleted` 事件、上下文状态条展示计数）；③ `desktop/src-tauri/src` 无记忆相关命令；④ 协议侧行为本身正确：缺分量报 `memory_invalid`（MemoryScope 五分量必填），三种完整作用域与按会话解析均返回 `{"memories": []}` 且可正常序列化，`memory.update`/`memory.delete` 对未知 id 报 `memory_not_found`。因此「0 条生效」恒为真，隔离性（条数、同 pair 换助手身份、同作用域跨聊天共享、摘要仅本聊天）无真实数据可验。

期望表现：
（预期结果）长期记忆应有真实写入路径（由对话产出记忆条目）与可操作的增删改入口；按 account/project/pair/character_ref/assistant_identity 隔离；同 pair 更换助手身份不读取旧助手记忆；同作用域跨聊天共享；摘要仅本聊天；增删改实际生效。

修改方式：请自行在后端代码中定位问题,找到你认为最好的解决方案并给出开发Plan,说明拟修改内容，修复理由和验证方式;不要直接修改代码,也不要问我问题。

## 问题编号：V039-S4-004

作业限制：
- 批次/验收 ID：batch-2026-09-10 / A14（远程接入前置条件）
- 候选提交/构建：`897193a2fb983cd39c8328e98f7e225963a1ee64`，同上 EXE
- 设备与网络：Windows 11 10.0.26200；本机回环 127.0.0.1:8765
- 前置状态：sidecar 以 `--real --serve 8765` 运行；`Get-NetTCPConnection` 显示 `0.0.0.0:8765` LISTEN，`ws://127.0.0.1:8765/ws` 可完成 `remote.pair` 握手并返回结构化响应
- 项目/聊天/任务 ID：不适用（界面状态）
- 时间戳：2026-09-10T10:04Z（生成配对码时观察到提示）
- 证据路径：`A14/02-remote-devices-page.png`、`A14/03-pairing-code-issued.png`
- 严重度/频率/影响范围：P2 / 必现 / 远程设备页与二维码入口
- 责任轨：Sidecar Python（服务地址探测/上报）+ Rust 启动信息

业务现象：
（前提条件）候选以 `--serve 8765` 正常启动，端口处于监听状态且 WS 握手可用（用该端口成功换取过真实鉴权 token）。
（操作步骤）打开设置中心 → 远程设备 → 点击「生成配对码」。
（目前结果）六位配对码正常生成（本次为 659304，剩余 4 分 52 秒），但同区域显示红字「远程服务地址未就绪：Sidecar --serve 未启动或启动失败，二维码暂不可用，请先核对桌面端启动提示」，二维码不可用；实际该端口当时可连通且能完成配对。

期望表现：
（预期结果）`--serve` 已监听且握手可用时，应上报可用的局域网接入地址并展示二维码；探测失败时应给出真实失败原因（如无局域网地址）而不是与事实相反的「未启动或启动失败」。

修改方式：请自行在后端代码中定位问题,找到你认为最好的解决方案并给出开发Plan,说明拟修改内容，修复理由和验证方式;不要直接修改代码,也不要问我问题。

## 问题编号：V039-S4-005

作业限制：
- 批次/验收 ID：batch-2026-09-10 / A14（首次运行账号门）
- 候选提交/构建：`897193a2fb983cd39c8328e98f7e225963a1ee64`，同上 EXE
- 设备与网络：Windows 11 10.0.26200；本机
- 前置状态：`%LOCALAPPDATA%\PairHarness` 已按首次运行清理删除（全新库）
- 项目/聊天/任务 ID：不适用（账号门状态）
- 时间戳：2026-09-10T04:55Z
- 证据路径：`A14/01-settings-account.png` 之前的账号门截图（`F-01` 目录内同批次截图）
- 严重度/频率/影响范围：P3 / 必现 / 全新安装的首次进入
- 责任轨：桌面端前端（账号门）+ Sidecar 默认账号种子

业务现象：
（前提条件）全新库，`accounts` 表由种子插入 `default-local/默认账号`，其 `password_hash` 为空。
（操作步骤）冷启动 → 账号门选中「默认账号」→ 点击「进入」（未填密码或填入任意非空密码）。
（目前结果）按钮在密码为空时禁用；填入任意非空密码后提示「密码错误」。该账号无任何可用出路，唯一路径是被迫注册新账号。

期望表现：
（预期结果）空密码的默认账号应可用空密码直接进入（或在首次进入时为其设置密码），不应展示一个不可用的账号条目。

修改方式：请自行在后端代码中定位问题,找到你认为最好的解决方案并给出开发Plan,说明拟修改内容，修复理由和验证方式;不要直接修改代码,也不要问我问题。

## 问题编号：V039-S4-006

作业限制：
- 批次/验收 ID：batch-2026-09-10 / A14（账号门）
- 候选提交/构建：`897193a2fb983cd39c8328e98f7e225963a1ee64`，同上 EXE
- 设备与网络：Windows 11 10.0.26200；本机
- 前置状态：账号门处于登录态，已出现一次登录失败
- 项目/聊天/任务 ID：不适用
- 时间戳：2026-09-10T04:56Z
- 证据路径：`F-01-demo-mode-scripted-reply/01-scripted-reply.png` 同批次账号门记录
- 严重度/频率/影响范围：P4 / 必现 / 首次运行体验
- 责任轨：桌面端前端（账号门）

业务现象：
（前提条件）登录默认账号失败，页面显示「密码错误」。
（操作步骤）点击「＋ 注册新账号」切换到注册表单。
（目前结果）注册表单顶部继续显示上一次登录留下的「密码错误」提示，与当前操作无关。

期望表现：
（预期结果）切换到注册表单时清理与登录相关的错误提示。

修改方式：请自行在后端代码中定位问题,找到你认为最好的解决方案并给出开发Plan,说明拟修改内容，修复理由和验证方式;不要直接修改代码,也不要问我问题。

## 问题编号：V039-S4-007

作业限制：
- 批次/验收 ID：batch-2026-09-10 / A14
- 候选提交/构建：`897193a2fb983cd39c8328e98f7e225963a1ee64`，同上 EXE
- 设备与网络：Windows 11 10.0.26200；本机
- 前置状态：候选 EXE 正常连接中
- 项目/聊天/任务 ID：不适用（连接状态呈现）
- 时间戳：2026-09-10T05:12Z
- 证据路径：`A14-recovery/01-sidecar-killed.png`（同帧内两种相反状态）
- 严重度/频率/影响范围：P3 / 必现（窗口 < 3 秒）/ 断连期间的连接状态呈现
- 责任轨：桌面端前端（连接状态药丸）

业务现象：
（前提条件）候选运行中，界面左上角显示绿点「已连接」。
（操作步骤）强制结束 sidecar 进程（`Stop-Process -Name pair-harness-sidecar -Force`），立即截图。
（目前结果）同一帧内：右上角 Toast 显示「Python Sidecar 已断开，正在重连…」，而左上角连接药丸仍为绿点「已连接」；约 3 秒后 Rust 侧拉起新 sidecar 进程并恢复。

期望表现：
（预期结果）sidecar 已断开期间连接状态应如实呈现为断开/重连中，与 Toast 一致。

修改方式：请自行在后端代码中定位问题,找到你认为最好的解决方案并给出开发Plan,说明拟修改内容，修复理由和验证方式;不要直接修改代码,也不要问我问题。

## 问题编号：V039-S4-008

作业限制：
- 批次/验收 ID：batch-2026-09-10 / A07、A08
- 候选提交/构建：`897193a2fb983cd39c8328e98f7e225963a1ee64`，同上 EXE
- 设备与网络：Windows 11 10.0.26200；本机直连公网
- 前置状态：真实模式；协作会话 `e5e91b6f-722a-4cd6-8557-989760491aee`（镜子）在 4 次审批后完成委派
- 项目/聊天/任务 ID：会话 `e5e91b6f-722a-4cd6-8557-989760491aee`；任务 `d55a166a-7c9c-4a20-bfe6-dd4f33d1c515`
- 时间戳：2026-09-10T05:08Z
- 证据路径：`A07-delegation/db-delegation.txt`（会话消息全量导出）
- 严重度/频率/影响范围：P3（待确认）/ 观察到的 3 次 / 助手消息在时间线上的呈现
- 责任轨：Sidecar Python（助手自然语言消息落库）

业务现象：
（前提条件）协作模式真实委派回合，引擎完成 4 轮工具调用后给出结构化结果摘要。
（操作步骤）委派结束后导出该会话的全部消息记录。
（目前结果）出现 3 条 `source=assistant`、`kind=assistant.natural_language`、`text` 为空、`status=done` 的消息，每条之后紧跟一条 `system.approval`；该回合最后一条结果摘要消息正文正常。空文本消息的成因未确认（是否仅为中间空 delta 的持久化）。

期望表现：
（预期结果）落库的助手自然语言消息要么承载真实正文、要么不生成空条目；时间线不应出现无内容的助手气泡。

修改方式：请自行在后端代码中定位问题,找到你认为最好的解决方案并给出开发Plan,说明拟修改内容，修复理由和验证方式;不要直接修改代码,也不要问我问题。

## 问题编号：V039-S4-009

作业限制：
- 批次/验收 ID：batch-2026-09-10 / A07
- 候选提交/构建：`897193a2fb983cd39c8328e98f7e225963a1ee64`，同上 EXE
- 设备与网络：Windows 11 10.0.26200；本机直连公网
- 前置状态：真实模式，引擎为 DeepSeek/Reasonix（ACP）
- 项目/聊天/任务 ID：会话 `e5e91b6f-722a-4cd6-8557-989760491aee`；任务 `d55a166a-7c9c-4a20-bfe6-dd4f33d1c515`
- 时间戳：2026-09-10T05:08Z
- 证据路径：sidercar stderr（`A07-delegation/sidecar.log` 同批次摘录）、`A07-delegation/03-delegation-completed.png`
- 严重度/频率/影响范围：P2（待确认）/ 观察到的 1 次 / 委派终态的真实性
- 责任轨：Sidecar Python（引擎事件 → 任务终态映射）

业务现象：
（前提条件）协作模式真实委派，4 次工具调用全部成功、目标文件按预期内容落盘。
（操作步骤）委派结束后查看 sidecar 原始日志与委派卡片状态。
（目前结果）日志留下 `WARNING:pair_harness.adapters.acp.engine:Reasonix returned terminal error after successful tool execution: error`，而界面委派卡片显示「已完成」，角色也给出了成功口吻的收尾语；界面无任何错误呈现。该 warning 的具体语义未确认。

期望表现：
（预期结果）引擎若以错误终态结束，界面应如实呈现失败（或明确说明该 warning 为可忽略的会话关闭噪声），不得让用户看到与引擎终态相反的「已完成」。

修改方式：请自行在后端代码中定位问题,找到你认为最好的解决方案并给出开发Plan,说明拟修改内容，修复理由和验证方式;不要直接修改代码,也不要问我问题。

## 问题编号：V039-S4-010

作业限制：
- 批次/验收 ID：batch-2026-09-10 / A06（可观测性）
- 候选提交/构建：`897193a2fb983cd39c8328e98f7e225963a1ee64`，同上 EXE
- 设备与网络：Windows 11 10.0.26200；本机
- 前置状态：EXE 经 `tauri-with-first-run-reset.ps1` 清理 `%LOCALAPPDATA%\PairHarness` 与 WebView Local/Session Storage
- 项目/聊天/任务 ID：不适用
- 时间戳：2026-09-10T10:37Z
- 证据路径：`%APPDATA%\com.jonahwu.hsr-partner-harness\sidecar.stderr.log`（跨会话追加，无会话分隔）
- 严重度/频率/影响范围：P4 / 必现 / 证据归属与排障效率
- 责任轨：打包脚本 + Sidecar 日志策略

业务现象：
（前提条件）同一台机器上先后运行多个候选会话（含上一批次遗留内容）。
（操作步骤）按批次开始时间点截取 stderr 相关行、尝试把日志行归属到具体会话。
（目前结果）`%APPDATA%\com.jonahwu.hsr-partner-harness\sidecar.stderr.log` 跨会话持续追加且无会话分隔/滚动（首次运行清理不覆盖该目录，`projects\initial-project` 与日志一并保留）；同一文件里混有上一批次的 traceback，本次批次需要靠 stderr 与数据库交叉比对才能归属。

期望表现：
（预期结果）日志按会话分隔或按次滚动（或记录会话起始标记），首次运行清理覆盖应用数据目录中的日志，使每个批次可独立取证。

修改方式：请自行在后端代码中定位问题,找到你认为最好的解决方案并给出开发Plan,说明拟修改内容，修复理由和验证方式;不要直接修改代码,也不要问我问题。

## 问题编号：V039-S4-011

作业限制：
- 批次/验收 ID：batch-2026-09-10 / A06、A08
- 候选提交/构建：`897193a2fb983cd39c8328e98f7e225963a1ee64`，同上 EXE
- 设备与网络：Windows 11 10.0.26200；本机
- 前置状态：真实模式；一组会话未绑定角色卡、另一组绑定导入卡
- 项目/聊天/任务 ID：未绑定卡会话 `10014344-d00d-40be-8fc4-9ba57bb81e55`；绑定卡会话 `daa0a2bc-629c-424b-8c0b-c2b48b83c3fb`
- 时间戳：2026-09-10T10:31Z
- 证据路径：`A02/a02-run.json`（assembly 探测）、`A08/a08-run.json`（绑定卡会话 modules 非空）
- 严重度/频率/影响范围：P3 / 必现 / 装配诊断对「无卡」与「空装配」的区分
- 责任轨：Sidecar Python（diagnostics.prompt_assembly 返回体）

业务现象：
（前提条件）会话未绑定角色卡（`character_card_id` 为空）。
（操作步骤）调用 `diagnostics.prompt_assembly`（普通与 `include_hidden=true`）。
（目前结果）返回 `{"modules": [], "diagnostics": []}`，没有任何说明字段；同一命令在绑定导入卡的会话上返回 5 个模块（世界书（角色设定前）、角色设定、性格、场景、世界书（角色设定后））。调用方无法区分「未绑定卡」与「绑定卡但装配为空」，与实现注释声明的「无绑定卡/卡已删除时返回空模块列表并附说明」不一致。

期望表现：
（预期结果）空模块结果应附带明确的说明字段（如未绑定卡/卡已删除），使调用方与界面能如实区分两种情况。

修改方式：请自行在后端代码中定位问题,找到你认为最好的解决方案并给出开发Plan,说明拟修改内容，修复理由和验证方式;不要直接修改代码,也不要问我问题。

## 问题编号：V039-S4-012

作业限制：
- 批次/验收 ID：batch-2026-09-10 / A08、A09
- 候选提交/构建：`897193a2fb983cd39c8328e98f7e225963a1ee64`，EXE 11,200,512 B（2026-09-10 02:08 +0800）
- 设备与网络：Windows 11 10.0.26200；本机直连 DashScope 北京专属域
- 前置状态：真实模式；语音开启；账号未保存 DashScope Key，使用开发机 `.env` 作者音色
- 项目/聊天/任务 ID：会话 `b3b1bc41-67c7-4b69-baa3-ffc1923e9109`（连续 80 个真实回合）、`225c5885-7492-4853-a1c2-d51e7ef0870e`（A09）
- 时间戳：2026-09-10T10:27Z–10:29Z（限流）、2026-09-10T10:57Z（粘性错误）
- 证据路径：`A02/sidecar.log`、`A08/sidecar.log`、`A08/a08-run.json`（`voice_runtime_state.error`）、`A09/a09-run.json`
- 严重度/频率/影响范围：P3 / 高频真实回合下必现；粘性错误必现 / 语音朗读的可用性呈现与成本
- 责任轨：Sidecar Python（TTS 队列、节流与错误生命周期）

业务现象：
（前提条件）语音开启，连续多轮角色回复各自触发一次在线 TTS 合成；界面输入区在 `voice.error` 非空时显示「语音异常：{error}」（`desktop/src/ui/composer/Composer.tsx:35`）。
（操作步骤）数分钟内产生约 80 条真实角色回复，观察 sidecar 日志与 `bootstrap.voice`；随后把语音总开关置为 false，再次读取 `bootstrap.voice`。
（目前结果）① 上游限流真实发生并**已如实暴露**（这一点符合 Let It Fail）：日志出现 `Throttling.RateQuota … Requests rate limit exceeded` 与 `websocket:close status: 1011`，`bootstrap.voice.tts=failed` 且 `voice.error` 携带供应商原始报文；同一时段还出现 `WARNING:…qwen_tts:TTS synthesis thread did not stop within 0.5s`。合成对每条最终角色消息都会发起，未见节流或退避。② `voice.error` 是**粘性**的：在合成状态回到 `idle`、乃至把 `voice.enabled` 置为 false 之后，`error` 字段仍保留数十分钟前的旧限流报文，界面因此持续显示「语音异常：…」，与当前状态不符。

期望表现：
（预期结果）高频回合下语音合成应有节流/退避（或按可用额度降级）避免稳定触发上游限流；失败错误应在状态恢复（成功合成或语音关闭）时清除，界面不应长期展示与当前状态矛盾的旧错误。

修改方式：请自行在后端代码中定位问题,找到你认为最好的解决方案并给出开发Plan,说明拟修改内容，修复理由和验证方式;不要直接修改代码,也不要问我问题。

## 问题编号：V039-S4-013

作业限制：
- 批次/验收 ID：batch-2026-09-10 / A02
- 候选提交/构建：`897193a2fb983cd39c8328e98f7e225963a1ee64`，同上 EXE
- 设备与网络：Windows 11 10.0.26200；本机直连公网
- 前置状态：真实模式；两条自动压缩均以真实模型完成（`status=completed`）
- 项目/聊天/任务 ID：会话 `10014344-d00d-40be-8fc4-9ba57bb81e55`（字节阈值）、`b3b1bc41-67c7-4b69-baa3-ffc1923e9109`（条数阈值）
- 时间戳：2026-09-10T10:24Z、2026-09-10T10:27Z
- 证据路径：`A02/a02-run.json`（summary.completed 事件字段）、`A02/sidecar.log`
- 严重度/频率/影响范围：P4 / 必现 / 摘要记录的可追溯性
- 责任轨：Sidecar Python（摘要记录字段回填）

业务现象：
（前提条件）自动压缩以真实模型完成。
（操作步骤）读取 `summary.completed` 事件字段与落库的摘要记录。
（目前结果）`provider`、`model` 均为 `null`，摘要记录未标注生成所用的供应商与模型；同一份记录里 `content` 与覆盖区间正常。

期望表现：
（预期结果）摘要记录应带上真实使用的供应商与模型标识，便于按批次核对“真实链路”证据。

修改方式：请自行在后端代码中定位问题,找到你认为最好的解决方案并给出开发Plan,说明拟修改内容，修复理由和验证方式;不要直接修改代码,也不要问我问题。

## 问题编号：V039-S4-014

作业限制：
- 批次/验收 ID：batch-2026-09-10 / A06
- 候选提交/构建：`897193a2fb983cd39c8328e98f7e225963a1ee64`，EXE 11,200,512 B（2026-09-10 02:08 +0800）
- 设备与网络：Windows 11 10.0.26200；本机直连公网
- 前置状态：账号「验收账号」，provider=deepseek；另有将 provider 切到 OpenAI 兼容后端的尝试（被如实拒绝）
- 项目/聊天/任务 ID：会话 `bf308a83-bc03-4702-bff4-2473ca99656a`、`cd8393c8-3765-4c3e-bcb7-455fd8078b90`；账号 `0b38c054-d2f1-4d1d-80c5-c06ac05c1cd6`
- 时间戳：2026-09-10T10:42Z、2026-09-10T10:44Z
- 证据路径：`A06/ws-frames-inject2.log`、`A06/ws-frames-unreachable.log`、`A06/a06-injections.json`、`A06/a06-unreachable.json`
- 严重度/频率/影响范围：P2 / 必现 / 模型设置页的所有端点类改动与「保存并测试」的结论可信度
- 责任轨：Sidecar Python（配置规范化与连接探测）

业务现象：
（前提条件）账号以 provider=deepseek 正常运行，端点 `https://api.deepseek.com`。
（操作步骤）调用 `config.set` 把 `dialogue.base_url` 分别改为 `https://127.0.0.1:1`（本机必被拒的连接）与 `https://no-such-host-s4.invalid`（不存在的主机），随后调用 `config.get` 回读，并调用 `config.test_connection`。
（目前结果）`config.set` 两次都返回成功；`config.get` 回读显示 `base_url` 仍是 `https://api.deepseek.com`（写入被静默改写，调用方无从察觉）；`config.test_connection` 返回 `{"ok": true, "message": "连接正常（延迟 531 ms）"}`——对一个从未落库、且本身不可达的端点给出连接正常的结论。另一次尝试把 provider 切到 `openai_compatible` 时被如实拒绝（「codex 引擎要求 Responses API 后端…」），说明拒绝路径本身是清楚的，而同一 provider 下的端点改写是静默的。

期望表现：
（预期结果）写入被规范化或拒绝时，`config.set` 应如实告知（例如端点不被该 provider 接受），不得返回成功却丢弃字段；`config.test_connection` 只能针对真实生效的配置给出「连接正常」，探测失败应返回可定位的失败原因。

修改方式：请自行在后端代码中定位问题,找到你认为最好的解决方案并给出开发Plan,说明拟修改内容，修复理由和验证方式;不要直接修改代码,也不要问我问题。

## 问题编号：V039-S4-015

作业限制：
- 批次/验收 ID：batch-2026-09-10 / A06
- 候选提交/构建：`897193a2fb983cd39c8328e98f7e225963a1ee64`，同上 EXE
- 设备与网络：Windows 11 10.0.26200；本机直连公网
- 前置状态：账号配置为有效端点 + 无效 API Key（`sk-s4-invalid-key-0000000000`）
- 项目/聊天/任务 ID：会话 `393b8673-5c26-4ad3-8e42-ab3020269ae3`；turn `d9dbe75e-045d-4ab3-8eb9-8e4a33dd3578`
- 时间戳：2026-09-10T10:43:59Z
- 证据路径：`A06/a06-turn-terminal.json`、`A06/a06-run.json`、`A06/sidecar.log`
- 严重度/频率/影响范围：P2 / 必现 / 所有真实模型失败的回合
- 责任轨：Sidecar Python（回合失败消息的构造）

业务现象：
（前提条件）会话以无效 API Key 发起真实回合。
（操作步骤）提交一条角色消息，等待回合计入终态，读取会话快照与 sidecar 日志。
（目前结果）turn 终态 `failed`、用户消息 `failed`（终态本身正确），但会话里出现一条 `system.status` 消息，正文是 `本次回复失败：`——冒号后没有任何原因；真实的失败原因（供应商返回的 401 原文与堆栈）只存在于 sidecar stderr。用户在不看日志的情况下无法知道失败原因。

期望表现：
（预期结果）失败提示应携带可定位的真实原因（至少包含失败类型与供应商/网络返回的原始信息），与日志保持同一事实，不得让界面提示成为空壳。

修改方式：请自行在后端代码中定位问题,找到你认为最好的解决方案并给出开发Plan,说明拟修改内容，修复理由和验证方式;不要直接修改代码,也不要问我问题。

## 问题编号：V039-S4-016

作业限制：
- 批次/验收 ID：batch-2026-09-10 / A08、A09
- 候选提交/构建：`897193a2fb983cd39c8328e98f7e225963a1ee64`，同上 EXE
- 设备与网络：Windows 11 10.0.26200；本机 WS（`remote.claim_control` 已成功）
- 前置状态：远程端已通过 `remote.claim_control` 取得播放控制权（返回 `{"claimed": true, "active_controllers": 1}`）
- 项目/聊天/任务 ID：会话 `225c5885-7492-4853-a1c2-d51e7ef0870e`；控制权由 device_name=s4-a09 持有
- 时间戳：2026-09-10T10:45Z
- 证据路径：`A09/a09-run.json`（`control_claim` 与 `preview` 字段）、`A09/ws-frames.log`
- 严重度/频率/影响范围：P3 / 持权远程端必现 / 远程控制态下的本地试听与长播放构造
- 责任轨：Sidecar Python（播放控制守卫与错误文案）

业务现象：
（前提条件）远程连接已成功领取播放控制权。
（操作步骤）同一远程连接调用 `voice.preview` 请求试听。
（目前结果）返回 `remote_playback_active`，消息为「手机正在控制语音，请先在手机退出远程控制或撤销该设备」。调用方就是唯一持有控制权的远程端，提示要求「手机退出远程控制」与其自身状态矛盾；结果是该路径下无法发起试听，长播放只能改用角色回复路径构造。

期望表现：
（预期结果）守卫语义与提示应自洽：明确区分「桌面播放被远程接管」与「远程自身发起试听」两种情形，或在持有控制权的远程端允许试听（如产品设计如此），不得给出让调用方去退出自己控制权的指引。

修改方式：请自行在后端代码中定位问题,找到你认为最好的解决方案并给出开发Plan,说明拟修改内容，修复理由和验证方式;不要直接修改代码,也不要问我问题。

## 问题编号：V039-S4-017

作业限制：
- 批次/验收 ID：batch-2026-09-10 / A08（同时影响 A02 的长语料装配）
- 候选提交/构建：`897193a2fb983cd39c8328e98f7e225963a1ee64`，同上 EXE
- 设备与网络：Windows 11 10.0.26200；本机
- 前置状态：导入卡 `ad95dd92f89d427f974e69d44b366901`（85 条世界书，正文 129,992 字节）绑定会话
- 项目/聊天/任务 ID：会话 `daa0a2bc-629c-424b-8c0b-c2b48b83c3fb`
- 时间戳：2026-09-10T10:39Z
- 证据路径：`A08/a08-run.json`（assembly.hidden.diagnostics）、`A08/ws-frames.log`
- 严重度/频率/影响范围：P2 / 长世界书必现 / 装配预算约束与提示词体积
- 责任轨：Sidecar Python（世界书激活与预算裁剪）

业务现象：
（前提条件）会话绑定含 85 条条目的世界书角色卡，其中 24 条为 constant（无条件注入）。
（操作步骤）调用 `diagnostics.prompt_assembly`（普通与 include_hidden=true）读取装配诊断。
（目前结果）诊断为 `scan_depth: 2`、`scanned_message_count: 2`、`budget_total: 2048`、`budget_used: 21552`，并列出多条 `overflow_entries`（模拟宇宙相关条目等）。预算被超出约 10 倍，说明实际注入量并未被 `budget_total` 约束，或统计口径与裁剪结果不一致；提示词体积随之膨胀，直接放大真实模型调用成本。

期望表现：
（预期结果）世界书注入量应受 `budget_total` 约束（超出部分按优先级裁剪），诊断中的 `budget_used` 与裁剪后的真实注入量一致，`overflow_entries` 明确列出被裁掉且未进入提示词的条目。

修改方式：请自行在后端代码中定位问题,找到你认为最好的解决方案并给出开发Plan,说明拟修改内容，修复理由和验证方式;不要直接修改代码,也不要问我问题。
