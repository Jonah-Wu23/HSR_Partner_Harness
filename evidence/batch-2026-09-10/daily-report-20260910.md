# S4 真机验证日报（2026-09-10）

批次：`batch-2026-09-10`（桌面/非移动专项）。
候选：`feat/v0.3.9-logic` @ `897193a2fb983cd39c8328e98f7e225963a1ee64`，契约 contract-v1。
证据根目录：`E:\AI\HSR-Partner-Harness-v0.3.9-logic\evidence\batch-2026-09-10\`。

## 今日执行

| 用例 | 状态 | 一句话结果 |
| --- | --- | --- |
| A01 并发/排队/取消/审批归属 | 通过 | 四会话协议级并发提交差值 0.0s；归属零串线；0.187s 排队项按序派发；取消回收待审批 |
| A02 压缩触发与摘要 | 失败 | 字节（单条 402,315 B）与条数（80 条）两条阈值均真实触发并由真实模型生成摘要；`summary.get` 无法读回 |
| A03 长期记忆 | 失败 | 写入路径缺失（`upsert_memory` 零调用者）+ 界面零入口；协议侧作用域与错误语义正确 |
| A04 后台运行与重启 | 通过 | 委派后台持续运行、切换导航正常；重启后最后活跃会话与完整时间线（审批结果、委派终态、角色终局回复、工具记录）正确恢复（GUI 批次已完成项，本批次只做交付物规范化） |
| A06 指标/装配诊断/失败注入 | 失败 | 隐藏原文规则通过；`metrics.query` 有记录即超时；`base_url` 被静默改写且测试连接误报成功；失败回合缺原因 |
| A07 真实委派与多轮工具 | 通过（Codex 子项受阻） | Reasonix 全链路真实：角色→委派→4 轮工具→4 次审批→确定性落盘 |
| A08 长世界书/装配诊断/语音 | 失败 | 装配诊断返回 5 个世界书模块；真实 ASR 逐字命中；**装配预算被超出约 10 倍**（V039-S4-017）；混合时间线缺工具与思考条目（B-05，夹具卡无可委派配置）；语音输出子项受阻（B-02） |
| A09 语音抢占 | 失败 | 520 字长回复真实播放中被桌面发送打断，8 秒后未复活；**preview 起播路径被控制权守卫误拒**（V039-S4-016，持权端无可用起播路径）；语音输出子项受阻（B-02） |
| A12 审批并发/重放/真实超时 | 通过 | 并发裁决首个终态获胜、重放被拒且携带真实终态；实测等待 **600.0 秒**得到 timeout |
| A13 角色库/导入导出/长字段 | 通过 | 85 条世界书导入导出往返一致（正文 129,992 B）；242,332 字符长字段写入回读一致 |
| A14 鉴权/断链/休眠恢复 | 通过 | 错误/缺失/撤销 token 全部如实拒绝；在途断链不丢工作；真实 Modern Standby **4 分 53 秒**后新 turn 成功 |

统计：通过 6（A01、A04、A07、A12、A13、A14，其中 A04 属 GUI 批次已完成项，本批次只做交付物规范化，未重复执行）、失败 5（A02、A03、A06、A08、A09）、受阻子项 4（A06 断网注入 B-01、A07 Codex 事件 B-03、A08/A09 语音输出 B-02、A08 混合时间线 B-05），无整例受阻。
口径说明：`voice.preview` 被拒与世界书预算超限是产品缺陷，判失败并登记 V039-S4-016/017，不计入受阻。
A05 与移动平台矩阵（A10/A11）不在本批次范围。

## 今日新发现（已登记入《V0.3.9-问题与修复台账.md》）

| 编号 | 摘要 | 严重度 |
| --- | --- | --- |
| V039-S4-001 | `metrics.query`/`summary.get` 响应体含 datetime 导致协议不可序列化，命令超时且失败与「0 条」并列上屏 | P1 |
| V039-S4-002 | 打包版首次运行恒为演示模式，界面配置无法切到真实模型 | P1 |
| V039-S4-003 | 长期记忆无写入路径且界面无入口 | P1 |
| V039-S4-004 | `--serve` 已正常监听仍报「远程服务地址未就绪」 | P2 |
| V039-S4-005 | 全新安装「默认账号」是死入口 | P3 |
| V039-S4-006 | 切到注册表单保留上一次登录错误 | P4 |
| V039-S4-007 | 断连瞬间连接药丸仍显示「已连接」 | P3 |
| V039-S4-008 | 空文本 `assistant.natural_language` 落库（待确认） | P3 |
| V039-S4-009 | Reasonix 工具全成功后报告终态错误而界面显示「已完成」（待确认） | P2 |
| V039-S4-010 | 首次运行清理未覆盖 `%APPDATA%` 日志与 initial-project | P4 |
| V039-S4-011 | 装配诊断空模块列表未附说明 | P3 |
| V039-S4-012 | 连续真实回合触发上游 TTS 限流（错误已如实进入 voice.error，成本需约束） | P3 |
| V039-S4-013 | 摘要记录未保存 provider/model | P4 |
| V039-S4-014 | `dialogue.base_url` 写入被静默改写但返回成功，`test_connection` 对未落库端点仍报「连接正常」 | P2 |
| V039-S4-015 | 回合失败时提示为「本次回复失败：」空原因，原始错误只进日志 | P2 |
| V039-S4-016 | 远程持有播放控制权时 `voice.preview` 以「手机正在控制语音」为由拒绝；源码核对确认守卫不比较调用方与租约持有者，控制期内无任何可用起播路径 | P2 |
| V039-S4-017 | 393KB 世界书激活使装配预算被超出约 10 倍（budget_used 21,552 / total 2,048，warnings 空，世界书模块实占约 14,400 字符）；constant 条目不参与预算判断、`budget_used` 为累计口径 | P2 |

## 证据与可复现

- 每个用例目录含 `result.json`、原始帧日志（`ws-frames*.log`）、事件流（`events*.log`）、
  sidecar stderr 摘录（`sidecar*.log`）、`commands.ps1`、`http.log`，本批次另含 `notes.md`。
- 夹具与哈希：`fixtures/fixtures-manifest.json`（fix-a / fix-b / fix-b2 / fix-c 的 SHA256、大小、生成脚本与导入方式）。
- 候选与资源哈希、设备与音频设备、服务与模型标识：`batch-manifest.json`。
- 协议客户端：`tools/ph_client.py`（真实 token、逐帧时间戳、token 只记 SHA256 前缀）；
  用例脚本 `tools/run_a*.py`；夹具生成 `tools/make_fixtures.py`。

## 纪律与过程说明（如实记录）

1. **只测不修**：未修改候选代码、前端资源、Sidecar 与数据库；`.env` 只作为真实模式入口被复制到
   `%LOCALAPPDATA%\PairHarness\.env`（这本身是 V039-S4-002 的一部分）。
2. **测试脚本自身缺陷已修并复测**：首轮 A01 脚本存在「响应先到而未被认领即丢弃」的缺陷，造成一次假超时；
   已修复（`_unclaimed` 缓存）并以修复后脚本复测并发/排队/取消。A14 首轮「在途切断」因等待窗口过长未构成在途，
   已改为「先确认请求已达服务端再断链」并复测。
3. **外部因素导致的中断**：A12 首轮 600 秒测量因测试脚本事件字段取值错误（未读 `payload`）而空等，
   已修正谓词后重跑，得到 600.0 秒实测。A06 首轮防火墙注入因权限失败，已登记 B-01。
4. **成本控制**：用户在 A08/A09 后要求停止对 393KB 相关回合进行语音合成，账号级 `voice.enabled` 已置 false；
   此前真实语音合成产生的额度消耗已如实记录，不作为后续用例的前提。
5. **错误注入的账号隔离未达成**：A06 计划在独立账号上注入错误，但 `account.register` 返回体不含
   `account_id`，测试脚本以空 id 调 `account.login` 被拒，注入实际落在验收账号上；
   执行结束后已用原始 `.env` 值恢复并核对 `config.get`（provider=deepseek、base_url=api.deepseek.com、Key 掩码一致）。

## 下一步

1. 修复批次处理 V039-S4-001/002/003（P1）与 014/015/016/017（P2）后，按本目录 `blockers.md` 的复验条件重跑 A02/A03/A06。
2. A08/A09 的语音输出子项在 V039-S4-016 修复（持权端起播路径可用）且额度与成本可控时复验；世界书装配在 V039-S4-017 修复后复验长世界书注入量；A08 混合时间线按 B-05 用可真实委派的 pair 承载世界书卡后重取工具与思考条目。
3. A07 的 Codex 事件子项待 Responses 兼容后端就绪后执行。
4. A05 与移动平台矩阵按范围另行安排。
