# Repository Guidelines

## 协作语言与执行方式

- 默认使用简体中文沟通、文档与注释。
- 直接推进当前主线，遵循 KISS、YAGNI 和最小改动原则。
- 先完成构思与设计确认，再编写实现计划，设计未获确认前不开始编码。
- 当前项目按本地单用户研究型 MVP 处理，不主动加入生产级权限、审计、遥测、插件化或复杂恢复机制。
- 终端命令使用 PowerShell 语法。

## 项目目标

将 `E:\AI\二次元情感陪伴助手` 中可复用的对话、角色配置和语音能力迁移为一个桌面端“角色扮演 + AI 代码编程 Harness”系统。

新系统使用 PyQt5 前端和 Python 后端，不包含 3D 角色展示、3D 互动、换装、相册或 MMD 资源链路。

## 核心角色关系

系统包含三组固定搭档：

- 白厄 + 神秘的古代机械
- 流萤 + 萨姆
- 三月七 + 第四面镜

每组包含“角色”和“助手”两个独立发言者。用户与角色共同向助手提出、讨论和调整任务。

- 角色负责角色扮演、讨论、建议、态度表达和对任务结果的回应。
- 助手负责代码编辑、文件操作、命令执行、工具调用、执行进度与结果汇报。
- 角色不得伪装成自己执行了文件操作、命令或工具调用。
- 用户直接要求角色干活时，角色应保持人物口吻回应，并把实际执行交给对应助手。
- 角色、助手、用户、系统/工具输出必须保留明确的身份字段，不靠文本猜测身份。

## 界面与消息展示

- 前端采用 PyQt5，后端采用 Python。
- 视觉主题由 `src/pair_harness/ui/theme.py` 统一驱动：所有颜色、圆角、字体走令牌（`DARK_TOKENS`/`LIGHT_TOKENS`），组件实现 `set_palette(tokens)` 响应切换；顶栏 `themeToggle` 按钮在深浅主题间切换，选择经 `QSettings` 持久化，默认深色。角色/助手气泡颜色继续由 `PairTheme` 驱动，深色主题下品牌色值有测试锁定，不得改动。
- 角色发言、助手发言、用户消息、命令/工具输出使用不同颜色或视觉样式。
- 命令、文件操作、工具调用与运行状态应作为可展开的结构化记录展示，参考 Codex 中“自然语言说明 + 折叠命令记录”的层次。
- 不恢复或移植任何 3D 视口、模型加载、动作、口型、触摸互动或 WebGL 代码。

## 语音边界

- ASR 使用 `qwen-audio-3.0-asr-flash-streaming`。
- TTS 使用 `qwen-audio-3.0-tts-flash`。
- 只有面向用户的自然语言发言进入 TTS。
- 命令文本、文件路径、工具调用参数、工具输出、运行日志、状态提示和代码块不得朗读。
- 语音文案必须保持当前角色或助手的身份与表达特色。

## Harness 执行边界

- Harness 的最小闭环为：接收用户/角色意图，形成助手任务，调用本地代码工具，持续展示进度，返回结果，再由角色进行角色化回应。
- 工具执行权只属于助手层。角色层可以提出或协商任务，但不能直接调用工具。
- 所有消息至少区分 `user`、`character`、`assistant`、`tool` 四类来源。
- 实现时优先保证单个角色搭档的完整闭环，再扩展到另外两组，避免同时铺开三套重复逻辑。

## 原项目复用原则

- 原项目路径固定为 `E:\AI\二次元情感陪伴助手`，默认只读检查；未经明确要求不得修改其文件。
- 原项目当前存在未提交改动和本地资源文件，迁移时不得覆盖、清理或重置。
- 优先评估复用 Python 后端中的角色卡解析、对话编排、模型提供方封装、ASR/TTS 接口和已有测试。
- React/Next.js 页面、Web/PWA 状态管理、Three.js/MMD、模型与动作资源不得直接搬入新项目。
- 复用代码前先确认依赖边界；能小范围复制并改造时，不整体复制旧仓库。

## 开发顺序

1. 确认单组角色与助手的消息协议、发言权和语音规则。
2. 跑通纯文本角色扮演与助手执行闭环。
3. 接入流式 ASR 和 TTS，并保证工具记录静音。
4. 扩展白厄、流萤、三月七三组配置。
5. 用户明确要求后再加入额外能力。

## 验证标准

- 角色收到执行请求时保持角色口吻，并将执行交给对应助手。
- 只有助手能够产生文件、命令或工具事件。
- 三组角色与助手能独立切换，身份、颜色、上下文和语音配置不串线。
- ASR 能流式形成用户输入；自然语言发言可合成语音；命令、日志与代码不进入 TTS。
- 不加载任何 3D/MMD 资源，也不依赖浏览器前端。

## 当前项目状态（2026-08-11）

### 项目位置与文档

- 当前项目根目录：`E:\AI\HSR Partner Harness`。
- 原目录名已经由用户手动更改，后续不得再使用 `E:\AI\白厄Harness` 作为项目路径。
- 远程仓库：`https://github.com/Jonah-Wu23/HSR_Partner_Harness`，本地 `main` 与 `origin/main` 已同步。
- 已完成并确认总体设计：`docs/specs/2026-08-10-roleplay-coding-harness-design.md`。同日修订：项目级沙箱、三种审批模式与审查智能体进入 MVP（见 §4.2、§4.3、§6.3）。
- 已完成详细版两阶段实施计划：`docs/plans/2026-08-10-mvp-implementation-plan.md`。2026-08-11 修订：B1 扩展为外部模型 API 接入（DeepSeek 优先），新增 B1.1 设计小节（参考仓库内 `DeepSeek-Reasonix/`，MIT License）。
- 实施模型的审查与修补提示词：`docs/plans/2026-08-10-implementer-kickoff-prompt.md`。
- 已完成 Harness 全仓设计评审，优化计划：`docs/plans/2026-08-11-harness-optimization-plan.md`（O1 正确性修复、O2 编排器链路、O3 B1 前置、O4 打磨），待用户确认后实施。
- 已完成 MVP 后续计划：`docs/plans/2026-08-11-post-mvp-plan.md`（P1 DeepSeek 特别优化、P2 角色扮演特别优化），待用户确认后实施。
- 计划 A 已由另一个模型实施完成，经审查修复后验收通过（验收时提交 `3a48c7f`）。

### 已确认的产品方案

- 使用供应商无关的 `DialogueModel` 与 `CodingEngine` 接口，首个编程后端适配 Codex app-server；以后可以替换其他模型 API，而不改界面消息协议。
- 使用聊天模式与协作模式。聊天模式是全宽角色对话且不调用工具；协作模式是角色区与助手工作台双栏，助手执行期间用户仍可与角色进行角色扮演式聊天。
- 默认发送对象是角色。“对角色说”支持 VAD、按键说话和文字输入；“直接交给助手”仅支持按键说话和文字输入。
- 角色只能讨论、建议、委派和回应结果。普通角色正文、命令文本或 `@板砖` 都不得触发工具；只有结构化任务才能交给助手。
- 用户直接交给助手的任务和角色委派统一形成 `TaskRequest`；用户对运行中任务的直接修改优先于角色建议。
- MVP 在整个应用中最多运行一个编程 turn，事件必须回到发起任务的原聊天。
- 一个项目绑定一个文件夹，一个项目可以保存多个聊天；每个聊天固定一组搭档。支持新建聊天、保存旧聊天、恢复消息和最终工具卡片。
- 另设无文件夹的日常聊天区，只允许角色聊天，不创建编程会话。
- 工具调用、命令、文件路径、代码、运行日志、审批和状态提示全部静音；只有角色与助手面向用户的自然语言发言可以进入 TTS。
- 白厄主题使用蓝色组：`#C7D4E3`、`#8AA4D4`、`#3A548C`、`#296CE1`。神秘古代机械使用新铸青铜金：`#B08D57`，亮金可用 `#C5A059`，沉稳色可用 `#8C6B3F`。
- 项目级沙箱为目录级约束：文件读写与命令工作目录限定在项目根内，越界由 Orchestrator 统一拦截；容器不进 MVP。
- 三种审批模式为项目级设置：请求批准（每次工具和命令操作都由用户选择允许、本对话内允许或否决）、帮我审核（低风险放行，命中 `config/risk_rules.yaml` 高风险规则的操作转审查智能体裁决，否决附简短理由和调整建议）、完全允许运行（沙箱外直接操作，工具与审批事件照常持久化）。
- 审批模式在输入区左下角的下拉选择框切换，选择持久化在 `projects.approval_mode`；审批请求在输入区正下方整体展开，点击后自动隐藏；审查智能体裁决以静音的系统卡片展示。
- 审查智能体只读待审批操作与当前聊天最近 3 条消息，不能修改文件、调用执行工具、创建子助手或继续委派，模型配置复用 `DialogueModel` 适配器。
- 不建设自定义 MCP 网关、统一代理层、注册表或工具市场。复杂多 Agent、长期自治、完整治理、容器集群、Electron、`@板砖` 触发和梦境监督器均不进入当前计划。

### 技术与环境确认

- Python：3.11.4。
- PyQt5：5.15.11，Qt 5.15.2。
- `qasync`：0.28.0，已作为 pip 包安装并验证可导入。
- `sounddevice`：0.5.5，已作为 pip 包安装，并检测到默认音频输入与输出设备。
- 真实语音模型固定为 `qwen-audio-3.0-asr-flash-streaming` 与 `qwen-audio-3.0-tts-flash`。
- 项目 `.venv` 已创建并安装全部依赖（含 ui、voice、dev 分组）；后续 Python 命令一律使用 `.\.venv\Scripts\python.exe`。

### 两阶段计划与完成状态

#### 计划 A：当前可以实施的本地测试闭环

- 状态：✅ 已完成并验收，完成度 100%。
- 范围：协议与状态机、白厄单组编排、测试适配器、项目级沙箱与三种审批模式、Codex app-server 离线适配器、PyQt5 双模式界面、SQLite 项目与聊天恢复、麦克风与测试语音闭环。
- 78 个测试全部通过（1 个因当前环境不支持创建符号链接而跳过），CLI 演示与界面演示通过，全部测试使用可预测的测试适配器，不调用真实模型，不执行真实文件修改。
- 已知计划内遗留：审查智能体否决后的“改方案重试”要等真实模型才有意义；Codex workspace-write 沙箱映射在 B1 联调时接入。

#### 计划 B：完整 MVP 闭环

- 状态：未开始，完成度 0%。
- B1 等待可由 Python `subprocess` 启动的 Codex app-server，以及角色对话 API 的 base URL、API Key 和模型名。
- 当前只能在 WindowsApps 中发现 `codex.exe`，终端启动时报“拒绝访问”，不能用于真实联调。
- B2 等待 DashScope API 配置，以及白厄和神秘古代机械的参考语音或已有 `voice_id`。
- B3 在前两步完成后扩展流萤、萨姆、三月七和第四面镜，并需要另外四套参考语音或 `voice_id`。

### 后续恢复开发时的入口

1. 计划 A 已完成，不要重复实施 A1–A7；任何改动前先用 `.\.venv\Scripts\python.exe -m pytest -q` 确认基线全绿。
2. B1 启动前等待用户提供：可由 Python `subprocess` 启动的 Codex app-server、角色对话 API 的 base URL、API Key 和模型名。
3. B2 启动前等待 DashScope API 配置，以及白厄和神秘古代机械的参考语音或 `voice_id`。
4. B3 在 B1、B2 完成后扩展流萤、萨姆、三月七和第四面镜。
5. 提交时只添加本次涉及的文件，不使用 `git add -A`；阶段性工作完成后推送到 `origin main`。
