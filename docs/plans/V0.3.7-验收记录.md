# V0.3.7 验收记录（§6.1 矩阵台账）

> 状态基准日：2026-09-03。逻辑轨分支 `v0.3.7-logic-ai`（最新提交见 `git log`）；本文件随验收推进持续更新，全部真实失败如实保留。
> 类别对照（计划 §6.4）：代码实现 / 离线测试 / 真实环境联调 / 真机验收 / 视觉验收（视觉轨）/ 打包结果。

## 12 项矩阵状态

| # | 验收项 | 状态 | 证据 / 阻塞 |
| --- | --- | --- | --- |
| 1 | 白厄 JSON 完整链路 | ◐ 离线面通过，对话面待额度 | 导入→装配→导出→再导入经真实 Sidecar 命令级走查通过（`../character-card/V0.3.7-ST交换联调记录.md` §1）；世界书/高级模块进角色提示词由装配诊断与单元测试覆盖（pytest）；"运行"指真实模型消费提示词的表现，待额度走查 |
| 2 | v3 PNG 单文件双向 | ✅ 通过 | 与本机真实 SillyTavern 双向交换各一次真实记录 + 证据 SHA-256（`../character-card/V0.3.7-ST交换联调记录.md` §2-3） |
| 3 | 世界书激活生效 | ◐ 装配级通过，真实对话面待额度 | 命中条目进提示词、"不提不进"、预算诊断由激活引擎与装配测试锁定（真实白厄 fixture）；真实对话中的端到端表现待额度 |
| 4 | depth_prompt 注入 | ◐ 同上 | splice 语义锁定测试（对齐 ST doChatInject）+ depth 注入单元测试；真实对话面待额度 |
| 5 | 数据宏 | ◐ 同上 | `{{char}}`/`{{user}}` 展开、非白名单保留+清单由宏模块测试锁定；真实对话面待额度 |
| 6 | 确定性触发 | ◐ 同上 | `runtime_trigger` 第 N 回合命中/其他回合不出现/once 语义/time kind 不执行由测试锁定；真实对话面待额度 |
| 7 | 助手边界 | ✅ 通过 | `assert_single_assistant_markdown` 守卫零改动零回归；助手侧消息构建路径零改动；双端静音 `is_tts_eligible` 既有测试；导入卡无工具授予路径（协议层无新增） |
| 8 | 存而不运行 | ✅ 通过 | 导入 CompatReport 真实呈现 13 项×20 条目清单（概率/递归/互斥/时效等）；运行时激活引擎逐条排除并记录；codec 幂等测试 |
| 9 | Android 壳真机 | ⏸ 阻塞：工具链 | 本机无 ANDROID_HOME/NDK_HOME/JAVA_HOME（仅 JDK 11）；契约 §9.3 清单 + `desktop/scripts/android-spike.ps1` 校验已就绪（失败路径实测）；配置后执行 spike 回写契约 §9.4 |
| 10 | 后台通知 | ⏸ 阻塞：同 #9 | 通知插件选型已定案（tauri-plugin-notification）、壳架构已定案（契约 §9.1/§9.2）、main.rs cfg 守卫已就位 |
| 11 | 电源提示 | ◐ 读取与事件面通过，翻转实测待用户 | 真实 powercfg 读取（平衡/AC1800s/DC120s）、at_risk 判定、事件节奏（60s 轮询、变化才 emit、失败不合成）均真实验证；**需用户手动调短睡眠超时**观察提示出现/消失（应用永不修改电源设置） |
| 12 | 固定模型与静音回归 | ✅ 通过 | 固定模型归一化与 voice_policy 既有测试 + 全量回归绿（pytest 789 / Vitest 252+111 / cargo 15+fmt）；导入卡其他模型字段归一化为固定模型（codec） |

## 类别小结

- **代码实现**：完成（13 个提交，L1-L17；文件与提交明细见各波次提交信息）。
- **离线测试**：完成且全绿（pytest 789 passed / desktop Vitest 252 / mobile Vitest 111 / cargo test --locked 15 + fmt --check / 双端 typecheck、build；版本五处 0.3.7）。
- **真实环境联调**：真实 Sidecar 命令级走查 ✅；真实 SillyTavern 双向交换 ✅（`../character-card/V0.3.7-ST交换联调记录.md`）；真实 powercfg 读取 ✅。
- **真机验收**：待 Android 工具链（#9/#10）。
- **视觉验收**：视觉轨职责（`output/playwright/`），双端一致性复查（V13）待视觉轨提交后集成期执行。
- **打包结果**：release EXE 0.3.7 重建 + 首次运行清理 + 打包 sidecar 冒烟 ✅（build-sidecar.ps1 缺陷根治）；NSIS 按节奏留待 V0.4.0。

## 待用户资源清单（收口前置）

1. Android 工具链（JDK 17 + SDK/NDK + 环境变量）→ spike 回写契约 §9.4 → 真机验收 #9/#10。
2. DashScope 额度授权 → 矩阵 #1/#3/#4/#5/#6 对话面走查。
3. 手动调短/恢复 Windows 睡眠超时 → 矩阵 #11 翻转实测。
