# V0.3.9 视觉团队台账

> 维护人：视觉主管 KIMI-K3-256K（本会话）。索引由逻辑主管统一维护，本台账不写入 docs/plans/README.md。
> 边界：真机验证、安装、真实设备操作前停止；不进入 S4-S8；不推送/PR/合并 main；不以 mock 作为真实通过证据。

## 基线

- 主目录：E:\AI\HSR Partner Harness，main @ 072d17e（origin/main 同步）。开工时点 git worktree list 仅见主目录；此后逻辑 worktree 已由 PM 确认存在。
- 保护对象：AGENTS.md 与 docs/plans/README.md 存在用户本地改动，docs/plans/V0.3.9-双轨实施计划.md 为未跟踪文件；视觉轨一律不触碰。
- 逻辑 worktree（PM 已核实）：E:/AI/HSR-Partner-Harness-v0.3.9-logic，冻结 SHA 未出，视觉暂不建分支。
- 视觉 worktree：待 <contract-sha> 后执行 git worktree add -b feat/v0.3.9-visual ../HSR-Partner-Harness-v0.3.9-visual <contract-sha>。

## 团队

| 角色 | 代理 ID | 模型 | 努力级别 | 任务 | 状态 |
| --- | --- | --- | --- | --- | --- |
| 视觉主管 | 本会话 | kimi-coding/k3-256k | high（PM 指定） | V01-V08 决策、审查、台账 | 进行中 |
| 工程师 V-A | aa3d8adf-7867-4403-86ce-52a2ec8b9d26 | deepseek-official/deepseek-v4.1-flash-expires-on-0910 | high | S0 桌面差距盘点（V01/V02/V03/V04/V05/V07/V08 桌面部分） | 进行中 |
| 工程师 V-B | e5c833c4-63a4-48bc-b315-b7ae8a90a26a | deepseek-official/deepseek-v4.1-flash-expires-on-0910 | high | S0 移动差距盘点（V01/V02/V05/V06/V07/V08 移动部分） | 进行中 |

## 文件归属（视觉轨，S0 阶段粗分，S1 后冻结到具体文件）

- 视觉轨目录：desktop/src/ui/、desktop/mobile/src/components/、desktop/mobile/src/pages/ 中分派组件与样式。
- 逻辑轨独占（视觉只读）：stores（desktopStore/mobileStore）、contracts/protocol.ts、backend、播放状态机（voicePlayback.ts、voice_runtime）。
- 设计系统：desktop/src/styles/tokens.css 语义令牌（dark 主题、配对主题变量），各功能目录就近 CSS；移动端 desktop/mobile/src/styles.css + 组件就近 CSS。

## 产品语义（PM 转达，S1 冻结依据）

- 记忆：同项目完整配对内可跨聊天共享；聊天摘要仅本聊天。
- 诊断：显式打开/关闭，普通聊天不展示隐藏提示；UI 无数据不得显示伪造零指标（区分无数据与零值）。
- 已有列表虚拟化优先复用，不重写。

## S0 结论摘要（两名工程师报告已齐，全文见各自会话）

桌面（V-A）：V01 最大缺口是消息级终态零渲染（Message.status 在 desktop/src/ui/ 零引用）、未读/完成反馈为零、无跨聊天任务总览（activeTaskCount 已算未用）、ApprovalBar 只渲染 pending[0] 且审批为全局列表会串聊天；V02/V03 桌面零实现（协议无压缩/记忆/指标字段，后端装配诊断无出口）；V04 无路由、工作室/导入导出/远程藏二级、无回到运行中工作入口；V05 已有消息流 >50 与角色库 >32 两处虚拟化可复用，聊天栏全量、工作台未虚拟化、世界书 key={index}；V07 VoiceMiniPlayer onRetry 死代码、speaker/summary 恒空、resolvedApprovals 未渲染、decision 命名漂移；V08 仅一档 1680px 断点、960×640 最小窗口无证据。

移动（V-B）：V01 列表页无徽章、activeTask 单会话口径（快照全量 active_tasks 被丢弃）、mobileStore 不消费 turn.*、无未读数据源；V02 协议层无压缩/记忆/指标类型；V05 虚拟化存在但流式 delta 触发全量 measure()、无分页、无负载实测；V06 租约/设备零 UI、ping 与 claim/release_control 不在 TS 命令类型中却被调用、休眠与断网不可区分；V07 抢占反馈零、PCM 溢出只丢分片无 UI、审批 timeout 无映射、审批 resolve 错误被吞（ChatPage.tsx:177-191，违反 Let It Fail）；V08 仅聊天页一处 360px 媒体查询、safe-bottom 疑似重复留白、软键盘无处理、无自动化测试。

需 S1/产品拍板：未读口径与权威来源；无数据 vs 零值表示法；压缩/记忆反馈打扰级别与诊断入口；租约/设备移动形态与休眠是否区分；通知点击归属验收口径；性能预算与数据集；分页/增量协议；useChatTimeline.ts 等 pages/ 下派生 hook 归属；Composer/PowerPrompt/usePowerStatus 直连 store 与 presenters 反向 import 视觉类型的归属冲突；虚拟化阈值三处不一致（>40/>50/>32）。

硬矛盾：store:948 resolved_by 默认 "desktop" 违反不伪造来源；计划把 ConnectionBanner/PowerStatusBanner 与桌面 PowerPrompt 并列但前两者仅在移动端。

## 进度记录

- S0：完成。两名工程师只读盘点报告已齐并汇总上报 PM；全程零修改、零构建、零真机。
- S1：等待契约冻结提交 SHA 与逻辑分支路径（PM 转达）。
- S2：未开始。
