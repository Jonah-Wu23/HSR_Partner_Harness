# A07 现场说明

真实 Reasonix 委派全链路（4 轮工具 + 4 次审批 + 确定性落盘），
`db-delegation.txt` 为该会话消息全量导出，`01-delegation-completed.png` 为委派完成后的界面，
`02-approval-pending.png` 为审批栏（含命令原文与嵌套执行提示）。
协议侧同类委派见 `../A01/a01-run.json` 与 `../A08/a08-run.json`。
