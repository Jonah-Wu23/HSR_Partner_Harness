# V0.3.9 验收记录（S4 桌面真机批次）

> 依据《V0.3.9-真机验证补充测试.md》执行与记录；状态仅四种：通过、失败、受阻、不适用。
> 逐项证据、原始日志与协议帧在 `E:\AI\HSR-Partner-Harness-v0.3.9-logic\evidence\batch-2026-09-10\` 对应目录。
> 本文件只记录实际执行结果；未执行项不写通过，也不以离线夹具或历史批次结果替代真实链路证据。**唯一例外是 2026-09-11 裁决**：Android 浏览器列按等效认定改判、`V039-R2-001`/`V039-R2-002` 以离线修复加离线验证收口，两处均已显式标注性质与边界，见 §28–§30。

## 1. 批次信息

| 项目 | 内容 |
| --- | --- |
| 批次 | `batch-2026-09-10`（桌面/非移动专项；A05 与移动端 A10/A11 及移动矩阵不在范围） |
| 时间段 | 2026-09-10 12:54 – 19:00 +0800（含真实休眠 4 分 53 秒） |
| 候选提交 / 契约 | `feat/v0.3.9-logic` @ `897193a2fb983cd39c8328e98f7e225963a1ee64` / contract-v1（`5dae701`+`effa4f3`） |
| EXE / Sidecar | `desktop\src-tauri\target\release\hsr-partner-harness.exe`（11,200,512 B，02:08 +0800）；同构建 `resources\sidecar\pair-harness-sidecar\pair-harness-sidecar.exe`；SHA256 见 `batch-manifest.json` |
| 设备 | Windows 11 10.0.26200；AMD Ryzen 7 7840H；2560×1440 / 缩放 150%；音频设备 5 个（Realtek / NVIDIA×2 / AMD×2） |
| 网络 | 本机直连公网；局域网 `sidecar --serve 0.0.0.0:8765`（PWA 静态 + `/ws`）；Tailscale 未配置、未执行 |
| 真实服务 | 对话 DeepSeek `deepseek-v4-flash`；委派 Reasonix（ACP）；语音 DashScope `qwen-audio-3.0-asr-flash-streaming` / `qwen-audio-3.0-tts-flash`（北京专属域）。密钥不记录 |
| 协议通道 | 候选 `GET /ws`，token 由桌面端「远程设备 → 生成配对码」（本次 659304）经 `remote.pair` 换取；逐帧时间戳落 `ws-frames*.log`，token 只记 SHA256 前 16 位 |
| 证据根 | `evidence/batch-2026-09-10/`（每用例含 `result.json`、帧日志、事件流、sidecar 摘录、`commands.ps1`、`http.log`、`notes.md`） |
| 夹具 | `fixtures/fixtures-manifest.json`：fix-a（402,315 B 单条）、fix-b（4 套 × 80 条 3,655–7,675 B，单套 404,377 B）、fix-b2（80 条 637–1,573 B，合计 77,246 B）、fix-c（v3 卡承载 85 条世界书，正文 129,992 B）；源语料 `E:\Tavern\崩铁-匹诺康尼補充版.json`（402,315 B，SHA256 `b5846ccd…629`） |

## 2. 逐项结果

| ID | 状态 | 实际结果（要点） | 证据 |
| --- | --- | --- | --- |
| A01 并发/排队/取消/审批归属 | **通过** | 四会话协议级并发提交，发送时刻最大差值 **0.0 s**（<1 s）；四会话消息与终态归属零串线；同会话 0.187 s 间隔的第二条进入 `queue_items`（queued/position=0）并按序派发；协作会话在 approval_id=5 上 `task.cancel`，取消后 active_task 归零、该会话待审批从快照移除 | `A01/` |
| A02 压缩触发与摘要 | **失败** | 两条阈值均真实触发并由真实模型生成摘要：字节路径（单条 402,315 B）`covers_message_count=1`、条数路径（80 条 1KB）`covers_message_count=80`，`status=completed`，摘要正文为模型对该语料的概括；原文永久保存；最近 12 条窗口由 `ROLE_CONTEXT_LIMIT_AFTER_SUMMARY=12` 实现。**但 `summary.get` 无法读回**（空摘要会话正常返回 `[]`，一旦存在摘要记录即 30 秒超时）→ V039-S4-001 | `A02/` |
| A03 长期记忆 | **失败** | 入口侦查四面完成：`upsert_memory` 全库仅定义无调用（无写入路径）、桌面端 UI 与 src-tauri 均无相关入口；协议侧正确（五分量必填、未知 id 报 `memory_not_found`、作用域解析按会话权威）。写入缺失 + 缺入口 → 按 §7 判失败 → V039-S4-003 | `A03/` |
| A04 后台运行与重启 | **通过** | 委派在后台持续运行，期间切换导航/设置/诊断正常；重启后最后活跃会话与完整时间线（4 条审批结果、委派终态、角色终局回复、工作台工具记录）正确恢复 | `A04/` |
| A06 指标/装配诊断/失败注入 | **失败** | 隐藏原文规则通过（普通请求 5 模块 hidden 全 null；`include_hidden=true` 返回 5 段 14,771 字符且不在对话流内）。指标读取整条失败（V039-S4-001），失败与「查询返回 0 条指标记录」并列上屏；`dialogue.base_url` 写入被静默改写而返回成功、`test_connection` 对未落库端点报「连接正常」（V039-S4-014）；失败回合提示为「本次回复失败：」空原因（V039-S4-015）；断网注入受阻（B-01） | `A06/` |
| A07 真实委派与多轮工具 | **通过** | Reasonix 全链路真实：角色自然语言 → 结构化委派 → 4 轮工具（列目录/存在性与编码检查/写文件/回读校验）→ 4 次真实审批 → `acceptance-note.md` 确定性落盘（29 B，UTF-8 无 BOM）→ 助手结果摘要 → 角色终局回复；事件与审批可在工作台与落库逐条追踪。Codex 事件子项受阻（B-03）；V039-S4-009 待确认 | `A07/` |
| A08 世界书/装配诊断/语音 | **失败** | 装配诊断在绑定卡会话返回 5 个世界书模块，隐藏原文需显式请求；**真实语音输入通过**（系统 TTS 合成 16 kHz WAV → 42 分片 131,680 B → ASR 逐字命中「请把项目根目录里的文件念给我听。」）；固定模型标识只读；`tts_eligible` 边界成立（用户 false / 角色 true）。**失败项**：装配预算被超出约 10 倍（`budget_total=2048` / `budget_used=21552` / `warnings` 空，世界书模块实际占约 14,400 字符）→ V039-S4-017。**受阻子项**：混合时间线取到 4 类（用户消息/角色发言/助手自然语言/系统审批，`tool_runs` 为空、未产生思考条目），原因是夹具卡不具备真实委派配置（B-05）；语音输出受上游 `Throttling.RateQuota` 与用户成本约束（B-02）。`voice.preview` 被拒属缺陷 V039-S4-016，不计入受阻 | `A08/` |
| A09 语音抢占 | **失败** | 真实长回复（520 字）触发真实播放（`tts=playing`）；播放中桌面发送抢占后 3 秒 `tts=synthesizing`、8 秒后 `idle`，未观察到迟到分片复活播放；队列长度归零；partial delta 未直接入队朗读。**失败项**：原定用 `voice.preview` 构造 60–120 秒长播放的路径不可用——持权远程端调用被 `remote_playback_active` 拒绝，提示与实际状态矛盾；源码核对确认守卫不比较调用方与租约持有者，控制期内不存在任何可用的起始播放路径 → V039-S4-016。**受阻子项**：语音输出受上游限流（B-02） | `A09/` |
| A12 审批并发/重放/真实超时 | **通过** | 同一审批从两条连接同毫秒发出 allow/deny：allow 生效（`resolved_by=remote`），deny 收到 `approval_already_resolved` 且 `details` 携带生效方真实终态；重放同一动作再被拒（幂等，副作用一次）；生效后待审批从快照移除。真实超时：approval_id=6 锚定 `10:14:07.176Z` → `10:24:07.170Z` 收到 `approval.resolved`，`decision=timeout`、`resolved_by=system`、**实测等待 600.0 秒**；迟到裁决被拒 | `A12/` |
| A13 角色库/导入导出/长字段 | **通过** | 导入 fix-c 成功（CompatReport 六段齐全）；`card.get` 返回 85 条世界书、正文 129,992 字节；导出文件 187,536 B 且条目数一致；长字段写入 242,332 字符后回读逐字符一致且世界书不受影响；重复导入生成副本卡；`card.list` 5 张卡 | `A13/` |
| A14 鉴权/断链/休眠恢复 | **通过** | 错误 token → `invalid_token`；缺失 token → `missing_token`；撤销设备后已建立连接收到关闭帧、同 token 请求 → `revoked_token`；在途断链（先确认请求已达服务端再切断）发起方收不到响应而服务端照常完成回合，重连后标记恰好 1 条、新 turn 成功；**真实 Modern Standby 4 分 53 秒**（Kernel-Power 506 18:54:30 → 507 18:59:23）后进程未变、无残留任务/审批、新 turn 完成 | `A14/` |

范围外：A05（统一负载与长列表）、A10/A11 与移动平台矩阵——按补充测试 §6 不在本批次范围。其中 **A05 依 2026-09-10 产品级决策记为「不适用（产品决策：永久不再测试）」**，见 §6；A10/A11 与移动平台矩阵仍按移动端单独安排，不得用桌面结果替代，也不据此宣称通过。

## 3. 失败与受阻统计

判定口径：状态按用例的**主断言**判定；被测能力本身不符合契约或应有能力缺失即为失败，环境/服务缺失才记受阻。因此「受阻」只落在确实缺设备或服务的子项上，不用于覆盖产品缺陷。

- 通过 6：A01、A04、A07、A12、A13、A14（A07 原含 Codex 事件受阻子项，该子项已按 §5 的产品决策取消）。
- 失败 5：A02（摘要读回）、A03（无写入路径且无入口）、A06（指标读取失败 + 配置静默改写 + 失败原因缺失）、A08（世界书装配预算超约 10 倍）、A09（控制期内无可用起始播放路径，`voice.preview` 被守卫误拒）。
- 受阻子项 4（无整例受阻）：语音输出（A08/A09，B-02 上游限流 + 用户成本约束）、Codex 事件（A07，B-03 已按产品决策取消，转范围变更）、断网注入（A06，B-01 已裁定改用简易沙盒）、混合时间线的工具与思考条目（A08，B-05 已裁定按替代方案执行）。
- 不适用 1：A05（产品决策：永久不再测试）。
- 问题登记 19 条（V039-S4-001 – 019），全部按补充测试 §10 格式写入 `V0.3.9-问题与修复台账.md`，「修改方式」字段为固定句原样。其中 V039-S4-016（播放控制守卫不识别调用方）、V039-S4-017（世界书预算不约束注入）、V039-S4-018（抢占被上报为合成失败）为产品缺陷，不计入受阻；V039-S4-019 为待确认观察项。

## 2.1 语音侧固化数据（B-02，不再复测）

时间窗 2026-09-10T10:24:09Z – 10:29:11Z。合成尝试 40 次：播完 36 次，失败 4 次——其中 DashScope `Throttling.RateQuota` 2 次（10:28:42.681Z、10:28:51.000Z），「TTS 消息不存在」误报 2 次（10:26:22.970Z、10:28:36.588Z，即 V039-S4-018）。**首次限流前播完 34 条**；限流为间歇性而非硬墙，限流后仍有 2 条播完。结论：40 次合成里只有 2 次是真正的供应商限流，因此「语音输出受阻」不能整体归因于限流，需先修 V039-S4-018 才能得到干净的限流基线。依产品决策，语音合成条数与限流阈值不再复测。

## 4. 过程纪律与如实说明

1. **S4 只测不修**：未修改候选代码、前端资源、Sidecar 与数据库结构；唯一环境改动是把开发机 `.env` 复制为 `%LOCALAPPDATA%\PairHarness\.env` 以获得真实模式（该步骤本身即 V039-S4-002 的证据），以及 A06 错误注入后按原始值恢复账号配置并核对。
2. **测试脚本缺陷与修正**：首轮 A01 存在「响应先到未认领即丢弃」的假超时 → 修复后复测；A14 首轮在途切断等待窗口过长未构成在途 → 改为先确认落库再断链后复测；A12 首轮等待谓词未读 `payload` 导致空等 → 修正后得到 600.0 秒实测；A06 首轮误把注入落在验收账号（独立账号登录参数错误）→ 结束后恢复并核对。
3. **固定提交与资源**：批次内核对 EXE/Sidecar/dist 哈希（`batch-manifest.json`），未替换资源、未重新构建。
4. **成本与额度**：真实语音合成消耗了 DashScope 额度；用户明确要求停止对 393KB 相关回合进行合成后，账号级 `voice.enabled` 已置 false，后续用例不再产生合成费用。
5. **已完成用例的规范化**：A04/A07 与 A00 真实链路对照、F-01 演示模式取证属 GUI 批次已完成项，本批次只做交付物规范化与证据归位，未重复执行。

## 5. 签收状态

不满足最终签收条件中的全项通过要求：存在 5 个失败用例，另有 4 个受阻子项，且移动矩阵不在范围（A05 已按产品决策记不适用）。
下一步按 `docs/plans/V0.3.9-复测计划.md` 执行复测批次；语音侧按产品决策不再复测合成条数，只复验守卫与抢占语义。
本记录不解除 V0.4.0 的验收要求。

## 6. 产品级决策落点（2026-09-10）

| 决策 | 内容 | 落点 |
| --- | --- | --- |
| B-01 | 断网类注入改用简易沙盒 | `blockers.md` B-01；复测计划 §4（首选进程级网络沙盒，备选 WSL2，兜底提升会话；本机 Windows 沙盒不可用） |
| B-02 | 播放数量已足够，不再重测，固化现有结果 | 本文件 §2.1；`blockers.md` B-02；复测计划 §7 |
| B-03 | 以最小成本彻底剥离 Codex 要求，只支持 OpenAI Chat Completions 兼容端点 | `blockers.md` B-03 转范围变更；复测计划 §5（原 A07 Codex 事件子项取消） |
| B-04 | A05 标注为永久不再测试 | 本文件 §2 范围外与 §3 不适用；`blockers.md` B-04；复测计划 §8 |
| B-05 | A08 混合时间线按替代方案执行 | `blockers.md` B-05；复测计划 §6 |

原有 4 个受阻子项中，B-01、B-03、B-05 已由上述决策给出明确路径，B-02 已固化为不再复测；因此本批次的受阻子项不再停留在「等环境」状态。

---

# S4-R1 复测批次（2026-09-10，桌面端）

> 依据《V0.3.9-复测计划.md》执行。状态仅四种：通过、失败、受阻、不适用。
> 逐项证据在 `E:\AI\HSR-Partner-Harness-v0.3.9-logic\evidence\retest-20260910\` 对应目录。
> 移动端按用户指示本阶段不执行，不得用本次结果替代。

## 7. 批次信息

| 项目 | 内容 |
| --- | --- |
| 批次 | `retest-20260910`（桌面端复测；A05 不适用，移动端 A10/A11 与移动矩阵不在范围） |
| 时间段 | 2026-09-10 20:50 – 21:35 +0800 |
| 候选 | **工作树快照（未提交 git）**：`HEAD=cc4077c` + 工作树未提交修复，`worktree_diff_sha256=ddee191b…012`（385,038 B）；产物 SHA256 见 `batch-manifest.json` |
| 契约 | contract-v1（`5dae701` + `effa4f3`） |
| EXE / Sidecar | `desktop\src-tauri\target\release\hsr-partner-harness.exe`（11,227,136 B，20:54 重建）；`resources\sidecar\pair-harness-sidecar\pair-harness-sidecar.exe`（11,308,823 B，20:52 重建） |
| 设备 / 网络 | Windows 11 10.0.26200 / 2560×1440 / 缩放 150%；本机直连公网；B-01 期间经本机 HTTP CONNECT 沙盒（`127.0.0.1:8767`）出站 |
| 真实服务 | 对话 DeepSeek `deepseek-v4-flash`（Chat Completions）；委派 Reasonix（ACP）；语音 DashScope `qwen-audio-3.0-tts-flash`（北京专属域）。密钥不记录 |
| 日志前提 | Sidecar 全程以 `PAIR_HARNESS_LOG_LEVEL=INFO` 启动，stderr 全量落盘（不过滤），逐用例另存 `sidecar-info.log` |
| 离线门禁（开测前） | 根 `pytest -q` **1074 passed, 4 skipped**；`npm test -- --run` 桌面 470 / 移动 336 passed；`typecheck`、`build` 全部 exit 0；`cargo test --locked` 23 passed（日志 `.tmp/r1-pytest.log` 等） |
| 数据起点 | 首次运行清理后重建（`%LOCALAPPDATA%\PairHarness` 为空），全部会话/项目/记忆为本批次新建 |

## 8. 逐项结果

| ID | 状态 | 实际结果（要点） | 证据 |
| --- | --- | --- | --- |
| V039-S4-001 协议序列化 | **通过** | `metrics.query` 读回 30 条记录、`summary.get` 读回 1 条 completed 摘要、`memory.list` 读回 2 条，时间戳全部为 ISO 8601 文本；空会话 `summary.get` 正常返回 `[]`。**上一批次「一旦存在摘要记录即 30 秒超时」未复现**。摘要由真实 80 条压缩触发（`covers_message_count=80`），GUI 诊断面板同时显示 50 条回合指标（上一批次此处整条读取失败） | `V039-S4-001/`（含 `metrics-panel-50-records.png`） |
| V039-S4-002 首次运行模式 | **通过** | 不带模式标志启动：`demo=False / mode_source=default_real`；`backend.ready` 帧同源。`--demo` 启动：`demo=True / explicit_demo`，顶部出现「演示模式：未调用真实模型」徽章，`config.test_connection` 如实返回 `ok=false` 并说明演示模式不生效（**不再给出「连接正常」**） | `V039-S4-002/`（含 `demo-mode-badge.png`、`sidecar-boot-default-real.log`） |
| V039-S4-003 长期记忆 | **通过** | `memory.create` 落库 + `memory.updated` 事件；同内容重复写入幂等（同 `memory_id`）；同 pair 跨聊天共享、不同 pair 隔离（读 0 条）；`update`/`delete` 实际生效；非法输入如实拒绝（`memory_invalid` ×3、`conversation_not_found`）。设置页「长期记忆」入口存在，列出五分量作用域与增删改控件 | `V039-S4-003/`（含 `settings-memory-page.png`） |
| V039-S4-014 配置结论归属 | **通过** | 端点/引擎不兼容的写入回**结构化 `config_rejected` 且保留底层报文**（修复前落成 `internal_error`）；`provider/base_url` 不一致被 `provider_endpoint_mismatch` 拒绝；两次拒绝后配置**未被半写入**；`test_connection` 一律回传实际探测的 `provider/base_url/model`，对错误 Key 如实返回 `ok=false` + 真实 401 报文 | `V039-S4-014/` |
| V039-S4-015 失败原因 | **通过** | 错误 Key 回合的可见提示为「本次回复失败：Client error '401 Authorization Required' for url …」，**不再是空壳「本次回复失败：」**；用户消息标记 failed；未过滤日志保留 httpx 401 记录。本轮命中「异常自述非空」路径，`_failure_reason` 的为空回落分支未在真实链路构造（仅离线覆盖） | `V039-S4-015/` |
| V039-S4-016 播放守卫 | **通过** | 三个共享守卫的方法逐一核对：无租约 → 桌面/远程全部放行；**租约期内持权端全部放行**（修复核心，修复前被拒）；另一台远程设备全部被拒且文案点名「另一台远程设备正在控制语音」；释放后恢复放行。持权端真实试听成功（`tts=playing`，1 条合成） | `V039-S4-016/` |
| V039-S4-017 装配预算 | **通过** | 字段收敛为 `budget_constant_used` / `budget_prunable_used` / `budget_limit_reached`（无 reconciliation / separator）；`prunable_used(0) <= total(2048)`；`limit_reached=false` 与 `overflow_entries` 为空一致；`budget_used` 超限时 **warnings 非空并准确归因**（超出来自免裁剪的 constant 部分）；世界书模块 char 区间与隐藏原文差额恰为小节标题前缀 28 字符 | `V039-S4-017/` |
| V039-S4-018 抢占误报 | **通过** | 真实抢占发生（日志「mobile-tts: 新回复到达，抢占中断旧合成任务」），旧合成已下发 66 分片后**静默收尾**；全程 `voice.mobile_tts_failed` **0 条**；新合成正常完成。真实供应商失败本轮未构造 | `V039-S4-018/` |
| V039-S4-019 空输出归因 | **通过** | INFO 全量日志中 261 行归因链完整可数：不可用告警 82 条（含 `category` 与原始片段）、重试决策 82 条（`category=empty，attempt=0→1，原始输出字符数=114，放宽 response_format=True`）、最终来源 97 条（「首次请求」16 / 「空输出重试第 N 次」81），**可区分重试后成功与直接失败**。「不再重试」路径未构造 | `V039-S4-019/` |
| V039-S4-008 空 text 条目 | **通过（待确认项关闭）** | 导出完整消息载荷后判定：21 条消息中 8 条 `text` 为空的 `assistant.natural_language` 条目**全部携带 `payload.reasoning`**（809 / 214 / 5 / 40 字符等），即 reasoning-only 的思考段，**不是「无内容气泡」** | `V039-S4-008/`、`B-05/messages-full.json` |
| V039-S4-009 ACP 终态 | **通过** | 同一次真实委派回合：`status=failed` 条目 0 条、系统失败提示 0 条、8 条工具记录全部 succeeded、6 次审批全部 allow | `V039-S4-009/` |
| B-05 A08 混合时间线 | **通过** | 绑定 fix-c（85 条世界书）的会话上取得 4 类来源（user/character/assistant/system）、5 类 kind、8 条 `tool_runs`（全部 succeeded）；世界书激活 5 个模块、隐藏原文 14,771 字符；文件真实落盘 66 B 且内容一致 | `B-05/` |
| B-03 仅 Chat Completions 可用 | **失败** | **正向通过**：DeepSeek 端点上真实委派全链路跑通（角色 NL → 结构化委派 → 8 条工具 → 6 次真实审批 → 落盘 66 B → 助手摘要 → 角色终局），终态错误 0 条。**负向三条全部失败**：(1) 仍存在要求 Responses 的校验与提示（`engine_factory.py:110-130`）；(2) 供应商切换仍被 Responses 拒绝（实测 `config_rejected` +「codex 引擎要求 Responses API 后端…」）；(3) 「OpenAI OAuth」入口仍在下拉中可选。**连带效果**：端点指向非 OpenAI 域会让 Sidecar 启动期直接失败退出 | `B-03/`（含 `provider-dropdown-openai-oauth.png`） |
| B-01 A06 断网注入 | **受阻** | 沙盒自检四形态全部可用，候选出站确认经代理。候选侧注入成立：连接被拒 →「All connection attempts failed」；传输中断 →「ReadError」；请求悬挂 → 30.3 s「ReadTimeout」，三种失败原因互不相同、可定位。**名称解析失败形态受阻**：改写 `.env` 端点为保留域后 Sidecar 启动期即因 Responses 校验抛错退出（退出码 2），候选无法进入可用运行态 | `B-01/`（含 `dns-form-blocked.md`、`sandbox-selftest.json`） |
| V039-S4-010 首启清理（额外复核） | **失败** | 打包后 `%LOCALAPPDATA%\PairHarness` 与 WebView 存储已清除，但 **`sidecar.stderr.log` 原样残留**（上一批次 72,790 B）。根因：脚本为 UTF-8 无 BOM + 中文注释，PowerShell 5.1 按 CP936 解码吞掉 2 个换行，注释与 `$logRoot = ...` 合并导致该变量恒为 null，日志删除代码从未执行；失败在打包流程中静默（构建仍 exit 0） | `V039-S4-010/` |

## 9. 通过 / 失败 / 受阻统计

- **通过 12**：V039-S4-001、002、003、008、009、014、015、016、017、018、019、B-05。
- **失败 1**：B-03（正向通过、负向三条未交付）。
- **受阻 1**：B-01 的名称解析失败形态（出站故障类的另外三种形态已成立）。
- **额外复核发现 1**：V039-S4-010 的日志清理未生效（本轮开测前准备时暴露，已按 §10 格式登记）。
- **不适用 1**：A05（依 2026-09-10 产品决策永久不再测试，本批次未列入）。
- **未执行**：移动端 A10/A11 与移动平台矩阵（按用户指示本阶段不执行）。

## 10. 过程纪律与如实说明

1. **只测不修**：未修改候选代码、前端资源、Sidecar 与数据结构；改动只落在证据目录与测试工具（`evidence/retest-20260910/tools/`）。
2. **候选冻结方式**：本轮候选为**工作树快照**而非提交（沿用修复批次的做法，未在验证前提交 git）。冻结依据是 `batch-manifest.json` 中的 EXE / Sidecar / 前端资源 / 夹具 SHA256 与工作树 diff 指纹；批次内未替换或重建任何产物。
3. **开测前手工纠正**：首次运行清理脚本未能清掉上一批次的 `sidecar.stderr.log`，为取得干净的日志起点，执行前手工清除了该文件并留证（见 V039-S4-010）。
4. **环境副作用如实说明**：B-01 的代理注入把**全部**出站引到只转发 `api.deepseek.com` 的沙盒，因此该期间 DashScope 的 WSS 合成必然失败（界面出现「语音合成失败：websocket connection could not established within 5s」）。该失败归因于测试沙盒，**不是产品缺陷**；016/018 的语音复验在无沙盒环境下完成。
5. **真实调用成本**：语音合成实际消耗 5 条（016 用 1 条；018 首次未构造出抢占、重跑一次各 2 条），**超出复测计划 §7「≤3 条」的自我约束**，如实记录。文字侧真实模型调用用于 003/015/016/018/019/001/B-01/B-03/B-05。
6. **触发式回归**：按复测计划 §9，修复批次触及了 A01（队列/取消/审批归属）与 A04（会话持久化）的实证路径，本批次的 B-03/B-05 委派回合覆盖了队列、审批裁决与消息落库的并发路径，未发现回归。
7. **重复采集**：B-01 的注入首轮因会话复用历史消息导致终态判定串台，改为每形态独立会话后重采；010 的根因经「加 BOM / 改 ASCII / 保持原样」三组对照实验确证。

## 11. 签收状态

复测对象中 12 项通过、1 项失败（B-03 负向）、1 项受阻（B-01 名称解析失败形态），另有 1 项额外复核失败（V039-S4-010）。

- **B-03 的失败属于产品决策的开发交付未落地**：决策要求「以最小成本彻底剥离 Codex 要求」，修复批次未完成该交付，导致负向三条全部不成立，并连带阻断了一个错误注入形态。
- **A05 为不适用（产品决策）**，移动端 A10/A11 与移动平台矩阵本阶段未执行，本次结果不得用于宣称其通过。
- 本记录不解除 V0.4.0 的验收要求。

---

# S4-R2 复测批次（2026-09-10，桌面端）

> 依据《V0.3.9-复测计划.md》§14 的开测前置执行；范围是 §14.2 列出的四项待真机复验条目，不含 S4-R1 已通过项。
> 状态仅四种：通过、失败、受阻、不适用。逐项证据在 `evidence/retest-r2-20260910/` 对应目录。

## 12. 批次信息

| 项目 | 内容 |
| --- | --- |
| 批次 | `retest-r2-20260910`（桌面端；A05 不适用，移动端单独成批见 §17） |
| 时间段 | 2026-09-10 22:07 – 23:10 +0800 |
| 候选 | **工作树快照（未提交 git）**：`HEAD=cc4077c`（`cc4077ce019063945e70b1b023291c992d2b37c2`） + S4-R2 未提交修复（`git diff --stat` 末行：74 files changed, 5911 insertions(+), 1895 deletions(-)） |
| 契约 | contract-v1（`5dae701` + `effa4f3`） |
| EXE / Sidecar | `desktop\src-tauri\target\release\hsr-partner-harness.exe`（11,266,048 B，SHA256 `2f58c825…`，22:10 重建）；`target\release\resources\sidecar\pair-harness-sidecar\pair-harness-sidecar.exe`（11,292,973 B，SHA256 `356c8ce5…`，22:07 重建）；随包前端 `resources/mobile-dist/index.html` 与 `desktop/mobile/dist/index.html` 逐字节相同（SHA256 `1eb7cc6a…`）；产物清单 `batch-manifest.json` |
| 构建方式 | `npm run build:sidecar`（exit 0）→ `npm run tauri -- build --no-bundle`（经 `tauri-with-first-run-reset.ps1` 包装，exit 0，日志 `.tmp/r2-sidecar-build.log`、`.tmp/r2-tauri-build.log`） |
| 设备 / 网络 | Windows 11 10.0.26200 / 2560×1440 / 缩放 150%；本机直连公网；出站故障注入经本机 HTTP CONNECT 沙盒（`127.0.0.1:8767`，控制通道 8768） |
| 真实服务 | 对话 DeepSeek `deepseek-v4-flash`（Chat Completions）；委派 Reasonix（ACP）。密钥不记录 |
| 日志前提 | Sidecar 全程以 `PAIR_HARNESS_LOG_LEVEL=INFO` 启动，stderr 全量落盘（不过滤），逐用例另存 `sidecar-info.log` |
| 数据起点 | 打包经首次运行清理（本轮该清理真机成立，见 §13 V039-R1-001）；随后按首启清理的设计清空运行目录，开测前把开发机 `.env` 复制回 `%LOCALAPPDATA%\PairHarness\.env` 以获得真实模式 |

## 13. 逐项结果

| ID | 状态 | 实际结果（要点） | 证据 |
| --- | --- | --- | --- |
| V039-R1-001 打包首启清理 | **通过** | 打包前 `%APPDATA%\com.jonahwu.hsr-partner-harness\sidecar.stderr.log` 为 219,247 B（SHA256 `ae83bbe7…`，上一批次残留）；打包日志出现 `Cleared: …\sidecar.stderr.log`，打包后该文件**不存在**且无 `sidecar.stderr.log*` 滚动副本残留（`before-reset.json` / `after-reset.json`）。**失败传播真机成立**：候选运行中执行 `npm run reset:first-run` → `RESET_EXIT=1`，输出 `tauri-with-first-run-reset: HSR Partner Harness is still running. Close the exe before resetting first-run state.` 并带脚本行号，经 `npm run` 如实传播（`failure-path.log`）。打包本身 exit 0，清理不再静默 | `V039-R1-001/` |
| B-03 负向 (1) 无 Responses 路径与提示 | **通过** | 静态扫描覆盖仓库四个源码根 + 打包产物（`desktop/dist` 全部 js/html、EXE、Sidecar exe）：`_require_responses_backend`、`Responses API 后端`、`codex 引擎要求 Responses` **零命中**；`OpenAI OAuth` 仅出现在「已移除」类说明文本与只读兼容注释中（9 处，全部为告知性文案，无可用路径） | `B-03/b03-run.json` → `part_a` |
| B-03 负向 (2) 供应商切换不再被 Responses 拒绝 | **通过** | 任意 http(s) 端点可进入可用配置态：`config.set {dialogue.provider: openai_compatible, dialogue.base_url: https://127.0.0.1:1/v1}` → `ok=true` 且 `config.get` 回 `provider_supported=true`；显式写旧 `engine=codex` → `invalid_engine`（报文说明只剩 reasonix acp，不再是 Responses 报文）；`dialogue.provider=openai_oauth` → `provider_unavailable`；`provider/base_url` 不一致 → `provider_endpoint_mismatch`。三类拒绝全部结构化可定位且未半写入 | `B-03/b03-run.json` → `part_b` |
| B-03 负向 (3) 不可用 OAuth 入口不再出现 | **通过** | 设置 → 角色对话模型 → 服务商下拉实际展开只有 **DeepSeek** 与 **OpenAI 兼容 API** 两项（截图）；后端 `codex.oauth_start` / `codex.api_login` 一律回 `codex_login_removed`，`codex.oauth_status` 保持只读 | `B-03/provider-dropdown.png`、`b03-run.json` |
| B-03 历史账号形态 | **通过** | 隔离 LOCALAPPDATA 下独立 Sidecar 实例（端口 8766），账号预置 `dialogue.provider=openai_oauth` + `engine=codex`：**启动不退出**（实例存活、`config.get` 正常应答）；启动期发出两条**非致命** `error.reported`（`provider_unavailable`、`engine_removed`，均 `severity=recoverable` / `fatal=false` / `source=sidecar`）；`config.get` 回 `provider_supported=false` + `provider_unavailable`，且**未改写**已保存的 provider/base_url/model；`config.test_connection` 同样如实拒绝；显式改选 `deepseek` 后 `provider_supported=true` 恢复可用 | `B-03/legacy-stdout-frames.json`、`b03-run.json` → `part_c` |
| B-03 正向 真实委派全链路 | **通过** | DeepSeek（Chat Completions）端点：角色自然语言 → 结构化委派 → **10 条 `tool_runs` 全部 succeeded** → **5 次真实审批全部 allow** → 文件 `r2-b03-chain.md` 落盘 57 B 内容一致 → 角色终局回复；时间线含 4 类来源（user/character/assistant/system）、5 类 kind；**终态错误 0 条**（无 failed 消息、无失败提示、无残留 active_task/queue） | `B-03-positive/` |
| B-01 名称解析失败形态 | **通过** | 候选以保留域端点 `https://no-such-host-r2.invalid/v1` **成功进入可用运行态**（`WS 服务器模式已启动`，与 S4-R1 的启动期抛错退出形成对照）；真实回合失败提示为 **`本次回复失败：[Errno 11001] getaddrinfo failed`**，未过滤日志追踪链为 `httpcore\_async\connection.py`（**不含 `http_proxy`**），即失败发生在客户端自身解析阶段 | `B-01/dns-run.json`、`B-01/sidecar-dns.log` |
| B-01 出站四形态（对照） | **通过** | 沙盒自检四形态 + 名称解析形态全部可注入（`sandbox-selftest`）；候选侧四形态失败原因**两两不同且可定位**：连接被拒 → `All connection attempts failed`；传输中断 → `ReadError`；请求悬挂 → 30.3 s `ReadTimeout`；名称解析失败 → `[Errno 11001] getaddrinfo failed`；forward 基线真实回合成功（证明候选确实经代理出站） | `B-01/network-run.json`、`B-01/cut-retry2-run.json` |

## 14. 通过 / 失败 / 受阻统计

- **通过 8**：V039-R1-001、B-03 负向三条、B-03 历史账号形态、B-03 正向、B-01 名称解析失败形态、B-01 出站四形态对照。
- **失败 0**。
- **受阻 0**（S4-R1 的 B-01 名称解析形态受阻在本轮解除）。
- **不适用 1**：A05（依 2026-09-10 产品决策，未列入本轮）。
- **未执行**：S4-R1 已通过的 001/002/003/008/009/014/015/016/017/018/019 与 B-05 依复测计划 §9 未复跑；移动端不在本批次范围（见 §17）。
- **本轮新发现问题 1 条**：`V039-R2-001`（`config.test_connection` 连接层失败的可见原因为空）。

## 15. 过程纪律与如实说明

1. **只测不修**：未修改候选代码、前端资源、Sidecar 与数据结构；新增脚本只落在 `evidence/retest-r2-20260910/tools/`。
2. **候选冻结方式**：工作树快照而非提交（沿用前两批做法），冻结依据是 `batch-manifest.json` 中的 EXE / Sidecar / 前端资源 SHA256 与 `git diff --stat`；批次内未替换或重建任何产物。
3. **环境改动**：首启清理按设计清空 `%LOCALAPPDATA%\PairHarness` 后，开测前把开发机 `.env` 复制回该目录（与 S4-R1 同法）；B-03 历史账号形态使用**隔离的** `LOCALAPPDATA` 与独立 Sidecar 实例（端口 8766），验收账号配置未被该用例改动（`account_before`/`account_after` 均为 `default-local`，`config_restored` 仍为 deepseek）。
4. **测试脚本缺陷两处（如实记录）**：(a) B-01「传输中断」首采用 `SET cut_soon 256`，沙盒 1 秒兜底先于字节阈值触发，客户端落在连接阶段（`ConnectError`），与「连接被拒」不可区分 → 为沙盒加 `--cut-delay` 并以 60 秒启动，使唯一触发条件变为「响应下行 256 字节」，重采得 `ReadError`（`cut-retry2-run.json`）；(b) 首采「固定秒数后 CUT」时 DeepSeek 6 秒内已完成整条回复，CUT 落在回复之后，回合成功（`cut-retry-run.json`），同样不作为传输中断证据。
5. **解析形态首采无效并已作废**：第一次 DNS 注入的追踪链经过 `http_proxy.py`（候选仍带代理），失败落在代理连接阶段而非客户端解析 → 已在启动命令中显式清空 `HTTPS_PROXY`/`HTTP_PROXY`/`ALL_PROXY` 并停掉沙盒后重采，§13 采信的是重采结果。
6. **真实调用成本**：文字侧真实模型调用用于 B-03 正向 1 回合、B-01 五形态 5 回合；本轮**未发起任何语音合成**。
7. **触发式回归**：S4-R2 改动了引擎装配与配置写入校验，B-03 的取证顺带覆盖了 `config.set` 的拒绝路径（三类结构化拒绝 + 显式改选恢复），未发现回归；S4-R1 已通过项按 §9 未重测。

## 16. 签收状态

§14.2 的四项开测前置全部落实并取得真机证据：B-03 负向三条由**失败**改判**通过**、历史账号形态**通过**、B-01 名称解析失败形态由**受阻**改判**通过**、打包后日志清理与失败传播**通过**（V039-R1-001 由**失败**改判**通过**）。
本轮另登记 1 条新问题（`V039-R2-001`，P3）。新候选已提交过构建但**未提交 git**，产物 SHA256 已冻结。
本记录不解除 V0.4.0 的验收要求。

---

# 手机端真机批次（2026-09-10）

> 依据《V0.3.9-真机验证需求.md》§4 移动平台矩阵执行，按用户指示**只做局域网远程连接**，非局域网（HTTPS / Tailscale）不测。
> 本次交付停在验证前不等于验收完成；本批结果不得用于宣称 A10/A11 或未覆盖维度通过。

## 17. 批次信息

| 项目 | 内容 |
| --- | --- |
| 批次 | `mobile-20260910`（Android 壳 + Android 浏览器） |
| 时间段 | 2026-09-10 22:23 – 23:05 +0800 |
| 候选 | 同 `retest-r2-20260910`（工作树快照 @ `cc4077c` + S4-R2 未提交修复） |
| APK | `desktop\src-tauri\gen\android\app\build\outputs\apk\arm64\debug\app-arm64-debug.apk`（249,566,546 B，SHA256 `cb23957c…`，22:13 构建，经 `desktop/scripts/build-android.ps1`）。**已核对不是仅替换 assets**：APK 内 `assets/index.html` 与 `desktop/mobile/dist/index.html` 逐字节相同（`1eb7cc6a…`），`lib/arm64-v8a/libhsr_partner_harness_lib.so`（120,382,200 B）SHA256 与 `target/aarch64-linux-android/debug/` 当次 cargo 产物一致（`8542693d…`） |
| 设备 | Xiaomi `24129PN74C`（代号 dada），Android 16 / SDK 36 / arm64-v8a；物理 1200×2670 / 520 dpi → 逻辑宽约 **369 dp**（另以 `wm density 560` 模拟 **342.9 dp** 后已还原） |
| 网络 | 局域网 `10.81.0.0/16`：手机 `10.81.140.245` ↔ PC `10.81.102.166:8765`（**HTTP，非 HTTPS**，浏览器地址栏显示不安全标记）。**非局域网远程连接按用户指示不测** |
| 连接方式 | 无线 ADB（`adb pair` + `adb connect`，Android 11+ 无线调试） |
| 桌面侧 | Sidecar 以 `--serve 8765` 监听，`lan_host=10.81.102.166`；配对码由桌面端「设置 → 远程设备 → 生成配对码」签发（本项目实现，非系统能力） |
| 证据根 | `evidence/mobile-20260910/`（`shell/`、`browser-20260910/`、`00-precheck/`）与 `evidence/retest-r2-20260910/mobile-security-concurrency/` |

## 18. 平台 × 维度结果

| 维度 | Android 壳（APK） | Android 浏览器（系统默认 Edge，`com.microsoft.emmx`） | iOS Safari / iOS PWA |
| --- | --- | --- | --- |
| 网络（局域网 HTTP） | **通过**：清数据后首启进入「配对桌面端」，手填 `ws://10.81.102.166:8765/ws` + 六位配对码 → 「开始配对」成功（桌面 Sidecar 记 `远程连接鉴权完成 device='我的手机'`，配对码 `claimed=true`），随后显示桌面真实聊天列表（`r2-chain-project` 11 个聊天） | **通过**：浏览器打开 `http://10.81.102.166:8765/`，服务端返回的 `index.html` 与冻结候选的 `mobile/dist/index.html` **逐字节相同**；页面如实提示「当前浏览器不支持原生扫码，请使用下方手动输入配对码」；填入新配对码后 `远程连接鉴权完成` 并显示同一份真实聊天列表 | **受阻**：无设备可用，未执行 |
| 网络（HTTPS / Tailscale / 断网重连 / PC 休眠恢复） | **未执行**（用户指示非局域网远程连接不测；PC 休眠类已在桌面批次 A14 覆盖，不替代移动端） | **通过（等效认定，非独立实测；见 §28）** | **受阻**：无设备 |
| 并发 | **通过**：手机在线期间以「移动连接（局域网地址）+ 桌面连接（回环）」双客户端对 **4 个会话并发提交**，发送时刻最大差值 **0.0 s**；四会话各自只收到自己的回复（`收到。`），零串线，全部正常收敛（权威落库见 `final-state-db.json`） | **通过（等效认定，非独立实测；见 §28）**（原注：浏览器仅验证连通与配对，并发维度由壳与协议侧代表） | **受阻**：无设备 |
| 语音（输入/播放/打断/挂起恢复/溢出） | **部分完成**：播放与打断**通过**、语音输入自动检测**通过**、按住说话**失败**（`V039-R2-003`）；挂起恢复与 A11 缓冲溢出未执行——详见 §22 | **通过（等效认定，非独立实测；见 §28）** | **受阻**：无设备 |
| 安全 | **通过**：未鉴权请求（不携带 token）→ `unauthorized / missing_token`；错误 token → `unauthorized / invalid_token`；两类请求携带的 `chat.submit` 均被拒且**未执行任何命令**。撤销：桌面撤销 `我的手机`（撤下 2 个 token）后，已建立连接被主动断开，后续请求 → `revoked_token`，手机侧如实显示「配对已失效或设备已被撤销，请重新配对」+「会话装载失败：revoked_token」+「语音不可用：与桌面端连接已断开」，**未伪装在线**，并提供「重新配对」入口 | **通过（等效认定，非独立实测；见 §28）**（原注：浏览器侧未做安全负向注入） | **受阻**：无设备 |
| 通知与电源 | **通过（有条件）**：未授权时 `POST_NOTIFICATIONS=granted=false`、应用 `importance=NONE`、已投递通知 0 条，应用**如实**显示「尚未获得系统通知权限：未授权时，锁屏与后台收不到审批请求、任务完成等通知」并提供「申请通知权限」；授予权限后应用投递常驻通知「**保持连接中** — 后台维持与桌面端的连接；锁屏期间可收到审批与任务通知」；应用切到后台期间，从桌面提交的回合完成后手机**收到「任务完成 / 「R2-B01-请求悬挂」已完成」通知**，说明后台连接保持有效。前后台切换本身行为正常（回前台后会话与时间线完整） | **通过（等效认定，非独立实测；见 §28）**（原注：页面自身声明浏览器端无系统通知与 Web Push 能力，本批未另行验证） | **受阻**：无设备 |
| 交互与负载 | **通过**：Safe Area（挖孔 140 / 手势条 2618）、软键盘不遮挡并可真实发送、长列表（31 个聊天）滚动稳定、≤360 dp（342.9 dp）布局完整、断网重连自动恢复且无重复消息——详见 §23。**未执行**：弱网、四聊天同时运行的交互负载 | **通过（等效认定，非独立实测；见 §28）** | **受阻**：无设备 |

> **Android 浏览器列中网络（HTTPS / Tailscale / 断网重连 / PC 休眠恢复）及其后共六行原为「未执行」，2026-09-11 按用户裁决改判为通过**，依据是 Android 壳已通过全部已测维度，且此前实测服务端返回的 `index.html` 与冻结候选的 `mobile/dist/index.html` 逐字节相同（本表首行即该实测）。该结论属**等效认定，不是浏览器端的独立实测**；iOS 两行仍为受阻，不得据此宣称 iOS 通过。

## 19. A10 / A11 状态

- **A10（手机关闭、崩溃、弱网与多设备重连）**：**部分覆盖**——多客户端并存、撤销后失联与如实提示、后台保活与通知投递已取得真机证据；**关闭/崩溃/弱网/租约到期回收未执行**。整例**不作通过判定**。2026-09-11 裁决终结全部真机测试，未执行项不再补测，本条判定为最终记录（见 §31）。
- **A11（播放停滞至 PCM 缓冲溢出）**：**未执行**（需移动端语音播放链路，本批未发起语音合成）。2026-09-11 裁决终结全部真机测试，本项不再安排补测，**不宣称通过**（见 §31）。
- 移动平台矩阵中 **iOS 两行受阻（无设备）**，HTTPS/非局域网、移动语音、弱网同样未执行；**不得用本轮结果宣称 A10/A11 或移动矩阵通过**。

## 20. 过程纪律与如实说明

1. **只测不改**：未修改候选代码、前端资源、APK 与数据结构；未替换 APK 内任何资源。
2. **安装路径的真实问题**：按仓库记录的命令构建出的 APK `versionCode=1`，而设备上已有 `tauri android build` 产物的 `versionCode=3007`，`adb install -r` 以 `INSTALL_FAILED_VERSION_DOWNGRADE` 失败 → 已登记 `V039-R2-002`；为继续测试改用 `adb install -r -d -t`（允许降级）安装，并用 §17 的两项哈希核对证明装入的确为本轮候选。
3. **手机侧环境改动（均留证）**：`pm clear` 复位应用数据以复现首次配对流程；`pm grant POST_NOTIFICATIONS` 授予通知权限以验证常驻通知与后台提醒；`wm density 560` 模拟 ≤360 dp 后已 `wm density reset` 还原为 520。测试结束时该设备 token 处于**已撤销**状态（本批安全维度的证据），重新使用需在手机上重新配对。
4. **测试脚本缺陷一处（如实记录）**：`run_mobile_matrix.py` 的收敛循环只检查 `active_task`/`queue_items`，四会话提交后首个轮询即判 settled 并提前退出，`mobile-matrix-run.json` 的 `final` 段因此只含用户消息；真实终态已从落库导出为 `final-state-db.json`（四会话各含一条角色回复）。该缺陷不改变并发结论。
5. **输入方式限制**：ADB `input text` 不支持中文与空格，手机侧文本输入改用 ASCII 片段并分块注入（首次整串注入被 IME 串扰成 `ws1://0.81…`，已清空重填并逐步核对）。
6. **账号与成本**：本批未发起任何语音合成，未消耗 DashScope 额度；真实模型调用用于并发四会话 + 后台通知 1 回合，共 5 回合。

## 21. 签收状态

移动平台矩阵中 **Android 壳**与 **Android 浏览器**的**局域网 HTTP 连通、配对与真实数据展示**已取得真机证据；安全、并发、通知与电源维度在 Android 壳上通过，交互维度部分通过。**iOS 两行受阻（无设备）**，HTTPS/非局域网远程连接、移动端语音、弱网与断网重连、A11 未执行；**A10 仅部分覆盖，A10/A11 与移动矩阵均不作通过判定**。
本轮新登记 1 条移动侧问题（`V039-R2-002`，P3）。本记录不解除 V0.4.0 的验收要求。

> **后记（2026-09-11）**：Android 浏览器列的 6 行已按裁决改判为**通过（等效认定，非独立实测）**（§18 注、§28）；`V039-R2-001` 与 `V039-R2-002` 已由离线修复 + 离线验证收口（§30）。其余表述是 2026-09-10 批次当时的判定，保持原样。

---

# Android 壳语音与交互维度补测（2026-09-10 深夜，接 §17 手机端批次）

> 本批为 §17 手机端批次的续测：补做 **Android 壳**的语音输入（含按住说话与自动检测两条路径）与交互维度（Safe Area、软键盘、长列表、≤360dp、断网重连）。
> 其中「按住说话」在用户手动真机测试中判失败并已登记 `V039-R2-003`；其余项测试侧执行。

## 22. 语音维度结果（Android 壳）

| 子项 | 状态 | 实际结果（要点） | 证据 |
| --- | --- | --- | --- |
| 播放（真实合成） | **通过** | 桌面提交要求长回复的角色消息 → 服务端真实合成并逐片下行：`speech:…:324b7ea3…` 共 **136 片**（base64 4,889,600 字符 ≈ 3.67 MB PCM ≈ 76 s 音频）；手机端消息气泡显示「🔊 **朗读中**」，即客户端确实进入播报态；合成日志为真实 DashScope WSS（`开始合成 / 合成完成`）；全程 `voice.mobile_tts_failed` **0 条** | `retest-r2-20260910/mobile-voice/voice-run.json`、`02-playing.png`、`03-playing-later.png` |
| 打断（抢占） | **通过** | 旧消息合成进行中提交新消息：旧消息分片在 15:08:25.815 后**停止**且**没有** `voice.mobile_tts_end`（合成被中断、无「合成完成」日志），新消息 3 片正常合成并 end；抢占后旧消息气泡由「朗读中」回到「○ 可朗读」，**未复活播放**；全程 `voice.mobile_tts_failed` **0 条**——满足 V039-S4-018 要求的「抢占只产生已中断的正常收尾，不产生失败」 | 同上、`04-after-preempt.png` |
| 语音输入（自动检测） | **通过** | 用户手动实测：打开 →「● 准备中…」→ 说话中「● 聆听中，检测到静音自动停止」→ 停止后回「准备中…」→ 自动关闭并发送。真实转写并发出两条消息（「我的哎呦，知道。」「Hello hello hello hello」），角色按这两条输入正常回复。服务端观测窗口同期只有 TTS 记录、无异常 | `voice-input/user-manual/02~04-*.jpg` |
| 语音输入（按住说话 / PTT） | **失败 → 已修复并通过真机验收**（2026-09-11，见 §25–§27） | 用户实测：长按「按住说话」并说话，**屏幕上始终没有转写**，**抬起手指也不停止采集**；该模式面板**没有任何采集状态反馈**（既无「准备中…」也无「聆听中…」），与自动检测模式形成明显差异。服务端观测窗口内**没有任何 `voice.mobile_ptt_*` 记录**，即该路径未产生可观测的服务端会话。静态核对确认该控件绑的是 `onClick`（无释放路径），而真正绑 `onPointerDown/Up` 的是另一个同名按钮，停止入口只挂在后者上 → `V039-R2-003`（P1） | `voice-input/manual-observation-window.log`、`user-manual/01-ptt-panel-no-status.jpg`、`voice-panel-state.png` |
| 挂起恢复（后台/锁屏） | **未执行** | 本轮未构造；播放期前后台切换只在「通知与电源」维度观察到连接保持有效 | — |
| 缓冲溢出（A11） | **未执行** | 客户端上限为 300 秒音频（≈14.4 MB 未播分片，`TTS_MAX_BUFFERED_PCM_BYTES`），本轮未构造到该量级，**不据此宣称 A11 通过** | — |

## 23. 交互维度结果（Android 壳）

设备基准：1200×2670 / 520 dpi（逻辑宽 369 dp）；系统 insets 实测——挖孔 `displayCutout` top=**140**（x 566–634）、手势导航条 frame top=**2618**。

| 子项 | 状态 | 实际结果（要点） | 证据 |
| --- | --- | --- | --- |
| Safe Area（顶部） | **通过** | 会话页固定头部标题 `top=152`，已让开 140 的挖孔带；列表页滚动时内容按预期从状态栏下方穿过（滚动容器正常行为），未出现被挖孔长期遮挡的关键控件 | `interaction/04-chat-top.png/.xml` |
| Safe Area（底部） | **通过（附一处观察）** | 滚到列表最底端时最深文本节点 `bottom=2518`，距手势条 2618 尚有 100 px，内容未被手势条遮挡。**观察**：会话页发送按钮 bounds `[893,2492][1163,2639]`，其底边 2639 比手势条上沿 2618 多 21 px（约 6 dp），同排输入框底边为 2619；不影响点击（本轮发送成功），仅记录为对齐差异 | `interaction/03-list-very-bottom.png/.xml`、`04-chat-top.xml` |
| 软键盘 | **通过** | 聚焦输入框后 IME 窗口 frame `[0,1626][1200,2670]`，而输入框底边 887、发送按钮底边 910，**均位于键盘上方**，无遮挡；键盘弹出期间页面正常上移（模式切换行从 2161 移到 432）；键盘态下真实输入并发送成功——15:38:35 发出 `keyboard-visible-send-check`，15:38:36 角色真实回复 | `interaction/05-keyboard-up.png/.xml`、`06-keyboard-typed.png`、`07-after-send.png` |
| 长列表 | **通过** | 灌入 25 条会话使项目达 **31 个聊天**，连续上滑/下滑 12+10+16 次：列表滚动稳定、无空白/错位、每条卡片标题与时间可见、`对话` 按钮可点；滚动到两端均正常止停 | `interaction/01-list-top.png`、`02-list-bottom.png`、`03-list-very-bottom.png` |
| ≤360 dp 小屏 | **通过** | `wm density 560` → 逻辑宽 **342.9 dp**（小于 360）：列表页与会话页均完整渲染，标题、审批模式行、模式切换、语音键、输入框与发送键全部在屏内且文本可读，无裁切、无重叠；测毕 `wm density reset` 还原为 520 | `interaction/08-small-343dp-list.png`、`09-small-343dp-chat.png/.xml` |
| 断网重连 | **通过** | 直接停止桌面候选制造断连：手机顶部横幅如实变为「与桌面端**连接中断，正在重连…**」，聊天列表降级为骨架占位（**不展示陈旧数据**）；重启候选后手机**自动重连**（Sidecar 记 `远程连接鉴权完成 device='我的手机'` ×2 与 `remote-control: 控制器已认领`），横幅消失、列表恢复真实数据（31 个聊天），会话 `R2-list-25` 落库仍为 **2 条消息、无重复** | `interaction/10-before-outage.png`、`11-during-outage.png`、`12-after-reconnect.png` |

## 24. 过程纪律与如实说明

1. **注入纪律**：本批起全部手机操作只用 `input tap` / `input swipe`（自带成对的 DOWN+UP），**不再使用裸 `input motionevent DOWN`**——先前正是缺少配对的抬起，在屏幕上留下了一个悬空的按下状态（已由 `motionevent UP` 收掉并留证）。
2. **环境改动**：`wm density 560 → 520` 已还原；为长列表测试新建 25 条会话（`R2-list-01..25`，同一项目 `r2-mobile-project`）；断网重连测试期间停止并重启了桌面候选（重启后状态正常）。
3. **未执行项**：语音的挂起恢复与缓冲溢出（A11）未构造；iOS 两行仍受阻；HTTPS/非局域网远程连接按用户指示不测。**不得用本批结果宣称 A11 或移动矩阵整体通过。**
4. **语音输入结论的适用范围**：自动检测路径通过、按住说话路径失败；用户已确认「短按只捕捉极短内容或什么都抓不到」属产品设计，故该项不作为缺陷，缺陷仅在 PTT 的按压-保持语义与状态反馈上（`V039-R2-003`）。

---

# 移动端「按住说话」PTT 修复批次真机验收（2026-09-11）

> 本批验收 `V039-R2-003`（§22 中判失败的条目）的修复成果。逐项改动、离线门禁与装机完整性核对见《V0.3.9-问题与修复台账.md》「修复批次回写（S4-R3，移动端「按住说话」PTT，2026-09-11）」。

## 25. 候选与设备

| 项目 | 内容 |
| --- | --- |
| 候选 | `desktop/src-tauri/gen/android/app/build/outputs/apk/arm64/debug/app-arm64-debug.apk`，129,303,613 B，2026-09-11 00:37:52，SHA256 `76650e0b3cb3eb7b4300c152feaaf9f0a29960e9fee7906194bf829c25a3bb1e` |
| 构建 | `desktop/scripts/build-android.ps1 -Offline`，exit 0；日志 `.tmp/r3-android-build.log` |
| 装机 | `adb install -r -t` → Success，`lastUpdateTime=2026-09-11 00:39:47`（`-r` 保留应用数据） |
| 设备 / 网络 | Xiaomi 24129PN74C（Android 16 / SDK 36 / arm64-v8a）；局域网 `10.81.0.0/16`，PC `10.81.102.166:8765` |
| 离线门禁 | 移动前端 **351 passed**（测试轨 00:40 独立复跑）、桌面 **475 passed**、`typecheck` exit 0；服务端与协议**零改动** |

## 26. 五条验收路径

| # | 路径 | 结果 | 证据 |
| --- | --- | --- | --- |
| ① | 打开面板**不隐式启动**（服务端无 `ptt_start`） | **通过** | `voicePanelOpen` 为独立状态、打开面板只置位不调采集入口（`ChatPage.tsx:119` / `:807`）；原同名模式分段控件已删除（`grep -c` = 0）；验收实跑无隐式启动 |
| ② | 长按说话 → 实时转写 → **抬起即停即发** | **通过** | 装机后 00:40:54 / 00:41:12 / 00:41:28 在 `R2-list-25` 落库三条**中文**用户消息（「你好呀。」「请问你。」「你是何人？」），每条 2 秒内收到角色回复；adb 注入只能产生 ASCII，中文必为手机端 ASR 输入 |
| ③ | 短按 → 如实显示「未识别到语音内容」 | **通过** | 落库中的「请问你。」即中途截断的短按结果，符合「短按只捕捉极短内容」的产品设计；未识别提示由验收实跑确认 |
| ④ | 按住滑出按钮**不中断**、抬起停止 | **通过** | 按压-保持按钮以 `setPointerCapture` 锁定指针（`ChatPage.tsx:692`），`onPointerUp`（`:695`）/`onPointerCancel`（`:699`）/`lostpointercapture` 收敛停止；滑出不再提前结束，验收实跑确认 |
| ⑤ | 按住中切后台 → 停止送达、**无 watchdog 泄漏** | **通过** | 验收窗口内服务端**无 watchdog、无会话泄漏、无 ERROR**；唯一连接事件为正常断连与租约回收（`连接断开，进入重连宽限 grace=15s` → `租约过期回收 reason=disconnected`），属设计内回收 |

## 27. 结论与状态

- `V039-R2-003`（P1，移动端「按住说话」PTT）：**通过，条目关闭**。真机状态由「失败」迁移为「通过」，迁移记录见台账 S4-R3 §9。
- 两个低危残留（被接管会话迟到失败的 error 噪音、串行停止最长推迟 30 s）在案，**不阻塞发布**。
- 本次修复落在移动端前端 5 个文件（`useVoiceCapture.ts`、`ChatPage.tsx`、`mobileStore.ts`、`chat.css`、`VoiceInput.test.tsx`），**服务端与协议未改动**。

---

# 真机测试终结与遗留 P3 离线收口（2026-09-11）

> 本批**不含任何真机执行**：未启动候选 EXE/APK、未连接真实设备、未调用真实模型或 DashScope、未重新打包、未修改真实账号配置、未清用户数据、未提交 git。以下结论只来自离线的代码核对、离线用例与用户裁决，不是新的实测。
> 本批是 V0.3.9 真机验证的最后一批：按 §28 裁决，全部真机测试就此终结。

## 28. 2026-09-11 用户裁决

| # | 裁决 | 性质与边界 |
| --- | --- | --- |
| 1 | **Android 浏览器维度按等效认定通过** | 依据是 Android 壳已通过全部已测维度，且此前实测服务端返回的 `index.html` 与候选 `mobile/dist` 逐字节相同。属**等效认定，不是浏览器端的独立实测**；§18 相关行据此改判（§29） |
| 2 | **全部真机测试就此终结** | 未执行项（iOS 两行无设备受阻、非局域网远程未测、语音的挂起恢复与 A11 缓冲溢出等）保持原记录状态，**不补写为通过** |
| 3 | **现有问题（R2-001、R2-002）修复并经离线测试通过后，即视为测试通过** | 以离线修复 + 离线验证替代真机复测；两条的离线收口见 §30 |

本裁决只改变「状态如何认定」与「是否继续排期真机批次」，不改变已记录的实测事实，也不把任何未执行项视为已执行。

## 29. Android 浏览器维度改判（等效认定）

§18 表中 Android 浏览器列原为「未执行」的六行，按 §28 裁决 1 改判如下：

| 维度 | 原记录 | 现记录 |
| --- | --- | --- |
| 网络（HTTPS / Tailscale / 断网重连 / PC 休眠恢复） | 未执行 | **通过（等效认定，非独立实测）** |
| 并发 | 未执行（浏览器仅验证连通与配对） | **通过（等效认定，非独立实测）** |
| 语音（输入/播放/打断/挂起恢复/溢出） | 未执行 | **通过（等效认定，非独立实测）** |
| 安全 | 未执行（未做负向注入） | **通过（等效认定，非独立实测）** |
| 通知与电源 | 未执行（页面声明浏览器端无系统通知与 Web Push） | **通过（等效认定，非独立实测）** |
| 交互与负载 | 未执行 | **通过（等效认定，非独立实测）** |

等效认定的依据：Android 壳已通过全部已测维度（§18 壳列、§22–§23），且 §18 网络行实测确认服务端返回的 `index.html` 与冻结候选的 `mobile/dist/index.html` 逐字节相同——浏览器加载的与壳内是同一份前端资源。

边界（不得扩大）：

- 该认定**不是**浏览器端的独立实测：浏览器端并未执行任何并发、安全、通知与电源、语音的链路操作或负向注入，本记录不声称这些行为在浏览器上被观测过。
- iOS Safari / iOS PWA 两行仍为**受阻（无设备）**，不适用等效认定。
- §18 的壳列与 iOS 列保持原样，本批未改动。

## 30. V039-R2-001 / V039-R2-002 离线修复验证

> 逐项改动、用例清单与状态迁移表见《V0.3.9-问题与修复台账.md》「修复批次回写（S4-R4，遗留 P3 离线修复，2026-09-11）」。

| 条目 | 离线修复 | 离线验证 | 状态 |
| --- | --- | --- | --- |
| `V039-R2-001`（`config.test_connection` 失败原因为空） | `_probe_dialogue_connection` 失败文案复用回合路径的 `_failure_reason()`：异常自述为空时回落真实类型名（`ConnectError`），不再出现「连接失败：」空壳；失败状态与探测目标归属不变，原始异常进日志 | `tests/unit/test_v039_r2_application_fixes.py` 2 用例（空自述回落、非空自述原样）；另有真实链路核对：进程内代理指向死端口 → 「连接失败：All connection attempts failed」 | **通过（离线修复 + 离线验证）** |
| `V039-R2-002`（`build-android.ps1` 产物 versionCode 恒为 1） | 调用 gradle 前读取 `tauri.conf.json` 的 `version`，按 `major*1000000+minor*1000+patch` 派生 versionCode（0.3.9 → 3009）并写出 `gen/android/app/tauri.properties`；版本非法即报错退出、不进打包 | `tests/unit/test_v039_r2_android_version_code.py` 10 用例（PS 5.1 解析、tempfile 沙箱整脚本 dry-run、派生式参数化、4 个非法版本、与真实 `build.gradle.kts` 核对键名）；旧脚本下 8 失败、修复后 10 通过 | **通过（离线修复 + 离线验证）** |

离线门禁：

| 命令 | 结果 |
| --- | --- |
| 根 `./.venv/Scripts/python.exe -m pytest -q` | **1107 passed / 4 skipped / exit 0** |

按 §28 裁决 3，两条**视为测试通过**。该「通过」不含任何真机证据：`V039-R2-002` 的 `adb install -r` 覆盖安装真机确认**未执行**（真机测试已终结），本记录不据此声称覆盖安装已在设备上验证。

## 31. A10 / A11 最终记录

判据取自《V0.3.9-真机验证需求.md》§3：A10「手机关闭、崩溃、弱网与多设备重连」的通过标准是「短暂重连受保护，失联租约到期回收，桌面播放权恢复」；A11「播放停滞至 PCM 缓冲溢出」的通过标准是「整条播放真实失败并释放资源，迟到分片不能恢复播放」。

| 用例 | 最终状态 | 依据 |
| --- | --- | --- |
| A10 | **部分覆盖，不作通过判定** | 通过标准的三条未全部满足，见下 |
| A11 | **未执行，不宣称通过** | 未取得任何证据，见下 |

**A10 已取得真机证据的子项**：多客户端并存与四会话并发（§18 并发行）、撤销后失联与如实提示（§18 安全行）、后台保活与通知投递（§18 通知与电源行）、断网重连自动恢复且无重复消息（§23）。**未满足的子项**：手机关闭、崩溃、弱网、失联租约到期回收、桌面播放权恢复均未取得独立取证。其中 §26 ⑤ 在 PTT 真机验收中曾观测到一次设计内的断连租约回收（`租约过期回收 reason=disconnected`），属该次验收的附带观测，**不构成 A10 的独立取证**。按 §28 裁决 2，真机测试已终结、未执行项不再补测；该判定为**最终记录**，不得读作通过。

**A11 明细**：客户端上限为 300 秒音频（≈14.4 MB 未播分片，`TTS_MAX_BUFFERED_PCM_BYTES`），从未构造到该量级，因此没有「整条播放真实失败并释放资源」「迟到分片不能恢复播放」的任何证据。按 §28 裁决 2 不再补测，**保持未执行，不宣称通过**。

## 32. 未执行项与终结状态（保持原判定）

| 项 | 状态 | 说明 |
| --- | --- | --- |
| iOS Safari / iOS PWA | **受阻（无设备）** | 保持原判定；真机测试已终结，不再安排 |
| HTTPS / Tailscale / 非局域网远程连接 | **未执行** | 按用户指示不测；已终结 |
| 移动端语音的挂起恢复、A11 缓冲溢出 | **未执行** | 保持原判定，见 §31 |
| 弱网、四聊天同时运行的交互负载 | **未执行** | §18 交互与负载行的壳列保留该未执行项 |
| 手机关闭/崩溃/弱网/租约到期回收/桌面播放权恢复 | **未执行** | 属 A10 未满足子项，见 §31 |
| A05（统一负载与长列表） | **不适用（产品决策：永久不再测试）** | 不受本批影响 |

## 33. 签收状态

V0.3.9 真机验证到此收口：判定依据是已记录的真机批次（§1–§27）与 2026-09-11 的三条裁决（§28）。本批不含新的真机执行——Android 浏览器六行为**等效认定**（§29），两条遗留 P3 为**离线修复 + 离线验证**（§30），A10/A11 与 iOS 等未执行项**保持原判定**（§31、§32）。本记录不解除 V0.4.0 的验收要求。
