# A00 真实链路对照说明

- 同一候选、同一 Key：`--demo` 下产出脚本化固定文本（见 ../F-01-demo-mode-scripted-reply/）；
  放置 `.env` 以 `--real` 重启后，角色回复为真实模型输出，会话标题与摘要均由真实模型生成。
- 证据：`db-messages.txt`（落库正文与时间戳）、`01-real-deepseek-reply.png`。
