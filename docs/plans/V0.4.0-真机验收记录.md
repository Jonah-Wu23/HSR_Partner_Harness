# V0.4.0 真机验收记录（T16 移动与公网隧道批次）

> 依据《V0.4.0 关键产品级决策.md》§5 与《V0.4.0-实施计划.md》§6 编制。
> 状态仅四种：**通过**、**失败**、**受阻**、**不适用**。
> 执行纪律：**批次只测不修**。批次内发现问题只在台账登记，不现场改代码。
> 准则约束：**Let It Fail, Let It Go**。真实失败如实暴露，不得用兜底伪造成功或掩盖未执行任务。

---

## 1. 批次信息

| 项目 | 记录 / 预填内容 |
| --- | --- |
| 批次编号 | `batch-v0.4.0-real-device`（待授权执行） |
| 实施时间 | 待定（真机前停止边界已达成） |
| 候选提交 / 分支 | `feat/v0.4.0` |
| 协议契约 | V0.4.0 契约增补（`docs/plans/V0.4.0-实施计划.md` §4） |
| PC 端构建物 | `desktop/src-tauri/target/release/hsr-partner-harness.exe` |
| Sidecar 构建物 | `resources/sidecar/pair-harness-sidecar/pair-harness-sidecar.exe` |
| 测试 PC 环境 | Windows 11 x64；Node.js / Rust / Python 3.11 环境 |
| 移动测试设备 | 真实手机终端（蜂窝网络，非同一 Wi-Fi） |
| 隧道环境 | Cloudflare Quick Tunnel（官方 `cloudflared` 二进制，版本 `2026.9.0`） |
| 真实服务 | 对话 DeepSeek；委派 Reasonix（ACP）；语音 DashScope（北京专属域） |
| 证据存放根目录 | `evidence/batch-v0.4.0-real-device/` |

---

## 2. 真机验收 12 项检查矩阵

严格依据《V0.4.0 关键产品级决策.md》§5 与《V0.4.0-实施计划.md》§6 设立以下 12 项检查矩阵：

| 序号 | 验收项 | 契约依据 | 验证方法与步骤 | 预期结果 | 实测状态 | 证据路径 / 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| **M01** | 默认回环监听隔离 | 决策稿 D2 / 计划 §4.1 | 默认启动应用（不带 `--lan`）。用同一局域网下的另一台设备（手机或另一台电脑）访问 PC 局域网 IP:8765。 | 连接被拒绝（Connection Refused），端口不对局域网开放；`serve.started` 上报 `mode: "loopback"`。 | 待测 | `evidence/.../M01/` |
| **M02** | 显式开启局域网直连 | 决策稿 D2 / 计划 §4.1 | 启动参数附加 `--lan`。局域网其他设备访问 PC 局域网 IP:8765，并观察桌面端远程设置面板。 | 局域网连接成功；桌面端常驻显示「局域网已暴露」黄色警告；关闭 `--lan` 或关闭开关后提示消失且局域网中断。 | 待测 | `evidence/.../M02/` |
| **M03** | 公网隧道与安全上下文 | 决策稿 D1 / 计划 §4.2 | 桌面端开启公网接入；手机**断开 Wi-Fi、使用蜂窝移动网络**，浏览器打开随机生成的 `https://<random>.trycloudflare.com` 页面。 | HTTPS 页面正常加载；浏览器麦克风权限提示正常弹出并可授权，Web Audio 安全上下文成立。 | 待测 | `evidence/.../M03/` |
| **M04** | 动态二维码与重启作废 | 决策稿 D1, D7 / 计划 §4.1 | 桌面端生成配对二维码，手机扫码完成配对；随后重启 PC 端应用，观察旧二维码与手机状态。 | 扫码成功进入对话；PC 重启后由于 Quick Tunnel 重新分配主机名，旧二维码与旧隧道地址失效，界面如实提示重配。 | 待测 | `evidence/.../M04/` |
| **M05** | 配对码限流与封锁退避 | 决策稿 D3 / 计划 §4.4 | 手机端连续 5 次输入错误配对码；第 6 次输入正确配对码；等待封锁期结束后再次输入正确码。 | 连续 5 次失败后触发封锁，提示重试倒计时（60s）；封锁期内正确码仍被拒绝（`rate_limited`）；解锁后成功配对；审计写 `pairing_rate_limited`。 | 待测 | `evidence/.../M05/` |
| **M06** | 单码有效性 | 决策稿 D4 / 计划 §4.5 | 桌面端点击「作废旧码并生成新码」，连续生成 2 次。手机端使用第一次生成的配对码尝试配对。 | 首次生成的旧码立即失效，返回 `invalid_code`；仅最后一枚生成的配对码可兑换 token；审计记 `pairing_code_issued`。 | 待测 | `evidence/.../M06/` |
| **M07** | 控制面来源分级收窄 | 决策稿 D6 / 计划 §4.2 | 已配对手机端经由 WebSocket 尝试调用控制面方法（如 `remote.issue_code`、`remote.revoke`、`remote.tunnel_start`）。 | 全部被鉴权门拦截，返回错误 `forbidden_scope`；审计记录 `scope_denied`；桌面端 stdin / 回环调用正常。 | 待测 | `evidence/.../M07/` |
| **M08** | 设备令牌生命周期 | 决策稿 D5 / 计划 §4.4, §4.5 | 手工将 SQLite 中某已配对设备令牌的 `expires_at` 修改为过去时间戳，随后该手机发起请求。 | 请求被拒并返回 `expired_token`，手机端明确提示「授权已过期，请重新配对」并不再重连；桌面端依然能列出并撤销该设备。 | 待测 | `evidence/.../M08/` |
| **M09** | 设备撤销即时生效 | 决策稿 D5 / 计划 §4.4 | 手机处于连接状态，桌面端在已配对设备列表中点击「撤销」。 | 手机端 WebSocket 连接被立即切断（关闭码 4401），之后重连鉴权返回 `revoked_token`，禁止重入。 | 待测 | `evidence/.../M09/` |
| **M10** | 隧道进程崩溃真实暴露 | 决策稿 D1 / 计划 §4.3 | 隧道运行在 `ready` 状态时，通过任务管理器或 `kill` 外部强行终止 `cloudflared.exe` 进程。 | 桌面端与事件流立即派发 `tunnel.failed`，状态切为 `failed` 并展示退出码与失败原因，严禁停留在「已连接」或伪装就绪。 | 待测 | `evidence/.../M10/` |
| **M11** | PWA 静态产物干净校验 | 决策稿 D7 / 计划 §4.6 | 在构建期与打包产物中检查 PWA 静态发布目录（执行 `validate-pwa-dist.ps1`）。 | 目录仅含编译后的 HTML/JS/CSS/Web 字体等静态资源，零用户数据、零私钥、零项目文件。 | 待测 | `evidence/.../M11/` |
| **M12** | 公网隧道语音全链路 | 计划 §6 第 12 条 | 手机蜂窝网络下按住说话，录制语音指令；角色回复并播放 TTS；助手执行工具命令。 | 手机端 ASR 识别字句准确上行；角色自然语言回复通过 DashScope TTS 流式下行播放；助手与工具执行事件保持静音无 TTS。 | 待测 | `evidence/.../M12/` |

---

## 3. 问题登记与修复台账（模版）

若在真机验收批次中发现缺陷，严格遵照「只测不修」纪律，在此处逐项登记：

| 问题编号 | 严重级 (P0-P3) | 触发用例 | 现象描述与影响 | 错误日志 / 契约偏差 | 根因初判 | 修复状态 |
| --- | --- | --- | --- | --- | --- | --- |
| *V040-R1-001* | *示例* | *M05* | *连续失败后提示文案未显示倒计时* | *缺少 retry_after_s 字段渲染* | *前端组件未解析 details 详情* | *待修复* |
| ... | | | | | | |

---

## 4. 停止边界与验收签收

### 4.1 真机前停止边界达成核对
- [x] T1 监听面收窄与 `--lan` 参数实现并通过离线测试
- [x] T2 配对码限流（连续 5 次失败封锁、退避翻倍、单码有效）已实现并通过测试
- [x] T3 令牌生命周期（30天绝对/7天空闲）与快照 v2 升级实现并通过测试
- [x] T4 控制面来源分级（仅回环/桌面端）与审计日志补齐并通过测试
- [x] T5 隧道管理模块（`tunnel.py` 下载、哈希校验、状态机、退出清理）已实现并通过测试
- [x] T6 Rust 侧 `--lan` 与隧道透传已实现
- [x] T7 桌面端 `RemotePairingPanel` 改造（动态 URL、隧道五态、局域网提示）已完成
- [x] T8 登录页标语更换（「一条会话，两条工作轨。」）已完成并通过测试
- [x] T9 移动端适配（`https`/`wss` 支持、三态错误友好提示）已完成
- [x] T10 PWA 静态目录构建期安全校验（34 项单元测试全绿）已完成
- [x] T11 文档同批更新（用户语音说明、计划路线图更新）已完成
- [x] T13 官方下载 URL、固定版本 `2026.9.0` 与 SHA256 哈希校验完备性核查完成
- [x] T15 数据库与配对状态平滑迁移、兼容性及零数据初启测试全部通过
- [x] T12 全量自动化门禁通过并留证：独立审核复跑 Python 1192 passed / 4 skipped、desktop 47 套件 497 passed、mobile 30 套件 361 passed、两次 typecheck 0 错误、cargo fmt 0 差异 + cargo test 28 passed；审核整改（3 项代码修复、3 项测试新增、夹具编码加固）后 Python 全量复跑 **1195 passed / 4 skipped**、迁移用例 7 passed——前端与 Rust 代码此后未再改动，其门禁结论延续有效。
- [x] T14 版本与打包完成并留证：五处版本号 0.4.0；审核整改后重跑 `npm run build:sidecar`（exit 0，PWA 校验两处通过）并经首次运行清理包装脚本重建 EXE 与 NSIS；Android arm64 Debug APK 构建成功并经包内内容级验证（见 §5.3）。
- [x] **当前状态：已抵达真机前停止边界。未启动真实设备测试，等待真机验收批次授权。**

### 4.2 签收结论
*（待真机验收批次真实执行后由指挥代理与测试负责人填写签收）*
- 矩阵通过数：0 / 12
- 矩阵失败数：0 / 12
- 签收状态：**待执行**

---

## 5. 独立审核整改与重新打包记录（2026-09-11）

T1–T15 交付后，独立审核子代理对 15 项声明逐条取证并复跑全部测试门禁，裁决 **FAIL**：四项阻断 + 一批建议缺陷。同日完成整改，本节为整改留证。

### 5.1 阻断项整改

**1. 重新打包（原 CRITICAL：安装包内 Sidecar 落后源码，缺 T15 最后一次修复）。**

- 重跑 `npm run build:sidecar`（exit 0，修改后的 PWA 校验在 `desktop/mobile/dist` 与 `resources/mobile-dist` 两处通过）与 `npm run tauri -- build --bundles nsis`（exit 0，经 `tauri-with-first-run-reset.ps1` 首次运行清理）。
- 包内证据：sidecar PYZ 内 `application_service` / `tunnel` / `pairing` / `ws_server` 四模块与 EXE 入口 `__main__` 脚本，经 marshal code object（co_code / co_consts / co_names / co_varnames）与工作区源码字节码级比对全部 **MATCH**；NSIS 包内提取的 `pair-harness-sidecar.exe` SHA256 与 resources 下新构建一致；迁移用例复跑 7 passed。
- 构建物哈希：

| 构建物 | SHA256 |
| --- | --- |
| `desktop/src-tauri/target/release/hsr-partner-harness.exe` | `28d48dbdff32722b95b3850517273a719906acb85c143934c9ff8614a71f344b` |
| `desktop/src-tauri/target/release/bundle/nsis/HSR Partner Harness_0.4.0_x64-setup.exe` | `c0e36c2b4c617c55533e6d21806d2738c5c930690c2a7ba8db65df7664fc05f2` |
| `desktop/src-tauri/resources/sidecar/pair-harness-sidecar/pair-harness-sidecar.exe` | `376de5e96dc4afd790ea237fc1934d7ebaa45b4a1c4c235238452b5d69275d21` |

**2. 候选提交建立**：按计划纪律逐路径 `git add`（未用 `git add -A`），提交 hash 回填至 §1 批次信息表。

**3. 台账补记**：§4.1 补 T12 / T14 两条核对项，T12 实测数字以独立审核复跑与本轮整改后复跑为准（此前的 1185 为 T15 修复前旧快照数字，作废）。

**4. darwin 哈希处置**：经 GitHub Release API（2026.9.0 tag）核对，官方 `cloudflared-darwin-amd64.tgz` / `cloudflared-darwin-arm64.tgz` SHA256 为 `8f2ecf41776d942bcc8070a56e7bafa4c5de70a1d1781110e2eb3774cca512a8` / `c0eccb3758420d1f4e46cbf2b8ecde01d9802a154232a817f25133340009fcc7`，原表两项确为错误值；且官方 darwin 资产是 `.tgz` 压缩包，原实现无解压流程，macOS 上本就无法工作。处置：删除 darwin 条目，`detect_platform_asset` 在 macOS 显式抛 `unsupported_platform`（产品未承诺 macOS 分发，不做未经真实验证的解压路径）；其余四平台哈希经同一 API 逐字核对无误。

### 5.2 建议缺陷同步修复

- **配对封锁失败路径当刻落盘**：`application_service.py` `_remote_pair` 失败分支补 `_persist_pairing_state()`，失败计数与封锁退避在拒绝请求当刻持久化（§4.4「重启不重置」），新增端到端测试（封锁状态跨重启存活、封锁期内正确码仍被拒）。
- **`tests/unit/test_pairing.py` 审计断言写虚**：原断言因 and/or 优先级退化为只数条数；改为逐方法校验 `scope_denied` 审计存在且 detail 含方法名与来源。
- **`validate-pwa-dist.ps1`**：forbidden 扩展检查提前（原为死分支——白名单先行导致 Forbidden 报错永不可达）；内容扫描从 json/txt/webmanifest 扩至 js/mjs/cjs/css/html/htm/map（打包产物主体）；新增 2 项测试，真实 dist 通过。
- **D7 标签引用链闭合**：决策稿 §4 第 20 条补「（D7）」标注；实施计划 §4.6 点明 D7「隧道 URL 不是秘密」与 PWA 静态目录约束的推导关系。
- **测试夹具编码健壮性**：三个 PowerShell 驱动测试文件在 `PYTHONUTF8=1` 环境按 UTF-8 解码 GBK 输出崩溃（12 项失败），改为按系统 ANSI（mbcs）解码并 errors=replace，复跑全绿。

### 5.3 Android APK（T14）

环境此前判定受阻，实际在 `E:\AI\CHD-class-table` 下齐备：JDK 17（Temurin 17.0.20.1）、Android SDK（platforms android-34/35/36）、NDK 28.2.13676358。2026-09-12 以注入环境变量方式执行 `scripts/build-android.ps1`，构建 arm64 Debug APK：

- 版本派生正确：`gen/android/app/tauri.properties` 写出 `versionName=0.4.0` / `versionCode=4000`（0.4.0 → 4×10⁶，符合派生式）。
- 产物：`desktop/src-tauri/gen/android/app/build/outputs/apk/arm64/debug/app-arm64-debug.apk`（129,439,676 字节，SHA256 `af27908ed5bbd1fdd76808d5145c89308de2791483d914c707462ec5ba8f1258`）。
- 包内内容级验证：`assets/assets/index-C-JeaTkU.css`（41,189 B）与 `assets/assets/index-C_8QOZa5.js`（343,580 B）与 `desktop/mobile/dist/assets/` 当前构建哈希名一致——新前端代码已打入 APK，非旧资源残留；`lib/arm64-v8a/libhsr_partner_harness_lib.so` 为本次 Rust 构建新产物。
- 备注：gradle 产出 APK 后脚本输出管道未回收（后台进程挂起被终止），脚本的 "Built APK" 收尾输出未能留档，以上结论均来自产物本身的内容级核验，不依赖脚本回执。
